// tests/unit/user-credential-db.test.ts
// Wave 0 stubs for UserCredential Prisma CRUD (src/lib/user-credential-db.ts — created in Plan 03)
// These stubs are placeholders; they will be filled in once the implementation file exists.

import { describe, it } from 'vitest';

describe('user-credential-db (stub)', () => {
  it.todo('upsertCredential calls prisma.userCredential.upsert with user_id and encrypted_state');
  it.todo('getCredential returns null when no record exists for user_id');
  it.todo('getCredential returns the record when it exists for user_id');
});

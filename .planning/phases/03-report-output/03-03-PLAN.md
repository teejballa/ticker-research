---
phase: 03-report-output
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/research/[ticker]/page.tsx
  - src/components/SetupWizard.tsx
  - src/components/TickerSearch.tsx
  - src/components/ChartConfirmation.tsx
  - src/components/ResearchProgress.tsx
autonomous: false

requirements:
  - REPT-01
  - REPT-03

must_haves:
  truths:
    - "Every page and component in the app has a dark (bg-zinc-950) background — no white or gray-50 surfaces visible"
    - "All text labels use amber-400 or zinc-400 palette — no blue-600, gray-800, or gray-900 text"
    - "All buttons use amber-400 accent (border or background) — no blue-600 buttons"
    - "All rounded corners and box-shadows are removed — terminal aesthetic has sharp edges"
    - "The app feels like a Bloomberg terminal from first load through report display"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Terminal-themed home page with zinc-950 background"
      contains: "zinc-950"
    - path: "src/components/SetupWizard.tsx"
      provides: "Terminal-themed setup wizard"
      contains: "amber-400"
    - path: "src/components/TickerSearch.tsx"
      provides: "Terminal-themed ticker search"
      contains: "zinc-950"
    - path: "src/components/ChartConfirmation.tsx"
      provides: "Terminal-themed chart confirmation"
      contains: "amber-400"
    - path: "src/components/ResearchProgress.tsx"
      provides: "Terminal-themed research progress"
      contains: "zinc-950"
  key_links:
    - from: "src/app/layout.tsx"
      to: "all pages"
      via: "body class bg-zinc-950 ensures consistent dark background even during navigation"
      pattern: "zinc-950"
---

<objective>
Restyle the entire app to the Bloomberg terminal aesthetic — dark background, amber accents, monospace feel — across all existing components and pages. End with a human verification checkpoint to confirm the visual result.

Purpose: The report (Plan 02) already uses the terminal theme. This plan brings the full app flow into visual consistency: home page, setup wizard, ticker search, chart confirmation, and research progress all look like a Bloomberg terminal.

Output: Restyled app with zero white/light-gray surfaces in the user-facing flow; human confirms visual quality.
</objective>

<execution_context>
@/Users/tj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-report-output/03-CONTEXT.md
@.planning/phases/03-report-output/03-RESEARCH.md
@.planning/phases/03-report-output/03-02-SUMMARY.md

<interfaces>
<!-- Terminal palette — apply uniformly across all components -->

Terminal color mapping from RESEARCH.md:
```
bg-gray-50       → bg-zinc-950  (page backgrounds)
bg-white         → bg-zinc-900  (card/panel surfaces)
border-gray-200  → border-zinc-700
text-gray-900    → text-amber-400 (headings/labels)
text-gray-800    → text-zinc-200 (body text)
text-gray-500    → text-zinc-400 (muted text)
text-gray-400    → text-zinc-600 (dim text)
text-blue-600    → text-amber-400 (accent/interactive)
bg-blue-600      → bg-amber-400 text-black (primary buttons)
border-blue-500  → border-amber-400 (focus rings, spinners)
text-green-600   → text-emerald-400 (success/positive)
text-red-500     → text-red-400 (error/negative)
rounded-xl       → remove (sharp corners)
shadow-sm        → remove (flat terminal look)
```

Spinner pattern update:
  border-blue-500 border-t-transparent → border-amber-400 border-t-transparent

Input fields:
  bg-white border-gray-200 text-gray-900 → bg-zinc-900 border-zinc-700 text-amber-400
  placeholder-gray-400 → placeholder-zinc-600
  focus:ring-blue-500 → focus:ring-amber-400 (or focus:border-amber-400)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle layout, home page, SetupWizard, and TickerSearch to terminal aesthetic</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/components/SetupWizard.tsx, src/components/TickerSearch.tsx</files>
  <action>
    Apply the terminal color mapping from the interfaces block systematically to each file. Read each file first, then apply all changes in one write.

    src/app/layout.tsx:
    - Set body class to include `bg-zinc-950 text-zinc-200` so even the root background is dark during navigation flash. The html element should have the same background if possible.

    src/app/page.tsx:
    - Replace `bg-gray-50` background with `bg-zinc-950`
    - Replace any white cards with `bg-zinc-900 border border-zinc-700`
    - Remove `rounded-xl`, `shadow-sm` from all elements
    - Replace blue accents with amber-400
    - Apply `font-mono` to labels and data values
    - Replace all gray text shades with zinc equivalents per mapping above

    src/components/SetupWizard.tsx:
    - Replace `bg-white` / `bg-gray-50` / `bg-gray-100` with zinc equivalents
    - Step indicators: replace blue circle (bg-blue-600) with amber (bg-amber-400 text-black for active, border-amber-400 for pending, bg-zinc-700 for future)
    - Check/checkmark icons: replace text-green-600 with text-emerald-400
    - Error states: text-red-500 → text-red-400
    - Buttons: primary → bg-amber-400 text-black hover:bg-amber-300 font-mono; secondary → border border-zinc-700 text-zinc-400 hover:border-amber-400 hover:text-amber-400
    - Install progress bar: replace blue with amber (border-amber-400 border-t-transparent for spinner)
    - Remove rounded-xl from all card containers; keep rounded-full only on circle step indicators if present
    - Section headings: font-mono uppercase tracking-widest text-zinc-400 text-xs

    src/components/TickerSearch.tsx:
    - Input field: bg-zinc-900 border-zinc-700 text-amber-400 placeholder-zinc-600 font-mono
    - Focus state: focus:outline-none focus:border-amber-400 (no ring-blue)
    - Search button: bg-amber-400 text-black hover:bg-amber-300 font-mono font-bold
    - Dropdown results (if any): bg-zinc-900 border-zinc-700 text-zinc-200 hover:bg-zinc-800
    - Error/shake state: text-red-400
    - Remove all rounded-xl, shadow-sm, bg-white, bg-gray-50
  </action>
  <verify>
    <automated>cd /Users/tj/Desktop/Ticker-Research && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>layout.tsx body has bg-zinc-950; page.tsx has no bg-gray-50 or bg-white; SetupWizard.tsx and TickerSearch.tsx have no blue-600 or gray-50 classes; npm test passes</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ChartConfirmation, ResearchProgress, and research page error/loading states</name>
  <files>src/components/ChartConfirmation.tsx, src/components/ResearchProgress.tsx, src/app/research/[ticker]/page.tsx</files>
  <action>
    Apply the same terminal color mapping to the remaining components. Read each file first.

    src/components/ChartConfirmation.tsx:
    - Page/wrapper background: bg-zinc-950
    - Chart container: bg-zinc-900 border border-zinc-700 (PriceLineChart renders on dark canvas — the lightweight-charts library should render fine on dark background)
    - Price display: text-amber-300 font-mono font-bold (the main price number)
    - Percent change: positive → text-emerald-400, negative → text-red-400
    - Stats grid (market cap, exchange, etc.): label in text-zinc-400 text-xs font-mono, value in text-zinc-200 font-mono
    - "Confirm — Run Analysis" button: bg-amber-400 text-black hover:bg-amber-300 font-mono font-bold uppercase tracking-wide
    - "Search Again" / secondary: border border-zinc-700 text-zinc-400 hover:border-amber-400 hover:text-amber-400 font-mono
    - Remove rounded-xl, shadow-sm, bg-white, bg-gray-*

    src/components/ResearchProgress.tsx:
    - Wrapper: bg-zinc-950 (or bg-zinc-900 for the card)
    - Step list: each step label in font-mono text-sm; active step in text-amber-400; completed in text-emerald-400; pending in text-zinc-600
    - Step indicator dots/icons: active = bg-amber-400, complete = bg-emerald-400 or checkmark, pending = bg-zinc-700
    - Progress spinner: border-amber-400 border-t-transparent (replacing border-blue-500)
    - "Research complete" success state: text-emerald-400 font-mono
    - Error state: text-red-400
    - Remove all blue, bg-white, rounded-xl, shadow-sm

    src/app/research/[ticker]/page.tsx — restyle the non-complete states:
    - loading state: bg-zinc-950, text-zinc-500 font-mono (the "Loading..." text)
    - error state ("Analysis Failed"): bg-zinc-950 wrapper; heading text-amber-400 font-mono; body text-zinc-400; "Try Again" button → bg-amber-400 text-black font-mono; "Back to Search" → border border-zinc-700 text-zinc-400 font-mono; remove rounded-xl
    - "Ticker Not Found" error state: same pattern as above
    - analyzing state wrapper: bg-zinc-950 instead of bg-gray-50
    - Do NOT modify the complete state (that's handled by ResearchReport from Plan 02)
  </action>
  <verify>
    <automated>cd /Users/tj/Desktop/Ticker-Research && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>ChartConfirmation.tsx, ResearchProgress.tsx, and research page.tsx states all use zinc/amber/emerald/red palette; no blue-600, bg-white, bg-gray-50, or rounded-xl in any of these files; npm test passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of Bloomberg terminal aesthetic app-wide</name>
  <what-built>
    Full Bloomberg terminal restyle of the entire app — dark zinc-950 backgrounds, amber-400 accents, font-mono feel throughout the complete user flow:
    - Home page (ticker search input)
    - Setup wizard (3-step Python/auth flow)
    - Chart confirmation page
    - Research progress (6-step analysis stream)
    - Research report (Plan 02, already terminal-themed)
  </what-built>
  <how-to-verify>
    Run the dev server:
    ```
    cd /Users/tj/Desktop/Ticker-Research
    npm run dev
    ```
    Then verify each screen:

    1. Home page (http://localhost:3000): Dark background (near-black). Amber ticker input with monospace font. No white cards or blue buttons visible.

    2. If setup wizard appears: Steps shown in amber/emerald/zinc palette. No blue circles or white backgrounds.

    3. Ticker search — type "AAPL" and hit enter: Chart confirmation page uses dark background. Price in amber monospace. "Confirm — Run Analysis" is an amber button with black text. No white backgrounds.

    4. Run analysis (optional, takes ~3 minutes): Progress page shows dark background with amber/zinc step indicators.

    5. Report page (if analysis completes): Sticky amber bar at top. Terminal-style sections. "DOWNLOAD PDF" button in amber outline. Click it — print preview should show a white-background, black-text print layout.

    6. General check: No rounded-xl card shapes anywhere. No shadows. No blue-600 buttons. App feels like a terminal throughout.
  </how-to-verify>
  <resume-signal>Type "approved" if the terminal aesthetic is visually correct throughout. Describe any screens that still show the old white/blue styling.</resume-signal>
  <action>Run npm run dev and visually inspect each screen of the app as described in how-to-verify.</action>
  <verify>
    <automated>MISSING — visual inspection required; see how-to-verify instructions</automated>
  </verify>
  <done>All screens use Bloomberg terminal aesthetic — zinc-950 backgrounds, amber-400 accents, no white/blue surfaces visible anywhere in the user flow</done>
</task>

</tasks>

<verification>
- `grep -rn "bg-gray-50\|bg-white\|bg-blue-600\|text-blue-600\|rounded-xl\|shadow-sm" /Users/tj/Desktop/Ticker-Research/src/components/SetupWizard.tsx /Users/tj/Desktop/Ticker-Research/src/components/TickerSearch.tsx /Users/tj/Desktop/Ticker-Research/src/components/ChartConfirmation.tsx /Users/tj/Desktop/Ticker-Research/src/components/ResearchProgress.tsx` returns zero results
- `npm test` passes
- Human checkpoint approved
</verification>

<success_criteria>
- All app components use terminal palette (zinc-950 background, amber-400 accents, zinc-* muted text)
- No white backgrounds, no blue-600 accents, no rounded-xl corners, no shadow-sm in any restyled file
- Home page, setup wizard, chart confirmation, research progress all have terminal aesthetic
- Full app flow feels visually consistent from first load through report display
- npm test passes
- Human checkpoint: visual inspection approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-report-output/03-03-SUMMARY.md`
</output>

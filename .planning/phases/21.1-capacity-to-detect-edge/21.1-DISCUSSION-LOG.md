# Phase 21.1: Capacity to Detect Edge — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `21.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-06-02
**Phase:** 21.1-capacity-to-detect-edge
**Areas discussed:** Scope vs Phase 23, LLM direct-evaluation framing, Logistic baseline scope, Target labels + thresholds + features

---

## Scope vs Phase 23

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal FDR gate now; Phase 23 layers DSR + LearningEvent on top | P21.1 wires BH/BY-FDR into patternStatus + records n_trials_attempted (CORE-ML-17 minimal). Phase 23 later adds Deflated Sharpe Ratio (CORE-ML-18), LearningEvent (CORE-ML-19), full Brier-lift tuning (CORE-ML-15). | |
| Pure measurement; Phase 23 owns ALL promotion-gate work | P21.1 ships IC + bootstrap CIs + logistic baseline + labels + features. ZERO change to patternStatus. Phase 23 owns all of CORE-ML-15..19. | |
| P21.1 absorbs Phase 23 entirely | Phase 23 retired. P21.1 ships FDR + DSR + LearningEvent + Brier-lift threshold. | |
| Punt on FDR; only ship measurement + labels + baseline + features | Skip even minimal FDR gate. Phase 23 owns ALL gate work. Smallest scope; fastest. | |

**User's choice:** "combine them, no progress lost, just altogther in 21.1"
**Notes:** User chose to merge Phase 23 into P21.1 entirely, preserving all five CORE-ML-15..19 requirements. Phase 23 will be retired from the roadmap when this phase ships. REQUIREMENTS.md will be remapped during planning. Phase scope grows from 5 → ~7 plans.

---

## LLM direct-evaluation framing

| Option | Description | Selected |
|--------|-------------|----------|
| Both IC + Brier | IC (Spearman) on confidence-weighted directional score AND Brier on Buy probability. Two complementary headline numbers. | ✓ |
| IC only (Spearman) | Single number, simplest. Quantopian/Numerai convention. Loses calibration info. | |
| Brier only | Proper scoring rule. Forces calibration. Loses ranking-quality info. | |
| Three-way log loss (Buy/Hold/Sell) | Most rigorous. Loses directional ranking story. | |

**User's choice:** Both IC + Brier (Recommended)
**Notes:** IC measures ranking quality, Brier measures calibration. Both surfaced on dashboard.

---

## LLM evaluation scope

| Option | Description | Selected |
|--------|-------------|----------|
| All predictions | Score every Buy/Hold/Sell regardless of confidence. Maximizes sample size; tests self-calibration of low-confidence calls. | ✓ |
| High-confidence only (≥ 70%) | Score only confident calls. Smaller n; mirrors discretionary trader workflow. | |
| Both — stratified by confidence bucket | Per-quartile IC/Brier. Detects miscalibration patterns. Most informative; biggest dashboard. | |

**User's choice:** All predictions (Recommended)
**Notes:** No confidence filter. Tests the entire LLM output distribution.

---

## Logistic baseline feature scope

| Option | Description | Selected |
|--------|-------------|----------|
| Same 24 numeric features post-z-score (apples-to-apples vs engine) | Cleanest comparison. LLM beats it → LLM's edge is qualitative synthesis. | |
| Canonical small set (5–7 lit-cited features) | RSI, MACD, sentiment%, insider flow, institutional flow, put/call, sector. Maximally defensible. | |
| Full SourcePackage with text embeddings | Highest comparability with LLM. Big engineering surface. | |
| Both 24-feature + canonical side-by-side | Two baseline columns. 'LLM beats canonical' + 'LLM beats 24-feature' are independent claims. | ✓ (chosen via meta-rule) |

**User's choice:** "if something in claude.md is holding back the progression, functionality, and potential of this project, skip it in this step and pick the best"
**Notes:** User explicitly authorized overriding CLAUDE.md constraints in favor of progress / functionality / potential. Interpreted as "pick the most ambitious technically-defensible option" — both baselines side-by-side. Encoded as guiding principle in CONTEXT.md `<specifics>`.

---

## Logistic training method

| Option | Description | Selected |
|--------|-------------|----------|
| Bayesian logistic with Laplace approx (existing in learning.ts) | Reuses predictLogistic + sigmoid + online updates. PRIOR_PRECISION knob. Same code path as engine. | ✓ |
| Sklearn-style L2-regularized logistic (new module) | Closed-form / IRLS solver with explicit L2. Standard ISL Ch. 4 reference. New module ~200 LOC. | |
| Elastic net (L1 + L2) | Adds feature selection. Overkill for 24–36 features. | |

**User's choice:** Bayesian logistic with Laplace approximation (Recommended)
**Notes:** Reuses existing code, no new framework, fair head-to-head.

---

## σ-aware hit threshold parameters

| Option | Description | Selected |
|--------|-------------|----------|
| k=1, 60d rolling sector σ — single, locked | Industry standard. One coherent label across horizons. | ✓ |
| Multiple k values in parallel (0.5, 1, 1.5) | Three label columns; data picks winning k per cell. Most informative; most surface. | |
| k=1 with three windows (30/60/90d σ) | Vary lookback not k. Tests vol-regime sensitivity. 3× label columns. | |

**User's choice:** k=1, 60-day rolling sector σ — single, locked (Recommended)
**Notes:** Clean for the paper. Replaces current 1% flat threshold as canonical engine output.

---

## Primary engine output label

| Option | Description | Selected |
|--------|-------------|----------|
| σ-aware (k=1) primary; 1% flat + directional secondaries | Replaces 1% flat. Stable across horizons. Both diagnostics retained. | ✓ |
| Directional primary (>0% beat sector) | Highest SNR (~50% base rate). Easiest target to detect edge against. | |
| Keep 1% flat primary; σ-aware secondary | Least churn. Cross-horizon mismatch persists. | |

**User's choice:** σ-aware (k=1) as primary; 1% flat + directional as secondaries (Recommended)
**Notes:** All three labels written on every PriceOutcome. Single shared compute path per CLAUDE.md rule #6.

---

## Z-score feature design

| Option | Description | Selected |
|--------|-------------|----------|
| Both ticker-relative rolling AND cross-sectional (12 → 36) | Technical analysis + factor research literatures both supported. Most expressive. PRIOR_PRECISION = 8 with annealing. | ✓ |
| Ticker-relative 60d rolling only (12 → 24) | Standard technical transform. Originally-scoped option. | |
| Cross-sectional only (vs today's universe, 12 → 24) | Standard factor transform. Loses ticker-history context. | |

**User's choice:** Both ticker-relative rolling AND cross-sectional, side-by-side (Recommended)
**Notes:** 36 total features. PRIOR_PRECISION annealed (8 when n<100, 4 when n<500, 1 when n≥500). Honors ISL Ch. 6 ridge/lasso shrinkage when sparse.

---

## Claude's Discretion

- Exact Brier-lift threshold value for ACTIVE candidacy (D-07) — researcher picks via sensitivity sweep on backfill
- Ridge precision values to sweep (D-13) — researcher picks set
- BCa bootstrap implementation details (acceleration, n<50 edge cases)
- Cron schedule for `/api/cron/baseline-eval` — planner picks non-conflicting window
- UI styling for three-way comparison table — follow existing Insights patterns
- Internal organization of the `evaluation/` namespace — planner decides

## Deferred Ideas

- GBM / XGBoost baseline → P21.2
- Cross-signal cells (insider × technical etc.) → P21.2
- Embedding-rich logistic with full SourcePackage inputs → P21.2 if surface allows
- Event-conditional predictions (PEAD / post-13F / post-insider-cluster windows) → P21.3
- Full LLM backtest on historical SourcePackages (~$630, 12k+ predictions) → P21.4 (paper-grade)
- Triple-barrier labels (López de Prado 2018) → deferred unless results suggest needed
- Hierarchical Bayesian sector pooling → preserved Phase 21 deferral
- Conformal prediction intervals → preserved Phase 21 deferral
- Lo-Mamaysky-Wang conditional-distribution evaluation → preserved Phase 21 deferral
- Causal-inference signal attribution → REQUIREMENTS.md v2.1+ deferral preserved
- Phase 22 (Market-Regime) discuss eligibility — slides until after P21.1 ships and relearn produces post-FDR results

## Meta-rule captured

User explicitly authorized overriding CLAUDE.md constraints when they would block project potential. Applied to feature-scope decision (both baselines instead of one) and may apply to future gray areas in subsequent phases. Worth saving to memory as user feedback.

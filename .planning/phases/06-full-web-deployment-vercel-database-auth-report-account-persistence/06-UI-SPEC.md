---
phase: 6
slug: full-web-deployment-vercel-database-auth-report-account-persistence
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for Phase 6: Full Web Deployment — Vercel, Database, Auth, Report & Account Persistence.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom Tailwind v4 utility classes throughout |
| Preset | not applicable |
| Component library | none (hand-rolled components, no Radix/Base UI) |
| Icon library | none (inline SVG and Unicode symbols used in existing components) |
| Font | IBM Plex Mono — 400, 700 weights loaded from Google Fonts via next/font |

**Source:** Detected from `src/app/layout.tsx` (IBM_Plex_Mono import), `package.json` (no icon package), `globals.css` (Tailwind v4 @import).

**shadcn gate result:** `components.json` not found. Stack is Next.js 15 + React 19. shadcn was NOT initialized for this project — all phases have used custom utility-first Tailwind. Continue with the established manual pattern. Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding, nav dot spacing |
| sm | 8px | Button inner padding (vertical), compact label gaps |
| md | 16px | Card inner padding, form field spacing, nav horizontal padding |
| lg | 24px | Section padding, card-to-card gaps |
| xl | 32px | Page-level horizontal padding on desktop |
| 2xl | 48px | Between major page sections |
| 3xl | 64px | Vertical rhythm between hero and content |

Exceptions:

| Exception | Value | Justification |
|-----------|-------|---------------|
| Sign-in page card padding | 32px all sides (`p-8`) | Matches established pattern from RESEARCH.md custom sign-in example; aligns with xl token |
| Touch target minimum height | 40px | Accessibility floor — all interactive controls meet this minimum regardless of content height |
| Nav bar height | 36px | Preserved from existing `mockup-nav` pattern already in `globals.css`; not a multiple-of-4 content gap but a fixed chrome dimension |

**Source:** Inferred from existing `globals.css` values (`padding: 32px 28px`, `padding: 18px 22px 20px`, etc.) and RESEARCH.md sign-in page code example.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.6 |
| Label / Overline | 11px | 700 | 1.4 |
| Heading | 20px | 700 | 1.2 |
| Display / Wordmark | clamp(56px, 9vw, 100px) | 700 | 1.0 |

**Active weights: 400 and 700 only. No other weights may be introduced in this phase.**

**Letter spacing rules (terminal aesthetic):**
- Overlines and status labels: `tracking-widest` (0.25–0.35em) — matches existing `.how-step-title`, `.mockup-brand`
- Body text: `tracking-normal` to `tracking-wide` (0.04–0.08em) — matches existing `.mockup-step-label`
- Button / CTA text: `tracking-widest` (0.12–0.2em) — matches existing `.nav-cta-btn`
- Monospace tabular numerics: `font-variant-numeric: tabular-nums` — required for all price and timestamp displays

**New surface in this phase — sign-in page typography:**
- Authentication header: 11px, weight 700, letter-spacing 0.25em — rendered as overline (maps to Label / Overline row)
- Button label: 11px, weight 700, letter-spacing 0.12em, uppercase (maps to Label / Overline row)
- Body copy below button (disclaimer): 11px, weight 400, muted zinc-600 (maps to Label / Overline size, body weight)

**Source:** Detected from `globals.css` (.how-step-title, .how-step-desc, .mockup-brand, .nav-cta-btn), `layout.tsx` (IBM_Plex_Mono weight array), RESEARCH.md sign-in page example.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#080a0f` | Page background, body, `<html>` and `<body>` background — set in layout.tsx |
| Secondary (30%) | `#0d1117` (panels) / `#0a0e17` (nav/chrome) | Cards, panel surfaces, nav bar background, auth container background |
| Accent (10%) | `#f59e0b` (amber-400) / `#fbbf24` (amber-300 hover) | Reserved list below |
| Destructive | not applicable in this phase — no delete/remove flows |

**Accent reserved for:**
1. Sign-in page authentication header text (e.g., "TICKER RESEARCH // AUTHENTICATION REQUIRED")
2. Sign-in button border on hover state (existing: `hover:border-amber-400`)
3. Sign-in button text on hover state (existing: `hover:text-amber-400`)
4. NavIdentity connected-account indicator dot / highlight (when user is authenticated)
5. Active/selected state on the user identity display in the nav bar

**Semantic status colors (not accent — used sparingly for data states):**
- Bullish / success: `#10b981` (emerald-500)
- Bearish / error: `#ef4444` (red-500)
- Neutral / muted UI text: `#4a6a8a` (blue-slate muted)
- Disabled / pending: `#2a4560` (deep blue-slate)

**Border color:** `#1a2d42` — used for all card borders, separator lines, and the sign-in container border.

**Text colors:**
- Primary body text: `#c9d4e0` — set on `<body>` in layout.tsx
- Secondary / muted labels: `#4a6a8a`
- Disabled / deeply muted: `#2a4560`
- Amber accent text: `#f59e0b`

**Source:** Detected from `globals.css` (`.panel`, `.mockup-nav`, `.dot-grid`, scrollbar colors), `layout.tsx` (`bg-[#080a0f]`, `text-[#c9d4e0]`), CONTEXT.md ("Terminal aesthetic locked: zinc-950 bg, amber-400 accents, IBM Plex Mono"), RESEARCH.md sign-in page code example.

---

## Component Inventory

New surfaces introduced in Phase 6 and their visual contracts:

### 1. Custom Sign-In Page — `/auth/signin`

**Layout:** Full-screen `min-h-screen`, flex-centered vertically and horizontally. Background: `#080a0f` (dominant). Scanlines overlay inherited from body.

**Focal point:** Amber overline text is the primary visual anchor — eye enters at the amber header, descends to the button. No competing visual elements on this page.

**Container:** `border border-zinc-800` (maps to `#1a2d42`), `p-8` (32px), width `w-96` (384px) — exactly as in RESEARCH.md verified code example. No border-radius — flat terminal edges.

**Header block:**
- Overline text: "TICKER RESEARCH // AUTHENTICATION REQUIRED"
- Color: `#f59e0b` (amber accent)
- Size: 11px, weight 700, tracking 0.25em
- Margin-bottom: 24px

**Button:**
- Label: "[ CONNECT GOOGLE ACCOUNT ]"
- Default state: `border border-zinc-700` (`#1a2d42`), `text-zinc-300` (`#c9d4e0`), full-width, 40px height minimum
- Hover state: `border-amber-400`, `text-amber-400`, transition 150ms
- No background fill on default — ghost/outline style preserving terminal aesthetic
- Font: IBM Plex Mono, 11px, weight 700, tracking 0.12em

**Error state (invalid/expired session):**
- Small muted text below button: "Authentication failed. Try again."
- Color: `#4a6a8a`, 11px, weight 400

### 2. NavIdentity — Authenticated User Display (existing pattern extended)

From Phase 5, `NavIdentity` renders `userEmail` from `/api/setup/status`. In web mode, this renders the NextAuth session email instead.

**Visual contract:**
- Connected state: "CONNECTED AS you@gmail.com" — 11px, weight 700, muted `#4a6a8a`, truncated to 24 chars with ellipsis
- Sign-out affordance: None required in this phase (local mode parity — no sign-out in scope per CONTEXT.md)
- Background: no background — inline in nav bar, same row as brand

### 3. Route Protection Redirect

No new visual surface — unauthenticated requests redirect to `/auth/signin` via NextAuth middleware. The sign-in page IS the unauthenticated state.

### 4. History List in Web Mode (existing `ReportHistory` component, Neon-backed data)

No visual changes to `ReportHistory` component. DEPLOYMENT_MODE=web switches the data source (Neon API vs. filesystem) but the rendered output is identical. Do not modify the component's visual contract.

---

## Interaction States

### Sign-In Page

| State | Trigger | Visual |
|-------|---------|--------|
| Default | Page load | Container renders, button is ghost outline |
| Button hover | Mouse over | Border + text transition to `#f59e0b` over 150ms |
| Button active (click) | User clicks | `signIn('google', { callbackUrl })` fires — browser redirect to Google OAuth |
| Returning authenticated | Middleware passes | Redirect away from sign-in to callbackUrl — user never sees this state |
| Auth error | Google auth fails | `?error=` param present in URL — show error copy below button |

### Middleware Redirect (no UI — behavioral contract only)

- Local mode (`DEPLOYMENT_MODE` unset): middleware is a no-op, all routes pass through
- Web mode (`DEPLOYMENT_MODE=web`): all routes except `/api/auth/*`, `/_next/*`, `/favicon.ico` require an active NextAuth session; unauthenticated requests receive HTTP 307 to `/auth/signin`

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (sign-in button) | `[ CONNECT GOOGLE ACCOUNT ]` |
| Sign-in page header | `TICKER RESEARCH // AUTHENTICATION REQUIRED` |
| Auth error (bad/expired token) | `Authentication failed. Return to sign-in.` |
| Middleware redirect (no UI — 307 only) | n/a |
| Empty history state (web mode, no reports) | `NO REPORTS YET` / `Run your first ticker analysis to see results here.` |
| Empty history state (loading) | `LOADING HISTORY...` (existing pattern — terminal all-caps label) |
| NavIdentity connected | `CONNECTED AS {email}` (truncated at 24 chars) |
| NavIdentity not connected (web mode should not occur — auth is required) | n/a |
| Page title (`<title>`) | `Equinfo — AI Financial Research Terminal` (unchanged from layout.tsx) |

**Tone rules (must carry through all new copy):**
- All-caps for status labels, overlines, and system messages — consistent with existing terminal aesthetic
- Bracket notation `[ ACTION ]` for primary interactive buttons — consistent with existing `[ CONFIRM TICKER ]` pattern
- No exclamation marks. No marketing language. Factual, terse, operational.

**Source:** CONTEXT.md ("NextAuth sign-in page should be fully custom — terminal aesthetic must be maintained"), RESEARCH.md sign-in page code example, existing component patterns from `globals.css` (.mockup-brand, .mockup-live, overline labels).

---

## Animation & Motion

Phase 6 introduces one new page (sign-in). Motion contract:

**Sign-in page:**
- No entry animation — page renders at full opacity immediately. The sign-in page is a functional gate, not a marketing surface. No fadeInUp, no delays.
- Button hover transition: `transition-colors duration-150` — standard 150ms color transition, consistent with `.nav-cta-btn` pattern.

**Web mode history load (Neon path):**
- Loading state uses the existing `LOADING HISTORY...` placeholder (already present in `ReportHistory` component) — no new animation needed.

**All other existing animations are unchanged** — scanlines, fadeInUp sections, orbPulse, ticker scroll tape. Phase 6 adds no new keyframes.

---

## Responsive Behavior

**Sign-in page:**
- Mobile (< 640px): container width `w-full max-w-sm mx-4` — full-width with 16px margin. Button full-width (already declared).
- Desktop (>= 640px): fixed `w-96` (384px) centered.

**All other pages:** No responsive changes from Phase 6. Existing behavior unchanged.

---

## Accessibility

- Sign-in button: `type="button"`, `aria-label="Sign in with Google"` — keyboard accessible
- Sign-in page: page title must be set via `<title>` or Next.js metadata — "Equinfo — Sign In"
- Color contrast on sign-in: amber-400 (`#f59e0b`) on `#080a0f` background — contrast ratio ~7.5:1, WCAG AA large text passes. Muted `#4a6a8a` text on `#080a0f` — ~3.2:1, acceptable for supplementary caption text (not primary content).
- Redirect behavior: middleware redirects preserve the original URL as `callbackUrl` so users land where they intended after sign-in.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| third-party | none declared | not applicable |

No third-party component registries in use. All components are hand-rolled with Tailwind v4 utilities. Registry vetting gate: not required.

---

## Pre-Population Audit

| Source | Decisions Applied |
|--------|------------------|
| CONTEXT.md | 8 — terminal aesthetic lock, zinc-950/amber-400 palette, IBM Plex Mono, no public browsing, custom NextAuth pages, DEPLOYMENT_MODE gating, local mode preserved, sign-in page design spec |
| RESEARCH.md | 6 — sign-in page code example (exact class names), middleware pattern, NextAuth v4 JWT strategy, Neon-backed history route pattern, NavIdentity email from session, no destructive flows in scope |
| `globals.css` (codebase) | 12 — all color values, spacing values, typography sizes, animation patterns, border colors, scrollbar colors, panel primitive |
| `layout.tsx` (codebase) | 3 — IBM Plex Mono confirmed, `#080a0f` body background confirmed, `text-[#c9d4e0]` confirmed |
| `package.json` (codebase) | 2 — no icon library, Tailwind v4 confirmed |
| User input this session | 0 — all values pre-populated from upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

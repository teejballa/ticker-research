---
phase: 05-user-identity-report-history
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
  - 05-04
files_modified:
  - tests/e2e/phase5-history.spec.ts
autonomous: false
requirements:
  - AUTH-01
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "Playwright e2e tests for Phase 5 pass: nav-identity visible, RESEARCH HISTORY section visible, /api/history returns valid JSON"
    - "Screenshots confirm terminal aesthetic: amber ticker symbols, sentiment chips with correct colors, muted secondary text"
    - "User confirms the home page renders correctly: email/NOT CONNECTED in nav, history table with proper columns"
  artifacts:
    - path: "tests/e2e/phase5-history.spec.ts"
      provides: "Updated e2e tests with precise selectors matching the implemented components"
  key_links:
    - from: "tests/e2e/phase5-history.spec.ts"
      to: "src/app/page.tsx"
      via: "page.goto('/') + data-testid selectors"
      pattern: "nav-identity|history-row|history-open-btn"
---

<objective>
Run the Phase 5 Playwright e2e tests, take screenshots to visually confirm the UI, iterate on any test failures, and present the home page to the user for final verification before marking Phase 5 complete.

Purpose: Closes the test loop (RESEARCH.md specified all Phase 5 behaviors must be covered by e2e tests), and gives the user a visual checkpoint before the phase is considered done.
Output: Passing e2e tests for AUTH-01, HIST-01, HIST-02; screenshots at /tmp/p5-*.png; user confirmation.
</objective>

<execution_context>
@/Users/tj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-identity-report-history/05-CONTEXT.md
@.planning/phases/05-user-identity-report-history/05-UI-SPEC.md
@.planning/phases/05-user-identity-report-history/05-03-SUMMARY.md
@.planning/phases/05-user-identity-report-history/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Playwright e2e tests and iterate until passing</name>
  <files>tests/e2e/phase5-history.spec.ts</files>
  <read_first>
    - tests/e2e/phase5-history.spec.ts — read to see the current test structure before running
    - src/components/ReportHistory.tsx — verify the data-testid attributes match what the tests expect
    - src/app/page.tsx — verify the data-testid="nav-identity" is present in the nav
  </read_first>
  <action>
1. Start the development server if not already running: `npm run dev`

2. Run the Phase 5 e2e tests with screenshots:
```bash
npx playwright test tests/e2e/phase5-history.spec.ts --headed=false 2>&1
```

3. Read each screenshot taken during the test run:
- /tmp/p5-nav-email.png — nav bar showing email or NOT CONNECTED
- /tmp/p5-history-section.png — home page showing RESEARCH HISTORY section

4. Visually verify from screenshots:
- Nav: correct position (top-right), amber email text or muted NOT CONNECTED
- History section: RESEARCH HISTORY header with ▶ glyph, panel border, column headers
- If reports exist: terminal-style rows with amber symbol, muted company name, sentiment chip
- If no reports: empty state message visible

5. Fix any failing tests by updating the test selectors or component attributes to match:
- Test expects `data-testid="nav-identity"` → confirm it exists in page.tsx nav
- Test expects `text=RESEARCH HISTORY` → confirm it exists in ReportHistory.tsx
- Test expects `data-testid="history-row"` etc.

6. Re-run until all non-pipeline tests pass:
```bash
npx playwright test tests/e2e/phase5-history.spec.ts --headed=false -g "Phase 5"
```

Note: The HIST-01 "report file written after analysis completes" test requires an actual analysis run — if history is empty, this test's `if (body.reports.length > 0)` branch is skipped gracefully. This is expected behavior.

The HIST-03 "regenerate creates new entry" test has `expect(true).toBe(true)` and will always pass — it's a placeholder for manual validation.

Target: All 8 tests pass (or skip gracefully where no reports exist yet).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase5-history.spec.ts --headed=false 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx playwright test tests/e2e/phase5-history.spec.ts exits 0 (all tests pass or skip gracefully)
    - Screenshots taken: /tmp/p5-nav-email.png and /tmp/p5-history-section.png exist
    - Read screenshots visually — confirm RESEARCH HISTORY section and nav identity are visible
  </done>
  <acceptance_criteria>
    - npx playwright test tests/e2e/phase5-history.spec.ts --headed=false exits 0
    - ls /tmp/p5-nav-email.png — file exists (screenshot taken)
    - ls /tmp/p5-history-section.png — file exists (screenshot taken)
    - After reading screenshots: nav shows email or NOT CONNECTED text in correct position
    - After reading screenshots: RESEARCH HISTORY section is visible with panel border and column headers
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5: User Identity + Report History
    - NavIdentity in home page nav (top-right): shows connected Google email (amber) or NOT CONNECTED (muted link)
    - RESEARCH HISTORY section below TickerSearch on home page: terminal-style table with SYMBOL | COMPANY | DATE | SENTIMENT | CONFIDENCE columns
    - [OPEN] button navigates to saved report view (/research/[ticker]?report=[filename])
    - [REGEN] button navigates to fresh pipeline run (/research/[ticker])
    - POST /api/analysis/[ticker] now persists reports to ~/.cipher/reports/ after successful analysis
    - GET /api/setup/status now returns userEmail field
    - GET /api/history returns list of all saved reports
    - GET /api/history/[filename] returns a single saved report
    - /research/[ticker]?report=[filename] loads saved report without re-running pipeline
  </what-built>
  <how-to-verify>
    1. Visit http://localhost:3000 (or npm start for production build)
    2. Check top-right nav: should show connected Google email (amber text) or "NOT CONNECTED"
    3. Scroll down past the hero — RESEARCH HISTORY section should appear below the search box
       - If no reports yet: should show "No reports yet. Analyze a ticker to get started."
       - If reports exist: table rows with amber ticker symbols, sentiment chips (BULLISH/BEARISH/NEUTRAL)
    4. Run a full ticker analysis (enter AAPL, confirm chart, wait for analysis)
    5. After analysis completes, navigate back to home — AAPL should appear in RESEARCH HISTORY
    6. Click [OPEN] on the AAPL row — should navigate to /research/AAPL?report=... and show the saved report WITHOUT re-running analysis
    7. Click [REGEN] — should navigate to /research/AAPL (no ?report= param) and start fresh pipeline
    8. Confirm: running a second AAPL analysis produces a NEW row in history (old one preserved)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:
1. npx playwright test tests/e2e/phase5-history.spec.ts --headed=false exits 0
2. User confirms home page looks correct via checkpoint
3. Completed analysis run writes a file to ~/.cipher/reports/ (verify with: ls ~/.cipher/reports/)
4. curl http://localhost:3000/api/history returns { "reports": [...] }
5. curl http://localhost:3000/api/setup/status returns JSON with "userEmail" key
</verification>

<success_criteria>
Phase 5 requirements satisfied:
- AUTH-01: Google account email displayed in nav (no separate signup)
- HIST-01: Analysis results persisted to ~/.cipher/reports/ as timestamped JSON files
- HIST-02: Home page shows RESEARCH HISTORY with openable past reports
- HIST-03: [REGEN] on any past report triggers fresh pipeline, saves new timestamped entry
- All Playwright tests pass
- User checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-identity-report-history/05-05-SUMMARY.md`
</output>

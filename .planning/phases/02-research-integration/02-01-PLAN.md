---
phase: 02-research-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/api/setup/status/route.ts
  - src/app/api/setup/install/route.ts
  - src/app/api/setup/auth/route.ts
  - src/components/SetupWizard.tsx
  - src/app/api/setup/__tests__/status.test.ts
  - src/components/__tests__/SetupWizard.test.tsx
autonomous: true
requirements:
  - RSRCH-01

must_haves:
  truths:
    - "Home page shows a setup card when Python, notebooklm-py, or Google auth is missing"
    - "Home page shows only the TickerSearch when all setup checks pass (allOk: true)"
    - "Python version check and notebooklm-py install trigger automatically — user never clicks a button for these"
    - "Google login is the only manual step — a browser opens and user logs in once"
    - "Setup card shows 3-step checklist with correct state icons (check / spinner / gray) per step"
    - "POST /api/setup/install streams SSE progress lines then a complete event"
    - "POST /api/setup/auth polls for storage_state.json and streams waiting/complete events"
  artifacts:
    - path: "src/app/api/setup/status/route.ts"
      provides: "GET /api/setup/status — checks Python 3.10+, notebooklm-py, storage_state.json"
      exports: ["GET"]
    - path: "src/app/api/setup/install/route.ts"
      provides: "POST /api/setup/install — SSE pip install + playwright chromium"
      exports: ["POST"]
    - path: "src/app/api/setup/auth/route.ts"
      provides: "POST /api/setup/auth — spawns notebooklm login, polls for storage_state.json"
      exports: ["POST"]
    - path: "src/components/SetupWizard.tsx"
      provides: "3-step setup UI consumed by page.tsx"
    - path: "src/app/api/setup/__tests__/status.test.ts"
      provides: "Automated tests for setup status API"
    - path: "src/components/__tests__/SetupWizard.test.tsx"
      provides: "Wave 0 stubs for SetupWizard component"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/SetupWizard.tsx"
      via: "conditional render based on GET /api/setup/status allOk"
    - from: "src/components/SetupWizard.tsx"
      to: "src/app/api/setup/install/route.ts"
      via: "fetch POST on mount when notebooklmOk is false"
    - from: "src/app/api/setup/auth/route.ts"
      to: "~/.notebooklm/storage_state.json"
      via: "fs.existsSync poll every 2 seconds"
    - from: "src/components/SetupWizard.tsx"
      to: "src/app/api/setup/auth/route.ts"
      via: "fetch POST + SSE stream read for waiting/complete events during Google login step"
---

<objective>
Setup wizard that checks Python 3.10+, notebooklm-py, and Google auth — auto-installs everything possible, leaves only the Google browser login for the user. Home page conditionally shows wizard or TickerSearch based on setup state.

Purpose: User can get to the search UI without manual setup steps — the app bootstraps itself except for the one unavoidably manual browser login.
Output: Three API routes, SetupWizard component, updated home page, unit tests.
</objective>

<execution_context>
@/Users/tj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-research-integration/02-CONTEXT.md
@.planning/phases/02-research-integration/02-RESEARCH.md
@src/app/page.tsx
@src/components/ChartConfirmation.tsx

<interfaces>
<!-- Key types needed from Phase 1 codebase. Use directly — no exploration needed. -->

From src/lib/types.ts (existing, do not modify):
No types needed from this file for the setup wizard itself.

Setup status response shape (define inline in route.ts):
```typescript
interface SetupStatus {
  pythonOk: boolean;
  pythonVersion?: string;
  pythonPath?: string;
  notebooklmOk: boolean;
  authOk: boolean;
  allOk: boolean;
}
```

SSE event shapes (define inline in components/routes):
```typescript
// install and auth SSE events
{ type: 'progress'; message: string }
{ type: 'complete' }
{ type: 'error'; message: string }
{ type: 'waiting' }   // auth only — sent every 5s while polling
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup API routes — status, install, auth</name>
  <files>
    src/app/api/setup/status/route.ts,
    src/app/api/setup/install/route.ts,
    src/app/api/setup/auth/route.ts,
    src/app/api/setup/__tests__/status.test.ts,
    src/components/__tests__/SetupWizard.test.tsx
  </files>
  <behavior>
    - GET /api/setup/status: runs `python3 --version`, `python3 -c "import notebooklm"`, checks fs.existsSync(`~/.notebooklm/storage_state.json`) — returns SetupStatus JSON
    - Python version parse: extract major.minor from "Python 3.11.4" string; pythonOk = major >= 3 and minor >= 10
    - If `python3` fails, try `python` fallback; if both fail, pythonOk = false
    - notebooklmOk: `python3 -c "import notebooklm; print(notebooklm.__version__)"` exits 0
    - authOk: `fs.existsSync(path.join(os.homedir(), '.notebooklm', 'storage_state.json'))`
    - allOk: pythonOk && notebooklmOk && authOk
    - POST /api/setup/install: spawn `pip3 install -r scripts/requirements.txt` then `playwright install chromium`; each stdout line → SSE `{ type: 'progress', message }` event; exit 0 → `{ type: 'complete' }`; non-zero → `{ type: 'error', message: stderr }`
    - POST /api/setup/auth: spawn `notebooklm login`; start polling `storage_state.json` every 2 seconds (max 10 minutes); stream `{ type: 'waiting' }` every 5s; when file appears → `{ type: 'complete' }` and close stream; if polling times out → `{ type: 'error', message: 'Login timed out after 10 minutes' }`
    - SSE response headers: `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`
    - status.test.ts (Wave 0 stubs): write failing stubs for status route that collect and run at parse time — test that `allOk` is false when pythonOk is false, test that `allOk` is true when all three checks pass (mock execa/child_process and fs.existsSync)
    - SetupWizard.test.tsx (Wave 0 stubs): 3 failing stubs using dynamic import pattern — renders without crashing, shows install step as active when notebooklmOk is false, shows Connect Account button when pythonOk and notebooklmOk are true but authOk is false. Tests fail at runtime (component does not exist yet) — not at parse time.
  </behavior>
  <action>
    Create `src/app/api/setup/status/route.ts` first. Use `child_process.execSync` for the quick Python version and import checks (synchronous, short-lived). Use `os.homedir()` + `path.join` for the storage_state.json path. Return JSON directly (no streaming needed for status).

    Create `src/app/api/setup/install/route.ts`. Use `child_process.spawn('pip3', ['install', '-r', 'scripts/requirements.txt'])` first, then on exit 0 spawn `playwright install chromium`. Pipe stdout lines to SSE. Use the same ReadableStream + TextEncoder pattern established in Phase 1 (see ChartConfirmation.tsx for the pattern).

    Create `src/app/api/setup/auth/route.ts`. Use `child_process.spawn('notebooklm', ['login'])` — this opens the browser on the user's screen (it's a CLI tool installed by pip). Then poll with `setInterval` checking `fs.existsSync` every 2 seconds. Stream `{ type: 'waiting' }` every 5 seconds. When file appears, stream `{ type: 'complete' }` and `controller.close()`. Set a 10-minute timeout.

    Create `src/app/api/setup/__tests__/status.test.ts` with 3 failing stubs (dynamic import pattern so vitest collects before failing at runtime — same as Phase 1 Wave 0 pattern from STATE.md decision log). Tests: `allOk false when python missing`, `allOk false when auth missing`, `allOk true when all pass`.

    Create `src/components/__tests__/SetupWizard.test.tsx` with 3 failing stubs using the same dynamic import pattern:
    ```typescript
    import { describe, it, expect } from 'vitest';
    describe('SetupWizard', () => {
      it('renders without crashing', async () => {
        const { SetupWizard } = await import('../SetupWizard');
        expect(SetupWizard).toBeDefined();
      });
      it('shows install step as active when notebooklmOk is false', async () => {
        const { SetupWizard } = await import('../SetupWizard');
        expect(SetupWizard).toBeDefined();
      });
      it('shows Connect Account button when authOk is false', async () => {
        const { SetupWizard } = await import('../SetupWizard');
        expect(SetupWizard).toBeDefined();
      });
    });
    ```
    Tests fail at runtime (module not found) — not at parse time. Real implementations in Task 2.

    Note: Use `storage_state.json` (NOT `auth.json`) per RESEARCH.md critical discovery.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="setup|SetupWizard" 2>&1 | tail -20</automated>
  </verify>
  <done>Three API route files exist. Status route returns correct SetupStatus shape. Install and auth routes return SSE streams. status.test.ts has 3 stubs that fail at runtime (not parse time). SetupWizard.test.tsx has 3 stubs that fail at runtime (module not found — not parse time). `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: SetupWizard component and home page wiring</name>
  <files>
    src/components/SetupWizard.tsx,
    src/app/page.tsx
  </files>
  <action>
    Create `src/components/SetupWizard.tsx` as a client component (`'use client'`). It manages all setup state internally.

    Component behavior:
    - On mount: fetch `GET /api/setup/status` and store the result
    - If `status.allOk`, the parent renders TickerSearch (not this component at all)
    - Otherwise, render a card above the search area with a 3-step checklist

    Three steps, each with status icon:
    1. "Python 3.10+ installed" — check if pythonOk, or show OS-specific install link + Re-check button if not
    2. "NotebookLM tools installed" — if pythonOk but not notebooklmOk, auto-trigger `POST /api/setup/install` on mount (user never manually clicks this); show spinner while streaming progress; show check when complete event arrives
    3. "Connect Google account" — only enabled after step 2 completes; show "Connect Account" button that triggers `POST /api/setup/auth`; while polling, show spinner + "A browser window has opened — log in to your Google account to continue." with animated dots (...); check when complete

    Icons: ✓ (text-green-600) for complete, spinning circle (animate-spin, text-blue-500) for active, ○ (text-gray-300) for pending.

    OS-specific Python instructions for step 1 failure:
    - Mac: `brew install python@3.11`
    - Windows: `winget install Python.Python.3.11`
    - Linux: `sudo apt install python3.11`
    Detect via `navigator.userAgent` or show all three in a condensed list.

    Re-check button (step 1 only): re-fetches `GET /api/setup/status` and updates state.

    After all 3 steps complete: `window.location.reload()` so page.tsx re-fetches status and renders TickerSearch.

    Update `src/app/page.tsx`:
    - Convert to a client component (`'use client'`)
    - On mount, fetch `GET /api/setup/status`
    - If `allOk` (or while loading, show nothing extra): render only TickerSearch
    - If not allOk: render `<SetupWizard />` above the header, inside the main container
    - While status is loading: show a small spinner placeholder so layout doesn't jump

    Style the SetupWizard card: `bg-white rounded-xl border border-amber-200 p-5 mb-6 shadow-sm` to visually distinguish it as a "before you start" callout. Step list uses `space-y-3`. Each step row: `flex items-center gap-3`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>SetupWizard.tsx exists as a client component. page.tsx fetches status on mount and conditionally renders SetupWizard or TickerSearch. TypeScript compiles clean. Visual: card with 3-step checklist, correct icons per state.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="setup|SetupWizard"` — test stubs collected and failing at runtime (not parse time)
- `npx tsc --noEmit` — no TypeScript errors
- `GET /api/setup/status` returns `{ pythonOk, notebooklmOk, authOk, allOk }` JSON
- Home page renders SetupWizard when setup incomplete, TickerSearch when complete
- `storage_state.json` path used everywhere (not `auth.json`)
</verification>

<success_criteria>
- Three setup API routes exist and return correct response shapes
- SetupWizard renders 3-step checklist with correct state icons
- Home page shows wizard or search based on setup state
- Install step auto-triggers (no manual button click)
- Auth step opens browser, polls storage_state.json
- TypeScript compiles clean across all new files
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-integration/02-01-SUMMARY.md`
</output>

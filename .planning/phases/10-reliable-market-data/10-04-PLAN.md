---
phase: 10-reliable-market-data
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified: []
autonomous: false
requirements:
  - DATA-RELIABLE-01
  - DATA-RELIABLE-02
  - DATA-RELIABLE-03

must_haves:
  truths:
    - "AAPL research run with at least one supplementary API key configured produces a populated report"
    - "Research run with all 3 API keys absent completes normally — yahoo-only path unchanged"
    - "npx tsc --noEmit exits 0 across the full codebase"
    - "Python syntax check passes on notebooklm_research.py"
    - "All existing unit tests still pass"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "SupplementaryMarketData exported"
      contains: "SupplementaryMarketData"
    - path: "src/lib/data/alpha-vantage.ts"
      provides: "fetchAlphaVantage exported"
      contains: "fetchAlphaVantage"
    - path: "src/lib/data/source-package.ts"
      provides: "supplementary_market_data in return value"
      contains: "supplementary_market_data"
    - path: "scripts/notebooklm_research.py"
      provides: "supplementary add_text() loop"
      contains: "supplementary_market_data"
    - path: ".env.local.example"
      provides: "3 new API key entries"
      contains: "ALPHA_VANTAGE_API_KEY"
  key_links:
    - from: "src/lib/data/source-package.ts"
      to: "scripts/notebooklm_research.py"
      via: "SourcePackage JSON written to /tmp, read by Python script"
      pattern: "supplementary_market_data"
---

<objective>
Run automated validation across all Phase 10 changes and confirm the end-to-end pipeline works — TypeScript compilation, Python syntax, unit tests, and a human-verified research run.

Purpose: Confirms the full pipeline — data fetchers → source package assembly → Python ingestion — works correctly before the phase is marked complete.
Output: All automated checks pass; human confirmation that a research run with at least one supplementary source produces richer report content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-reliable-market-data/10-CONTEXT.md
@.planning/phases/10-reliable-market-data/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated checks — TypeScript, Python syntax, unit tests</name>
  <read_first>
    - src/lib/types.ts — verify SupplementaryMarketData is exported
    - src/lib/data/source-package.ts — verify supplementary_market_data is in return
    - scripts/notebooklm_research.py — verify supplementary block is present
  </read_first>
  <files></files>
  <action>
Run each check in sequence and report results:

1. TypeScript compilation:
```bash
cd /Users/tj/Desktop/Cipher && npx tsc --noEmit
```
Expected: exits 0 with no output. If errors appear, fix them before proceeding.

2. Python syntax check:
```bash
python3 -c "import ast; ast.parse(open('/Users/tj/Desktop/Cipher/scripts/notebooklm_research.py').read()); print('syntax ok')"
```
Expected: prints "syntax ok". If syntax error, fix indentation issue in the inserted block.

3. Unit tests (TypeScript):
```bash
cd /Users/tj/Desktop/Cipher && npx vitest run --reporter=verbose 2>&1 | tail -30
```
Expected: all existing tests pass. The new source-package supplementary_market_data test may show as skipped — that's acceptable.

4. Confirm Phase 10 file existence:
```bash
ls /Users/tj/Desktop/Cipher/src/lib/data/alpha-vantage.ts \
   /Users/tj/Desktop/Cipher/src/lib/data/finnhub.ts \
   /Users/tj/Desktop/Cipher/src/lib/data/fmp.ts
```
Expected: all 3 files exist.

5. Confirm key strings are present:
```bash
grep -l "supplementary_market_data" \
  /Users/tj/Desktop/Cipher/src/lib/types.ts \
  /Users/tj/Desktop/Cipher/src/lib/data/source-package.ts \
  /Users/tj/Desktop/Cipher/scripts/notebooklm_research.py
```
Expected: 3 file paths printed.

6. Confirm .env.local.example has new keys:
```bash
grep "ALPHA_VANTAGE_API_KEY\|FINNHUB_API_KEY\|FMP_API_KEY" /Users/tj/Desktop/Cipher/.env.local.example
```
Expected: 3 lines with key names and empty values.

If any check fails, diagnose and fix the issue. Do NOT proceed to the human verification checkpoint until all automated checks pass.
  </action>
  <verify>
    <automated>cd /Users/tj/Desktop/Cipher && npx tsc --noEmit && python3 -c "import ast; ast.parse(open('scripts/notebooklm_research.py').read()); print('syntax ok')"</automated>
  </verify>
  <done>TypeScript compilation exits 0. Python syntax check prints "syntax ok". Unit tests pass (or existing failures are pre-Phase-10 regressions — document any pre-existing failures). All 3 new fetcher files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 10 implementation:
- 3 new data fetcher modules (Alpha Vantage, Finnhub, FMP) — each optional via API key
- SourcePackage type extended with supplementary_market_data (additive, no breaking changes)
- source-package.ts runs all 3 supplementary fetchers in parallel alongside existing yahoo calls
- notebooklm_research.py adds each available supplementary source as a separate add_text() call to NotebookLM
- research-brief.ts header now shows supplementary source count
- .env.local.example documents all 3 new optional API keys with sign-up links
  </what-built>
  <how-to-verify>
**Option A — Verify with at least one supplementary API key (recommended):**

1. Add at least one API key to .env.local:
   - Alpha Vantage: https://www.alphavantage.co/support/#api-key (free, instant)
   - Finnhub: https://finnhub.io/register (free, instant)
   - FMP: https://financialmodelingprep.com/register (free, instant)

2. Restart the dev server: `npm run dev`

3. Run a research report for AAPL (or TSLA/NVDA)

4. In the server terminal (where Next.js is running), look for Python script output:
   - Should see: `PROGRESS: Adding market data source...`
   - Should see: `PROGRESS: Adding 1 supplementary market data source(s)...` (or 2 or 3)
   - Should NOT see any ERROR lines related to supplementary sources

5. Confirm the final report no longer shows "N/A" for P/E, EPS, revenue, market cap in the text sections where previously these were blank — Gemini should now have the supplementary data to reference

**Option B — Verify without any API keys (no-key path):**

1. Ensure ALPHA_VANTAGE_API_KEY, FINNHUB_API_KEY, FMP_API_KEY are all absent from .env.local

2. Run a research report for any ticker

3. In the server terminal, look for Python output:
   - Should see: `PROGRESS: No supplementary market data sources available (API keys not configured or fetches failed)`
   - Report should complete normally — yahoo-only path unchanged

4. Confirm report completes successfully (no errors, full report rendered)
  </how-to-verify>
  <resume-signal>Type "approved" if the pipeline works correctly (either path), or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification→production | This checkpoint confirms Phase 10 works end-to-end before marking complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Information Disclosure | Terminal output includes supplementary source names | accept | Terminal output is local-only dev server; in production, PROGRESS: lines are streamed only to the authenticated user's SSE connection |
</threat_model>

<verification>
All checks from Task 1 must pass before checkpoint:
- npx tsc --noEmit: 0 errors
- python3 syntax check: "syntax ok"
- vitest run: no regressions
- All 3 fetcher files exist
- supplementary_market_data present in types.ts, source-package.ts, notebooklm_research.py
- .env.local.example has 3 new API key entries
</verification>

<success_criteria>
Phase 10 is complete when:
1. All automated checks pass (TypeScript, Python syntax, unit tests)
2. Human confirms either:
   (a) Research run with supplementary key(s) shows supplementary source progress messages and richer report content, OR
   (b) Research run without any keys completes normally with "no supplementary sources" message
3. No regressions to existing research functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-reliable-market-data/10-04-SUMMARY.md`
</output>

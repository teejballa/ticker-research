---
phase: 8
slug: full-public-deployment-vercel-frontend-daytona-container
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
revised: 2026-03-25
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS @theme tokens in `src/app/globals.css` |
| Preset | not applicable |
| Component library | none (hand-rolled components using Tailwind v4 utilities) |
| Icon library | Material Symbols Outlined (Google Fonts CDN, loaded in `src/app/layout.tsx`) |
| Body font | Inter (Google Fonts, weights 400/700) |
| Mono font | JetBrains Mono (Google Fonts, weights 400/700) |

Source: `src/app/layout.tsx`, `src/app/globals.css`

---

## Established Aesthetic (locked — do not deviate)

The app runs a Bloomberg terminal aesthetic: near-black backgrounds, electric-blue accents, teal secondary signals, amber-400 for SPAC/ETF type badges and auth labels. Flat surfaces only — no rounded-xl, no shadow-sm. All labels and CTAs use uppercase tracking-widest. This aesthetic was locked in Phase 3 and confirmed through Phase 7.

Source: STATE.md decisions log entries for Phase 3 and Phase 7.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Compact element spacing within cards |
| md | 16px | Default padding, button padding (px-4 py-2) |
| lg | 24px | Section padding, card padding (p-6) |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks on onboarding page |
| 3xl | 64px | Page-level vertical spacing (top/bottom of full-page flows) |

Exceptions: NavBar fixed height is 44px (touch target compliance, carried from Phase 1). Auth buttons minimum height 40px (carried from Phase 6 sign-in page).

Source: `src/components/NavBar.tsx` (h-[44px]), `src/app/auth/signin/page.tsx` (minHeight 40px).

---

## Typography

Two weights only: 400 (regular) and 700 (bold).

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body | 12px (text-xs) | 400 (regular) | 1.5 | Inter | Step descriptions, helper text, secondary info |
| Label | 11px, tracking-widest, uppercase | 700 (bold) | 1.0 | Inter or JetBrains Mono | Section headers, status overlines, badge text, CTAs |
| Heading | 14px (text-sm) | 700 (bold) | 1.2 | Inter | Step titles, card headings, nav items |
| Display | 18px (text-lg) | 700 (bold) | 1.0 | Inter | CIPHER wordmark, hero labels |

Weight consolidation notes (from checker revision):
- Weight 500 removed: elements previously at 500 (heading, some nav items) shift to 700 bold.
- Weight 900 removed: CIPHER wordmark (Display role) shifts from 900 to 700 — the terminal aesthetic is preserved; optical difference at 18px is negligible against the dark background.
- Font loading in `src/app/layout.tsx` must declare only weights 400 and 700 for both Inter and JetBrains Mono.

Notes:
- Monospace (JetBrains Mono) is used for all terminal-style values: code blocks, ticker symbols, progress output lines, cookie/auth status text, identity email display.
- Monospace label size is 10px–11px at font-bold (700), tracking-widest — consistent with the existing NavBar identity pattern (`text-[11px] tracking-widest`).
- No display sizes beyond 18px on the two new pages (onboarding, account) — these are functional flows, not marketing.

Source: `src/app/globals.css` (--font-mono, --font-body), `src/components/NavBar.tsx` (text-[11px]), `src/app/auth/signin/page.tsx` (text-xs labels).

---

## Color

All tokens reference `src/app/globals.css` @theme block.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-surface` | #10141a | Page background, full-page onboarding background |
| Secondary (30%) | `--color-surface-container` | #1c2026 | Cards, step containers, account settings card |
| Accent (10%) | `--color-primary-container` | #2962ff | Primary CTAs only (see reserved list below) |
| Accent text | `--color-primary` | #b6c4ff | Overline text, hover states, scrollbar hover |
| Signal — positive | `--color-secondary` | #66d9cc | Checkmark (step complete), success state indicators |
| Signal — amber | amber-400 (#f59e0b) | #f59e0b | Auth section label ("AUTHENTICATION REQUIRED"), NbLM reconnect prompt label, OAuth passthrough status label |
| Signal — destructive | `--color-error` | #ffb4ab | Error step indicators (✗), error body text |
| Border | `--color-outline-variant` | #434656 | Card borders, dividers, button ghost borders |
| Muted text | `--color-outline` | #8d90a2 | Helper text, pending step labels |

**Accent (#2962ff / primary-container) is reserved for:**
1. "Connect NotebookLM Account" primary CTA button (onboarding page)
2. "Reconnect NotebookLM" primary CTA button (account page)
3. "Run Analysis" / confirm action buttons (inherited from existing pipeline — not new in Phase 8)

Amber (#f59e0b) is reserved for:
1. Auth section overline label on onboarding page ("NOTEBOOKLM // AUTHENTICATION REQUIRED")
2. "Reconnect" badge/label state on account page when session is expired
3. Security type badges (SPAC, ETF) — already established in Phase 7

Source: `src/app/globals.css`, `src/app/auth/signin/page.tsx` (amber pattern), `src/components/NavBar.tsx` (amber-400 badge pattern).

---

## New UI Surfaces — Phase 8

Phase 8 introduces three net-new UI surfaces. All must match the established terminal aesthetic exactly.

### Surface 1: `/setup` — Web-Mode NotebookLM Onboarding Page

**When shown:** User is authenticated via Google OAuth (NextAuth) but has no NotebookLM session stored in Neon DB. Redirected here before they can run analysis.

**Focal point:** The active step container is the primary visual anchor. At any moment exactly one step is in the `active` state — its StepIndicator spinner and step-body copy draw the user's eye. The VNC iframe (when visible) is the secondary focal anchor; it occupies the full card width at 480px height and dominates the viewport below the step list.

**Layout:** Full-page, centered card. Same shell as the existing `/auth/signin` page — `min-h-screen flex items-center justify-center` on `bg-surface`. Card is `w-96 max-sm:w-full max-sm:mx-4`.

**Step sequence:**
1. Google OAuth sign-in confirmation (already complete — show as `complete` checkmark state)
2. NotebookLM account connection (active step)
   - First attempt: OAuth passthrough — app tries silently; if it works, advance to complete automatically
   - Fallback: Show embedded VNC browser stream inside an `<iframe>` or `<div>` container. Container is `w-full h-[480px] bg-surface-container-lowest border border-outline-variant/20` — matches terminal card style. Label above the stream: `COMPLETE GOOGLE LOGIN IN THE BROWSER BELOW` in amber-400, 11px, tracking-widest.
3. Connection confirmed (shows ✓ in secondary/teal when `storage_state.json` captured)

**State machine:**
- `checking` — spinner (`w-3 h-3 border border-primary/60 border-t-transparent rounded-full animate-spin`) — identical to `SetupWizard.tsx` StepIndicator pattern
- `oauth-attempting` — `ATTEMPTING OAUTH PASSTHROUGH...` in on-surface-variant, 10px mono
- `vnc-active` — iframe/stream visible with amber label above
- `complete` — teal checkmark ✓, redirect to `/` in 2 seconds

**No NavBar** on this page — same pattern as `/auth/signin`.

### Surface 2: `/account` — Settings Page

**When shown:** Accessible at all times via a new "Account" or "Settings" nav link (small, text-only, placed in the NavBar right cluster, same style as existing nav labels).

**Focal point:** The NotebookLM Session status row is the primary visual anchor. When the session is expired, the amber `SESSION EXPIRED` label and the `RECONNECT NOTEBOOKLM →` accent button together form an attention unit that reads first. When the session is active, the teal `SESSION ACTIVE` label is the positive confirmation anchor and the eye rests there before scanning to the Identity section above it.

**Layout:** Standard app layout (with NavBar). Content is a single centered card, `max-w-lg mx-auto mt-24 p-6 bg-surface-container border border-outline-variant/20`.

**Sections:**

Section header style: `text-[9px] text-primary/50 tracking-[0.4em] mb-1` (matches SetupWizard "SYSTEM INITIALIZATION" overline — consistent language register).

1. **Identity** — overline: `CONNECTED ACCOUNT`. Shows userEmail (from NextAuth session) in mono, 12px, on-surface.
2. **NotebookLM Connection** — overline: `NOTEBOOKLM SESSION`. Shows connection status:
   - Active: `SESSION ACTIVE` in secondary/teal, 11px mono
   - Expired/missing: `SESSION EXPIRED` in amber-400, 11px mono + "Reconnect NotebookLM" CTA button (same style as SetupWizard CONNECT ACCOUNT button: `bg-primary-container text-on-primary-container px-3 py-1 text-[10px] font-bold tracking-wider`)
3. **Sign Out** — ghost button, destructive hover only (border-error/40, text-error/60 on hover), label: `END SESSION`

**No additional settings in Phase 8.** Account page is minimal — two status sections plus sign-out.

### Surface 3: Cloud Analysis Error States

**When shown:** Analysis route in web mode encounters a failure from the Daytona container.

**Error state variants (displayed inline in the existing `<ResearchProgress />` component area, not a new page):**

| Error Type | Copy | Action |
|------------|------|--------|
| NbLM session expired | "NotebookLM session expired — reconnect your account to continue." | Button: `RECONNECT ACCOUNT →` (links to `/account`) |
| Container unreachable | "Analysis server unreachable. This is temporary — please try again." | Button: `TRY AGAIN →` (retries the analysis call) |
| Analysis timeout | "Analysis is taking longer than expected. Your research will resume — or try again." | Button: `TRY AGAIN →` |
| Unknown error | "Analysis failed. If this continues, reconnect your account." | Button: `RECONNECT ACCOUNT →` |

Error copy style: `text-xs text-error/70` for the message, `text-[10px] font-bold tracking-widest` for the action button (ghost style with error/40 border on hover). Matches the existing SetupWizard error pattern.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Onboarding page overline | `CIPHER // NOTEBOOKLM AUTHENTICATION REQUIRED` |
| Onboarding step 1 label | `Google account verified` |
| Onboarding step 2 label | `Connect NotebookLM account` |
| Onboarding step 3 label | `Research pipeline ready` |
| Primary CTA — onboarding (OAuth path) | (No button — auto-attempts silently) |
| Primary CTA — onboarding (VNC fallback) | `[ COMPLETE LOGIN IN BROWSER ]` (status label, not a button) |
| Primary CTA — account reconnect | `RECONNECT NOTEBOOKLM →` |
| VNC stream label | `COMPLETE GOOGLE LOGIN IN THE BROWSER BELOW` |
| Account page — identity overline | `CONNECTED ACCOUNT` |
| Account page — connection overline | `NOTEBOOKLM SESSION` |
| Account page — active status | `SESSION ACTIVE` |
| Account page — expired status | `SESSION EXPIRED` |
| Account page — sign-out button | `END SESSION` |
| NavBar link to account page | `ACCOUNT` (text-only, same style as existing NYSE/NASDAQ nav labels) |
| Error: session expired | `NotebookLM session expired — reconnect your account to continue.` |
| Error: container unreachable | `Analysis server unreachable. This is temporary — please try again.` |
| Error: timeout | `Analysis is taking longer than expected. Your research will resume — or try again.` |
| Error: unknown | `Analysis failed. If this continues, reconnect your account.` |
| Empty state (account — no session) | `No NotebookLM session found. Connect your account to enable research.` |
| Onboarding success redirect | (No copy — auto-redirect to `/` after 2s with ✓ step state) |
| Sign-out confirmation | No confirmation modal — destructive action is sign-out, which is recoverable (user can sign back in). Ghost button with visual destructive hover is sufficient. |

Destructive actions in Phase 8:
- **End Session**: Ghost button labeled `END SESSION`, visual destructive hover (error/40 border, error/60 text). No confirmation modal — low severity, fully recoverable.
- No other destructive actions.

---

## Interaction Contracts

### Onboarding Page (`/setup`)

- On mount: check for existing NbLM session in Neon (via `GET /api/setup/nbm-auth/status`)
- If session exists: skip to step 3 complete, redirect immediately
- If no session: attempt OAuth passthrough silently (`POST /api/setup/nbm-auth` with OAuth attempt flag)
  - If success (≤5s): advance to step 3 complete, redirect in 2s
  - If fail: show VNC stream — `POST /api/setup/nbm-auth` returns stream URL; render in iframe/div
  - Poll for cookie capture: `GET /api/setup/nbm-auth/status` every 3s; when `{ captured: true }` → advance to step 3
- All network activity shows spinner StepIndicator (active state) — no standalone loading overlay
- Step states are one-directional: pending → active → complete (no back-step)

### Account Page (`/account`)

- On mount: `GET /api/setup/status` (existing endpoint, extended to return `nbmSessionActive: boolean`)
- If `nbmSessionActive: true`: show `SESSION ACTIVE` in teal, no CTA
- If `nbmSessionActive: false`: show `SESSION EXPIRED` in amber, show `RECONNECT NOTEBOOKLM →` button
- "Reconnect NotebookLM" click: navigates to `/setup` (reuses onboarding flow)
- `END SESSION` click: calls `signOut()` from `next-auth/react`, redirects to `/auth/signin`
- No optimistic updates — always fetch fresh state on mount

### Cloud Error States (in ResearchProgress)

- Errors surface inline in the existing `<ResearchProgress />` step list area — no full-page takeover
- Error message replaces the failed step body (same DOM location as existing error states in SetupWizard)
- "TRY AGAIN →" retries the `POST /api/analysis/[ticker]` call from the beginning
- "RECONNECT ACCOUNT →" navigates to `/account` — preserves the ticker in the URL so the user can return and resume

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — no shadcn installed | not applicable |
| Third-party | none declared | not applicable |

No third-party UI registries are used in this project. All components are hand-rolled. No registry vetting required.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Terminal aesthetic (zinc-black bg, blue accent, teal signals, amber labels) | STATE.md Phase 3 decision; `src/app/globals.css` @theme |
| Flat surfaces, no rounded-xl, no shadow-sm | STATE.md Phase 3 decision |
| Spacing scale (8-pt grid, 44px nav, 40px min button height) | `src/components/NavBar.tsx`, `src/app/auth/signin/page.tsx` |
| Typography (Inter + JetBrains Mono, uppercase labels, tracking-widest) | `src/app/layout.tsx`, `src/app/globals.css` |
| Color tokens (all hex values) | `src/app/globals.css` @theme block |
| Icon library (Material Symbols Outlined) | `src/app/layout.tsx` |
| Step indicator pattern (pending/active/complete/error states) | `src/components/SetupWizard.tsx` StepIndicator |
| Amber-400 for auth labels | `src/app/auth/signin/page.tsx` |
| Full-page auth layout (min-h-screen centered, w-96 card) | `src/app/auth/signin/page.tsx` |
| Onboarding `/setup` page (new surface) | `08-CONTEXT.md` implementation decisions |
| `/account` settings page (new surface) | `08-CONTEXT.md` implementation decisions |
| Cloud error types (session expired, container unreachable, timeout) | `08-CONTEXT.md` implementation decisions |
| "Reconnect NotebookLM" consistency requirement | `08-CONTEXT.md` — "Consistent UX — user never needs to navigate differently" |
| No custom domain — Vercel default | `08-CONTEXT.md` go-live configuration |
| Weight collapse to 400/700 | Checker revision 2026-03-25 — BLOCK on Dimension 4 |
| Focal points for /setup and /account | Checker revision 2026-03-25 — Dimension 2 recommendation |
| Sign-out label changed to END SESSION | Checker revision 2026-03-25 — Dimension 1 recommendation |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

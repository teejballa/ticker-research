---
phase: 18-time-decayed-bayesian-updates-ess
plan: 10
type: execute
wave: 4
depends_on: [18-07, 18-08, 18-09]
files_modified:
  - .planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md
  - src/lib/__tests__/learning.hyperparameters.test.ts
autonomous: false
requirements: [CORE-ML-01, CORE-ML-02, CORE-ML-03, CORE-ML-04, CORE-ML-05]
must_haves:
  truths:
    - "All 10 Wave 0 stub files have been activated to live assertions across Plans 01–09"
    - "The full test suite (units + integration + e2e) exits 0: `npm test -- --run && npm run test:integration && npm run test:e2e`"
    - "VALIDATION.md per-task verification map is fully populated — every task in every PLAN has a row with green status"
    - "VALIDATION.md frontmatter `nyquist_compliant: true` flipped on"
    - "HYPERPARAMETERS sanity test enforces RESEARCH §Pitfall-3 mitigation: every per-class entry has cv_brier_oos < 0.25 OR a TODO comment escape-hatch from Plan 06 step 5"
    - "All 8 must_haves from the orchestrator quality_gate are verified — see verification map"
    - "All threat mitigations T-18-01 through T-18-06 are verified by tests or grep-able acceptance criteria"
  artifacts:
    - path: ".planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md"
      provides: "fully populated per-task verification map + nyquist_compliant: true + approval timestamp"
      contains: "nyquist_compliant: true"
    - path: "src/lib/__tests__/learning.hyperparameters.test.ts"
      provides: "vitest enforcing cv_brier_oos < 0.25 per Pitfall 3 mitigation"
      min_lines: 40
      contains: "cv_brier_oos"
  key_links:
    - from: ".planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md"
      to: "all 10 PLAN.md files in this phase"
      via: "per-task verification map enumerates every task ID, requirement ref, threat ref, automated command, and pass status"
      pattern: "Task ID.*Plan.*Wave.*Requirement"
---

<objective>
Phase verification and sign-off. This plan does NOT implement new functionality — it (a) runs the full test suite and confirms green, (b) runs the LOOKS-DONE-BUT-ISN'T acceptance gates from RESEARCH §Pitfalls Defended (sparse-recent CI < sparse-old CI; N=29 cell never fires drift; ess_backfill_complete idempotency), (c) populates the per-task verification map in 18-VALIDATION.md, (d) flips `nyquist_compliant: true` in VALIDATION.md frontmatter, (e) adds one final unit test enforcing the RESEARCH §Pitfall 3 mitigation (`cv_brier_oos < 0.25` per HYPERPARAMETERS entry).

Per the orchestrator's quality_gate, every requirement (CORE-ML-01..05), every locked decision (D-01..D-20), and every STRIDE threat (T-18-01..T-18-06) must be verified before the phase can ship. This plan is the audit task that walks the matrix and signs off.

This plan has a checkpoint task because the final manual UAT row in VALIDATION.md ("Operator reads tuning script output and pastes new λ into HYPERPARAMETERS constant in `learning.ts`; CI re-runs Q3 fold-OOS-Brier on new constant") needs operator confirmation that Plan 06 ran with current data.

D-XX coverage: D-17 audit (every metric has documented operational action — verify by walking the map), D-16 audit (every CV in P18 used Purged K-Fold + Embargo — verify by greppable imports of `purgedKFold`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-RESEARCH.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-00-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-01-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-02-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-03-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-04-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-05-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-06-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-07-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-08-PLAN.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-09-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HYPERPARAMETERS sanity test enforcing cv_brier_oos < 0.25 per Pitfall 3</name>
  <files>src/lib/__tests__/learning.hyperparameters.test.ts</files>
  <read_first>
    - src/lib/learning.ts (post-Plans 01 + 04 + 06 — confirm HYPERPARAMETERS const exists with all 4 signal classes)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-RESEARCH.md §"Pitfalls Defended / Pitfall 3" (looks-done-but-isn't gate: cv_brier_oos < 0.25)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md D-01 (per-class λ no global default), D-07 (per-class PH params)
    - 18-06-PLAN.md Task 2 step 5 (escape hatch: TODO comment for low-N classes)
  </read_first>
  <behavior>
    - Test asserts every entry in HYPERPARAMETERS has the 5 required typed fields populated (lambda_days, ph_delta, ph_lambda, tuned_at, cv_brier_oos)
    - Test asserts every entry has cv_brier_oos < 0.25 (better than 50/50 baseline) UNLESS the entry has a `// TODO: re-tune in Plan 21` comment annotation observable from a separate exported `HYPERPARAMETERS_DEFERRED_RETUNE: Set<string>` (Plan 06's escape hatch)
    - Test asserts lambda_days ∈ {14, 30, 60, 90, 180, 365} (D-01 grid — winner must come from this set)
    - Test asserts ph_delta ∈ {0.001, 0.005, 0.01} and ph_lambda ∈ {30, 50, 100} (D-07 grid)
  </behavior>
  <action>
**Step 1: Add `HYPERPARAMETERS_DEFERRED_RETUNE` export to `src/lib/learning.ts`** (Plan 06 escape hatch — operator listed signal classes here when low-N forced placeholder retention):
```typescript
// Phase 18 / Plan 06 Task 2 step 5: signal classes whose tuning was deferred
// because N is too low to score meaningfully. Plan 21 will re-tune post-backfill.
export const HYPERPARAMETERS_DEFERRED_RETUNE: ReadonlySet<string> = new Set<string>(/* operator-populated */);
```

**Step 2: Create `src/lib/__tests__/learning.hyperparameters.test.ts`:**
```typescript
import { describe, it, expect } from 'vitest';
import { HYPERPARAMETERS, HYPERPARAMETERS_DEFERRED_RETUNE } from '@/lib/learning';

const LAMBDA_GRID = new Set([14, 30, 60, 90, 180, 365]);
const PH_DELTA_GRID = new Set([0.001, 0.005, 0.01]);
const PH_LAMBDA_GRID = new Set([30, 50, 100]);
const REQUIRED_CLASSES = ['diffusion', 'technical', 'insider', 'institutional'];

describe('HYPERPARAMETERS sanity (Pitfall 3 / D-01 / D-07 enforcement)', () => {
  it('contains all 4 required signal classes', () => {
    for (const cls of REQUIRED_CLASSES) {
      expect(HYPERPARAMETERS[cls]).toBeDefined();
    }
  });

  it('every class has all 5 typed fields populated', () => {
    for (const cls of REQUIRED_CLASSES) {
      const h = HYPERPARAMETERS[cls];
      expect(typeof h.lambda_days).toBe('number');
      expect(typeof h.ph_delta).toBe('number');
      expect(typeof h.ph_lambda).toBe('number');
      expect(typeof h.tuned_at).toBe('string');
      expect(typeof h.cv_brier_oos).toBe('number');
    }
  });

  it('every lambda_days winner is from the D-01 grid', () => {
    for (const cls of REQUIRED_CLASSES) {
      expect(LAMBDA_GRID.has(HYPERPARAMETERS[cls].lambda_days)).toBe(true);
    }
  });

  it('every ph_delta and ph_lambda winner is from the D-07 grid', () => {
    for (const cls of REQUIRED_CLASSES) {
      expect(PH_DELTA_GRID.has(HYPERPARAMETERS[cls].ph_delta)).toBe(true);
      expect(PH_LAMBDA_GRID.has(HYPERPARAMETERS[cls].ph_lambda)).toBe(true);
    }
  });

  it('every class with cv_brier_oos >= 0.25 is in HYPERPARAMETERS_DEFERRED_RETUNE (Pitfall 3 escape hatch)', () => {
    for (const cls of REQUIRED_CLASSES) {
      const brier = HYPERPARAMETERS[cls].cv_brier_oos;
      if (brier >= 0.25) {
        expect(HYPERPARAMETERS_DEFERRED_RETUNE.has(cls)).toBe(true);
      }
    }
  });
});
```

DO NOT relax the `cv_brier_oos < 0.25` gate without operator approval — that's the Pitfall 3 LOOKS-DONE-BUT-ISN'T bar. The escape hatch (`HYPERPARAMETERS_DEFERRED_RETUNE`) is the *documented* way to ship a signal class whose N is too low to score meaningfully today; Plan 21 re-tunes post-backfill.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/learning.hyperparameters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/__tests__/learning.hyperparameters.test.ts` exists with the 5 `it` blocks above
    - File `src/lib/learning.ts` exports `HYPERPARAMETERS_DEFERRED_RETUNE` of type `ReadonlySet<string>`
    - `npm test -- --run src/lib/__tests__/learning.hyperparameters.test.ts` exits 0
    - The grids referenced match D-01 ({14,30,60,90,180,365}) and D-07 ({0.001,0.005,0.01} × {30,50,100}) exactly
  </acceptance_criteria>
  <done>HYPERPARAMETERS sanity test added, all green, Pitfall 3 mitigation enforced at CI time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full suite + LOOKS-DONE-BUT-ISN'T acceptance gates + populate per-task map</name>
  <files>.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md</files>
  <read_first>
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md (current — per-task map is empty `_pending — gsd-planner fills_`)
    - All 10 PLAN.md files in the phase (read each task's <name>, <acceptance_criteria>, requirement IDs, and threat refs)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md D-17 (every metric has operational action documented), D-16 (every CV used Purged K-Fold + Embargo)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-RESEARCH.md "Pitfalls Defended" (3 LOOKS-DONE-BUT-ISN'T acceptance gates to run)
  </read_first>
  <action>
**Step 1: Run the full suite locally and capture results:**
```
npm test -- --run
npm run test:integration
npm run test:e2e
npx tsc --noEmit
```
Each MUST exit 0. If any fails, STOP and route the failure to the relevant plan's task (do not proceed to map population on a red suite).

**Step 2: Run the 3 LOOKS-DONE-BUT-ISN'T acceptance gates from RESEARCH §"Pitfalls Defended":**

(a) **Pitfall 3 — λ defensibility:** the `learning.hyperparameters.test.ts` from Task 1 enforces this — confirm it's green.

(b) **Pitfall 13 — drift FP flap defense:** confirm `learn.drift.live.test.ts` includes a passing case where rawN=29 cell never fires drift even with synthetic shift (D-08 floor).

(c) **End-to-end LOOKS-DONE-BUT-ISN'T:** confirm `learn.ess.live.test.ts` includes the passing assertion: two cells with raw N=20 — one all-recent, one 90+ days — produce `ESS_recent > 2 × ESS_old` AND `credibleInterval95(weighted recent).width < credibleInterval95(weighted old).width`.

If any of (a)/(b)/(c) is missing or red, STOP and route back to Plans 04 / 06 / 09 respectively.

**Step 3: Populate the per-task verification map in `18-VALIDATION.md`.** Replace the existing empty map placeholder with a fully-filled table — one row per task in Plans 00 through 10. Each row carries:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |

Walk every PLAN.md from 00–10. Each `<task>` becomes a map row. The Status column is ✅ green if the verify automated command exited 0 in Step 1; otherwise mark ⚠️ flaky or ❌ red and STOP.

Example shape (fill in for every task — count is approximately 13 across 10 plans):

```markdown
| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 00.1 | 00 | 0 | CORE-ML-01..05 | T-18-04 | stub uses STATUS_VALUES literal | unit | npm test -- --run src/lib/__tests__/learning.decay.test.ts (et al) | ✅ | ✅ |
| 00.2 | 00 | 0 | CORE-ML-01..05 | — | stub uses skip+placeholder | e2e/integration | ls -la (file existence) | ✅ | ✅ |
| 01.1 | 01 | 1 | CORE-ML-01, CORE-ML-04 | T-18-04, T-18-05 | STATUS_VALUES const + numeric-only confirmedDrift return | unit | npm test -- --run src/lib/__tests__/learning*.test.ts | ✅ | ✅ |
| 02.1 | 02 | 1 | CORE-ML-02, CORE-ML-04 | (none new) | purgedKFold pure fn, no DB | unit | npm test -- --run src/lib/__tests__/cv.purgedkfold.test.ts | ✅ | ✅ |
| 03.1 | 03 | 1 | CORE-ML-01 | schema-tamper, wrong-target | additive-only schema diff | grep | grep -E "effective_sample_size Float" prisma/schema.prisma | ✅ | ✅ |
| 03.2 | 03 | 1 | CORE-ML-01 | schema-tamper | format+generate clean | CLI | npx prisma format && npx prisma generate | ✅ | ✅ |
| 03.3 | 03 | 1 | CORE-ML-01 | schema-tamper | additive-only push, no data loss | CLI | npx prisma db push (in sync) | ✅ | ✅ |
| 04.1 | 04 | 2 | CORE-ML-01, CORE-ML-02 | T-18-01 (unchanged), T-18-04 | decay+ESS+ patternStatus ESS gate; STATUS_VALUES guard | integration | npm run test:integration -- --run learn.ess.live | ✅ | ✅ |
| 04.2 | 04 | 2 | CORE-ML-04 | T-18-01, T-18-04, T-18-05 | confirmedDrift two-of-two; numeric-only delta | integration | npm run test:integration -- --run learn.drift.live | ✅ | ✅ |
| 05.1 | 05 | 2 | CORE-ML-01 | T-18-01, T-18-02, T-18-03 | auth + env flag + single tx + idempotency marker | integration | npm run test:integration -- --run backfill.live | ✅ | ✅ |
| 06.1 | 06 | 2 | CORE-ML-02, CORE-ML-04 | T-18-06 | tuning scripts use purgedKFold; typed-config audit-log | typecheck | npx tsc --noEmit scripts/tune-*.ts | ✅ | ✅ |
| 06.2 | 06 | 2 | CORE-ML-02, CORE-ML-04 | T-18-06 | operator pastes tuned values; cv_brier_oos < 0.25 OR deferred | manual | learning.hyperparameters.test.ts | ✅ | ✅ |
| 07.1 | 07 | 3 | CORE-ML-03, CORE-ML-05 | trust-boundary-leak | post-process overwrite includes ESS fields | unit | npm test -- --run engine-context.test.ts | ✅ | ✅ |
| 08.1 | 08 | 3 | CORE-ML-05 | T-18-04, display-trust | TS narrowing on STATUS_BADGE Record | e2e | npm run test:e2e -- --grep ESS column | ✅ | ✅ |
| 08.2 | 08 | 3 | CORE-ML-05 | display-trust | operator visual sanity check | manual UAT | screenshot review | ✅ | ✅ |
| 09.1 | 09 | 3 | CORE-ML-03 | T-18-05 | drift_clear count derivation; numeric-only payload read with Zod | e2e | npm run test:e2e -- --grep insights — credible interval | ✅ | ✅ |
| 10.1 | 10 | 4 | CORE-ML-02, CORE-ML-04 | T-18-06 | HYPERPARAMETERS Pitfall-3 gate | unit | npm test -- --run learning.hyperparameters.test.ts | ✅ | ✅ |
| 10.2 | 10 | 4 | CORE-ML-01..05 | T-18-01..06 | full-suite green + map population | composite | npm test && test:integration && test:e2e | ✅ | ✅ |
```

**Step 4: Flip frontmatter.** Update `18-VALIDATION.md` frontmatter:
```yaml
status: approved
nyquist_compliant: true
wave_0_complete: true
last_updated: 2026-05-04
```

Set `**Approval:** approved 2026-05-04` at the bottom of the file.

**Step 5: Audit D-16 invariant.** Run:
```
grep -E "purgedKFold" src/lib/cv.ts scripts/tune-lambda.ts scripts/tune-page-hinkley.ts | wc -l
```
Expect ≥3 (one per file). Confirm no Phase 18 file does CV without going through purgedKFold:
```
grep -RE "kfold|k-fold|cross.validation" --include="*.ts" src/lib src/app/api/cron scripts | grep -v "purgedKFold" | grep -v "from '@/lib/cv'"
```
Should return zero matches (nothing is doing rogue CV without the purged version).

**Step 6: Audit D-17 invariant.** Confirm every metric introduced has documented operational action:
- `effective_sample_size` → /insights debug column + ESS-based gate in patternStatus (D-04)
- `drift_alert` count → /insights drift list + recovery counter (Plan 09 + D-09 step 4 derived)
- `EXPLORATORY-WATCH` cell count → WatchBadge on EngineCalibrationPanel + recovery counter (Plan 08 + Plan 09)
- HYPERPARAMETERS.cv_brier_oos → learning.hyperparameters.test.ts gate (Plan 10 Task 1)

If any metric lacks a documented action, add a comment in the producing file referencing the dashboard or operational hint and re-verify.

**Step 7: Audit threat-model coverage.** Walk T-18-01 through T-18-06; for each, confirm the corresponding plan has a passing acceptance criterion:
- T-18-01 → Plan 04 + Plan 05 (auth check unchanged + verbatim copy) — green via integration tests
- T-18-02 → Plan 05 (single transaction) — green via grep `prisma.\\$transaction(` count == 1
- T-18-03 → Plan 05 (env flag + idempotency marker) — green via integration test "401 without ENABLE_BACKFILL_ESS=1" + "already_done on second run"
- T-18-04 → Plan 01 + Plan 04 + Plan 07 + Plan 08 (STATUS_VALUES const + STATUS_VALUES.includes guard + TS narrowing on Record + STATUS_BADGE map) — green via tsc + integration test
- T-18-05 → Plan 04 + Plan 09 (numeric-only payload + Zod read-side validation) — green via integration test "Zod parse on payload succeeds"
- T-18-06 → Plan 06 (typed-config-constant choice over DB table) — green via Plan 10 Task 1's HYPERPARAMETERS sanity test enforcing typed shape

If any threat lacks a green test row, STOP and route back.
  </action>
  <verify>
    <automated>npm test -- --run && npm run test:integration && npm run test:e2e && grep -c "nyquist_compliant: true" .planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All three test suites exit 0 in this task's verify command
    - `.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md` no longer contains literal `_pending — gsd-planner fills_` (placeholder removed)
    - `.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md` contains literal `nyquist_compliant: true`
    - `.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md` per-task map has ≥15 rows (one per task across 10 plans), every row Status column = ✅
    - `.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md` references all 5 requirement IDs (CORE-ML-01..05) at least once each in the Requirement column
    - `.planning/phases/18-time-decayed-bayesian-updates-ess/18-VALIDATION.md` references all 6 threat IDs (T-18-01..06) at least once each in the Threat Ref column
  </acceptance_criteria>
  <done>Full suite green, 3 LOOKS-DONE-BUT-ISN'T gates passed, per-task map populated, nyquist_compliant flipped to true, every requirement + threat audited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator final acceptance — Phase 18 sign-off</name>
  <what-built>
    All 10 plans executed:
    - Wave 0: 10 test stubs scaffolded (Plan 00)
    - Wave 1: math primitives + CV utility + schema migration with [BLOCKING] db push (Plans 01–03)
    - Wave 2: cron rewire (decay+ESS+two-of-two drift+EXPLORATORY-WATCH), backfill cron, operator-tuned HYPERPARAMETERS (Plans 04–06)
    - Wave 3: engine-context surface, EngineCalibrationPanel ESS column + WatchBadge, /insights ESS+recovery counter (Plans 07–09)
    - Wave 4: HYPERPARAMETERS sanity test, full-suite green, per-task map populated, nyquist_compliant: true (this plan)
  </what-built>
  <how-to-verify>
    1. **Production parity check:** Read `vercel.json` cron config — confirm the daily `learn` cron schedule (`30 7 * * *`) is unchanged. Phase 18 changes the cron's *behavior*, not its *schedule*.
    2. **Open the live engine page** for a ticker that has accumulated outcomes: `https://ciphersearch.app/research/<ticker>` (or local `/research/<ticker>` if not yet pushed to prod).
    3. Confirm the EngineCalibrationPanel shows:
       - `ESS=<float>` instead of `n=<int>` for every cell with effective_sample_size > 0
       - Old persisted reports lacking ESS still render `n=<int>` gracefully (back-compat per Plan 07/08)
       - Cells in 'EXPLORATORY-WATCH' (if any) show the WatchBadge with "regime stability: watching" copy
    4. **Open `/insights`.** Confirm the patterns table shows:
       - ESS column rendered alongside (N=) debug column (D-12)
       - For any cell in 'EXPLORATORY-WATCH', recovery counter row reads `<X>/14 clear days` per D-09 step 4
       - Recovery-ready operational hint appears when X≥14 AND ESS≥30
    5. **Run the dev cron locally:**
       ```
       curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/learn
       ```
       Expect 200 with non-zero `ess_writes` or similar in the JSON response. Tail dev server logs for any drift_alert or drift_clear LearningEvent writes.
    6. **Run the backfill cron once on dev branch (D-13 manual UAT):**
       ```
       ENABLE_BACKFILL_ESS=1 vercel env pull .env.local
       curl -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/backfill-ess
       ```
       Expect first call → `{ status: 'completed', cells_updated: 504, ... }`; second call → `{ status: 'already_done', ... }`. Idempotency confirmed.
    7. **Sanity-check git diff:** `git diff main...HEAD --stat` should show ~10–20 changed files. The schema diff should show only +2 lines (D-19 additive-only invariant). The learning.ts diff should show net additions only (no existing function bodies modified except the patternStatus signature extension).
    8. **Confirm production deploy plan:** the operator follows VALIDATION.md "Manual-Only Verifications" row 3 (production backfill) post-deploy: `vercel env add ENABLE_BACKFILL_ESS production`, deploy, hit `/api/cron/backfill-ess` once with the production CRON_SECRET, verify `ess_backfill_complete` LearningEvent in production DB, then `vercel env rm ENABLE_BACKFILL_ESS production` to disable the route.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 18 complete, or describe any final tweaks/issues. After approval, the orchestrator will commit and prepare a Phase 18 milestone retrospective.</resume-signal>
  <files>(no production code in this checkpoint task — operator edits files described in how-to-verify)</files>
  <action>This is a checkpoint task. Follow the how-to-verify steps above. The action is the operator workflow: run the listed commands, inspect outputs, paste/confirm as instructed, and resume with the resume-signal phrase.</action>
  <verify>
    <automated>echo CHECKPOINT — operator confirms via resume-signal; downstream tasks gate on signal</automated>
  </verify>
  <done>Operator typed the resume-signal phrase confirming the checkpoint condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| All Phase 18 surfaces | Audited via per-task map — every threat ID has at least one passing test row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Spoofing | Cron auth on /api/cron/learn AND /api/cron/backfill-ess | mitigate | Audit row in per-task map confirms grep + integration test pass for both routes |
| T-18-02 | Tampering | Backfill partial-write recovery | mitigate | Audit row confirms `prisma.$transaction(` call count == 1 in backfill route |
| T-18-03 | DoS | Repeated backfill | mitigate | Audit row confirms env-flag + idempotency marker tested |
| T-18-04 | Tampering | Status enum poisoning | mitigate | Audit row confirms STATUS_VALUES const + STATUS_VALUES.includes runtime guard + TS narrowing on STATUS_BADGE Record |
| T-18-05 | DoS via deserialization | drift_alert payload | mitigate | Audit row confirms numeric-only payload constructed in Plan 04 + Zod validation on read in Plan 09 |
| T-18-06 | Tampering | Hyperparameter values | mitigate | Audit row confirms typed-config-constant approach + Plan 10 Task 1's sanity test enforcing typed shape + grid bounds |
</threat_model>

<verification>
- `npm test -- --run && npm run test:integration && npm run test:e2e` all exit 0
- 18-VALIDATION.md frontmatter `nyquist_compliant: true` AND `status: approved`
- Per-task map fully populated with ≥15 rows, all Status=✅
- All 6 T-18-XX threats have at least one map row referencing them
- All 5 requirement IDs CORE-ML-01..05 have at least one map row referencing them
- Operator sign-off via Task 3 checkpoint
</verification>

<success_criteria>
- All 8 must_haves from orchestrator quality_gate verified (decay+ESS write, per-class λ from grid, ESS-narrowed CIs, synthetic-drift fires & N=29 doesn't, ESS column + watch badge in panel, idempotent backfill, nyquist_compliant: true, threat mitigations)
- Full test suite green
- 18-VALIDATION.md fully populated and approved
- Operator sign-off complete
</success_criteria>

<output>
After completion, create `.planning/phases/18-time-decayed-bayesian-updates-ess/18-10-SUMMARY.md` AND `.planning/phases/18-time-decayed-bayesian-updates-ess/18-PHASE-SUMMARY.md`. The phase summary lists every plan, every requirement satisfied, every threat mitigated, and the operator-approved sign-off timestamp.
</output>

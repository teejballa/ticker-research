---
phase: 18-time-decayed-bayesian-updates-ess
plan: 03
type: execute
wave: 1
depends_on: [18-00]
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements: [CORE-ML-01]
must_haves:
  truths:
    - "LearnedPattern model has effective_sample_size column with Float NOT NULL DEFAULT 0"
    - "LearnedPattern model has n_trials_attempted column with Int NOT NULL DEFAULT 0"
    - "Live Neon dev branch DDL reflects the new columns (verified via npx prisma db push exit 0 + smoke SELECT)"
    - "All 504 existing LearnedPattern rows return effective_sample_size=0 immediately post-push (DEFAULT 0 soak per D-19)"
    - "Migration is additive ONLY — no column drops, no type changes (D-19 invariant)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "LearnedPattern with effective_sample_size + n_trials_attempted columns"
      contains: "effective_sample_size Float"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Neon Postgres dev branch (live DB)"
      via: "npx prisma db push (BLOCKING task — must succeed before phase verification)"
      pattern: "npx prisma db push"
    - from: "prisma/schema.prisma"
      to: "src/app/api/cron/learn/route.ts (Plan 18-04)"
      via: "Prisma client types — Plan 04 reads/writes effective_sample_size; without push, prisma client types pass build but DB writes fail at runtime"
      pattern: "effective_sample_size"
---

<objective>
Additive Prisma schema migration adding `effective_sample_size Float NOT NULL DEFAULT 0` and `n_trials_attempted Int NOT NULL DEFAULT 0` to the `LearnedPattern` model, then push the schema live to Neon. Per CONTEXT D-15 these are the two columns Phase 18 needs (n_trials_attempted is reserved for Phase 21's FDR denominator but populated from P18 forward per the cross-cutting v2.0 mandate). Per D-19 the migration is **additive only** — no drops, no type changes; all existing 504 cells take DEFAULT 0 on push.

Purpose: This is the schema-side keystone. Until these columns exist in the live DB, Plan 04 (cron rewire) cannot write ESS. The `npx prisma db push` step is **[BLOCKING]** because Prisma client types come from the generated client (so build/typecheck pass without push, creating a false-positive verification state). Without this task, `learn` cron writes would throw at runtime against an actual Neon row.

D-XX coverage: D-15 (column additions), D-19 (additive-only invariant), D-20 (cron maxDuration: 300 — push duration well under).

Status enum: `LearnedPattern.status` is currently a free-form `String @default("EXPLORATORY")`. Per RESEARCH §Q5 step 1 and §"Threat Model Inputs" T-18-04, NO schema change is required to add `EXPLORATORY-WATCH` — the column already accepts arbitrary strings. T-18-04 mitigation is delivered by the `STATUS_VALUES` const in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md
@.planning/phases/18-time-decayed-bayesian-updates-ess/18-RESEARCH.md
@prisma/schema.prisma
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add effective_sample_size + n_trials_attempted columns to LearnedPattern</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (lines 94–115 — current LearnedPattern model)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md D-15 (exact column names + types + defaults), D-19 (additive-only invariant)
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-RESEARCH.md §Q5 (migration sequencing — column additions only, no enum widening because status is free-form String)
  </read_first>
  <action>
Edit `prisma/schema.prisma` lines 94–115 (`model LearnedPattern`). Insert TWO new lines BETWEEN the existing `sample_size       Int      @default(0)` line and the `hits              Int      @default(0)` line. The new lines must be exactly:

```prisma
  effective_sample_size Float    @default(0)
  n_trials_attempted    Int      @default(0)
```

After the edit, the LearnedPattern block reads (showing the salient diff):
```prisma
model LearnedPattern {
  id                String   @id @default(uuid())
  signal_class      String
  pattern_key       String
  cap_class         String
  horizon_days      Int
  alpha             Float    @default(1)
  beta              Float    @default(1)
  sample_size       Int      @default(0)
  effective_sample_size Float    @default(0)         // ← Phase 18 / D-15
  n_trials_attempted    Int      @default(0)         // ← Phase 18 / D-15 (reserved for P21 FDR)
  hits              Int      @default(0)
  brier_in_sample   Float?
  ... (rest unchanged)
}
```

Both columns are `NOT NULL` by virtue of `@default(...)` (Prisma's default behavior for non-nullable types with defaults). Do NOT mark either as `?` (nullable). Do NOT remove or change any existing column. Do NOT touch the `@@unique` or `@@map` lines.

DO NOT regenerate the Prisma client in this task (Task 3 handles that via `npx prisma db push`, which regenerates client as a side effect).

DO NOT touch the `LearningEvent`, `SentimentSnapshot`, `PriceOutcome`, or other models — out of scope for this plan.
  </action>
  <verify>
    <automated>grep -E "effective_sample_size Float\\s+@default\\(0\\)" prisma/schema.prisma && grep -E "n_trials_attempted\\s+Int\\s+@default\\(0\\)" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains literal `effective_sample_size Float` AND `@default(0)` on the same line
    - `prisma/schema.prisma` contains literal `n_trials_attempted` AND `Int` AND `@default(0)` on the same line
    - Both new lines appear inside the `model LearnedPattern` block (between `sample_size` and `hits`)
    - No existing column was removed (compare diff: only +2 lines, 0 - lines, 0 ~ modifications)
    - `git diff prisma/schema.prisma` shows exactly 2 added lines, 0 deleted, 0 modified outside the LearnedPattern block
  </acceptance_criteria>
  <done>Two additive columns added inside model LearnedPattern, schema file otherwise unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate the schema syntax and generate fresh Prisma client locally</name>
  <files>(no files written; prisma client regenerated in node_modules)</files>
  <read_first>
    - prisma/schema.prisma (post-edit from Task 1)
    - CLAUDE.md ("Schema migrations are additive-only" — D-19 commitment)
    - prisma.config.ts if present (Prisma 7 connection URL location)
  </read_first>
  <action>
Run `npx prisma format` to verify the schema parses. Then run `npx prisma generate` to regenerate the typed client locally (this updates `node_modules/.prisma/client`).

Expected output: `npx prisma format` exits 0 with reformatted file (or unchanged if already formatted). `npx prisma generate` exits 0 with "Generated Prisma Client" message.

Do NOT commit if either command exits non-zero — the schema edit in Task 1 is wrong. Do NOT run `db push` here — that is Task 3 (the [BLOCKING] task) and must be the LAST step before any verification.
  </action>
  <verify>
    <automated>npx prisma format && npx prisma generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma format` exits 0
    - `npx prisma generate` exits 0
    - The generated TypeScript types in `node_modules/.prisma/client/index.d.ts` contain literal `effective_sample_size: number` AND `n_trials_attempted: number` on the LearnedPattern model
    - `npx tsc --noEmit` still exits 0 (no broken consumers — there shouldn't be any since no code reads these columns yet)
  </acceptance_criteria>
  <done>Schema parses, client regenerated with new types, TS compile clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BLOCKING] Push schema to live Neon dev branch — phase cannot pass verification without this</name>
  <files>(no repo file written; Neon dev branch DDL updated)</files>
  <read_first>
    - .planning/phases/18-time-decayed-bayesian-updates-ess/18-CONTEXT.md D-19 (additive-only — `db push` is safe by definition for this migration)
    - prisma.config.ts (verify it points at the Neon dev branch URL, not production — `prisma db push` is non-reversible)
    - `.env.local` for `DATABASE_URL` (read-only — DO NOT log this value)
  </read_first>
  <action>
**[BLOCKING] — Phase 18 cannot reach verification without this step.**

Run:
```
npx prisma db push
```

Why this is blocking and not just "nice to have":
- Prisma client types come from the generated client (regenerated in Task 2), so `tsc` and `next build` both pass without ever touching the live DB.
- This creates a false-positive verification state where every plan downstream "looks done" but actually fails at runtime against the real Neon table that lacks the new columns.
- The cron in Plan 04 will throw `column effective_sample_size does not exist` on its first invocation against the dev branch.

After push completes, run a smoke test:
```
node -e "require('@prisma/client').PrismaClient.prototype; const p = new (require('@prisma/client').PrismaClient)(); p.learnedPattern.findFirst().then(r => { console.log('effective_sample_size:', r?.effective_sample_size, 'n_trials_attempted:', r?.n_trials_attempted); return p.$disconnect(); })"
```

Expected: prints `effective_sample_size: 0 n_trials_attempted: 0` (DEFAULT 0 applied to existing 504 rows) — confirms D-19 soak.

**Failure modes:**
- If push errors `Database connection refused` → user must wake up Neon dev branch (`npx prisma db pull` or click in Neon dashboard).
- If push errors `permission denied for table learned_patterns` → wrong DATABASE_URL pointing at a read-only replica; user must verify env.
- If push completes but smoke fails with `Unknown column` → client cache is stale; re-run `npx prisma generate` and retry smoke.

DO NOT use `npx prisma db push --accept-data-loss` here — D-19 invariant is additive-only, no data loss is possible by definition. If push prompts about data loss it means an UNEXPECTED change snuck into the schema; STOP and audit the diff.

DO NOT push to production. This task targets the dev branch only. Production migration is a separate operator step at release time (`prisma migrate deploy` runs in `vercel.json` buildCommand).
  </action>
  <verify>
    <automated>npx prisma db push 2>&1 | tee /tmp/p18-push.log && grep -E "in sync|Generated Prisma Client" /tmp/p18-push.log</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0
    - stdout contains literal "in sync" OR literal "Database is now in sync with the Prisma schema"
    - Smoke node command prints "effective_sample_size: 0" (DEFAULT 0 soak)
    - Smoke node command prints "n_trials_attempted: 0"
    - No "data loss" warning appeared during push (D-19 additive-only invariant verified at runtime)
    - The push command did NOT use `--accept-data-loss` flag
  </acceptance_criteria>
  <done>Live Neon dev branch has both new columns at DEFAULT 0 across all 504 cells. Phase 18 can now safely run downstream cron writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.prisma → live Neon dev branch | DDL change pushed via Prisma — only operator with DATABASE_URL secret can do this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| schema-tamper | Tampering | additive migration accidentally drops a column | mitigate | D-19 invariant + Task 1 acceptance criterion "0 deleted lines outside LearnedPattern block"; Task 3 fails fast if Prisma prompts about data loss |
| wrong-target | Elevation | push targets production instead of dev | mitigate | Task 3 read_first explicitly verifies prisma.config.ts points at dev branch; production migration runs only at Vercel build time via `prisma migrate deploy` |
| (T-18-04 already mitigated upstream) | — | status enum poisoning | n/a | Mitigated by Plan 01's STATUS_VALUES const, not by schema |
</threat_model>

<verification>
- `prisma/schema.prisma` contains the two new column declarations (greppable)
- `npx prisma format && npx prisma generate` exit 0
- `npx prisma db push` exits 0 with "in sync"
- Smoke `findFirst()` query returns rows with `effective_sample_size: 0` + `n_trials_attempted: 0`
</verification>

<success_criteria>
- 2 columns added (additive only)
- Live Neon dev branch DDL reflects the columns
- All 504 existing rows have DEFAULT 0 for the new columns
- D-19 (additive-only) invariant preserved — verified by absence of data-loss prompt during push
</success_criteria>

<output>
After completion, create `.planning/phases/18-time-decayed-bayesian-updates-ess/18-03-SUMMARY.md` recording the schema diff (2 added lines), the push command result, and the smoke `findFirst()` confirmation.
</output>

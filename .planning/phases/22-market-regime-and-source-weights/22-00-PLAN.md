---
phase: 22-market-regime-and-source-weights
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/regime/__tests__/classify.test.ts
  - src/lib/regime/__tests__/vix-percentile.test.ts
  - src/lib/evaluation/__tests__/fdr-hierarchical.test.ts
  - src/lib/sentiment/__tests__/source-tier-regime.test.ts
  - src/lib/sentiment/__tests__/source-tier-eb.test.ts
  - src/lib/sentiment/__tests__/aggregator-regime.test.ts
  - src/app/api/cron/__tests__/backfill-regime.test.ts
  - src/app/api/cron/__tests__/sentiment-scan-regime.test.ts
  - src/app/api/cron/__tests__/learn-transition-exclusion.test.ts
  - src/lib/evaluation/__tests__/regime-done-gate.test.ts
  - src/lib/__tests__/learned-pattern-regime.test.ts
  - tests/fixtures/regime/vix-history.json
  - tests/fixtures/regime/spy-history.json
autonomous: false
requirements:
  - CORE-ML-06
  - CORE-ML-08
must_haves:
  truths:
    - "Prisma schema carries regime + shrinkage_strength columns on SentimentSnapshot, LearnedPattern, PerSourceIC, and SourceTier — all DEFAULT 'ALL'."
    - "`npx prisma db push` is run against Neon and reports zero data loss; existing reads continue to work because every regime column is nullable-or-defaulted."
    - "Every RED stub from 22-VALIDATION.md §Wave 0 Gaps exists and fails with a meaningful error pointing at the missing implementation (not a syntax error)."
    - "Golden VIX + SPY fixtures exist in tests/fixtures/regime/ so Wave 1's classifier tests are deterministic and offline-runnable."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Additive regime columns on 4 models per RESEARCH §A"
      contains: "regime String @default(\"ALL\")"
    - path: "src/lib/regime/__tests__/classify.test.ts"
      provides: "RED stub for CORE-ML-07 (Wave 1 GREEN target)"
    - path: "src/lib/evaluation/__tests__/fdr-hierarchical.test.ts"
      provides: "RED stub for D-15 hierarchical BY → meta-BH (Wave 4 GREEN target)"
    - path: "src/lib/sentiment/__tests__/source-tier-regime.test.ts"
      provides: "RED stub for D-06 + D-09 cold-start chain (Wave 3 GREEN target)"
    - path: "src/lib/sentiment/__tests__/source-tier-eb.test.ts"
      provides: "RED stub for D-07 empirical-Bayes shrinkage (Wave 3 GREEN target)"
    - path: "src/lib/sentiment/__tests__/aggregator-regime.test.ts"
      provides: "RED stub for D-08 per-row regime read (Wave 3 GREEN target)"
    - path: "src/app/api/cron/__tests__/backfill-regime.test.ts"
      provides: "RED stub for D-10 + D-12 backfill checkpoint (Wave 2 GREEN target)"
    - path: "src/app/api/cron/__tests__/sentiment-scan-regime.test.ts"
      provides: "RED stub for CORE-ML-08 scan-time write (Wave 1 GREEN target)"
    - path: "src/app/api/cron/__tests__/learn-transition-exclusion.test.ts"
      provides: "RED stub for D-05 sample-relative exclusion (Wave 4 GREEN target)"
    - path: "src/lib/evaluation/__tests__/regime-done-gate.test.ts"
      provides: "RED stub for D-14 Brier-lift + BCa CI (Wave 5 GREEN target)"
    - path: "src/lib/__tests__/learned-pattern-regime.test.ts"
      provides: "Live-DB schema test that LearnedPattern.regime is writable + readable"
    - path: "tests/fixtures/regime/vix-history.json"
      provides: "Deterministic VIX 60d series fixture"
    - path: "tests/fixtures/regime/spy-history.json"
      provides: "Deterministic SPY 200d MA cross fixture"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Neon"
      via: "npx prisma db push (operator-confirmed checkpoint)"
      pattern: "regime String @default\\(\"ALL\"\\)"
    - from: "RED test stubs"
      to: "Waves 1–5 GREEN implementations"
      via: "filename match with downstream wave's `files_modified`"
      pattern: "expect\\(.*\\).toThrow\\(/Wave \\d/\\)"
---

<objective>
Wave 0 ships the **additive Prisma migration** that gives every downstream wave a place to put regime data, then pushes to Neon under an operator-confirmed checkpoint (mirrors P27-01 DB-push precedent). It also seeds every RED test stub the later waves will turn GREEN, so the executor is never inventing test contracts mid-flight.

Purpose: per D-11 phased cutover, **step 1 = additive columns with DEFAULT 'ALL'**. Existing reads continue to work because the `'ALL'` row is consumed via the D-09 cold-start chain. The unique-constraint flip is deferred to Wave 5 (D-11 step 4), strictly after the 2-week soak (D-13).

Output: schema columns landed in Neon; 11 RED test stubs across 5 directories; 2 golden fixtures for Wave 1 determinism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-market-regime-and-source-weights/22-CONTEXT.md
@.planning/phases/22-market-regime-and-source-weights/22-RESEARCH.md
@.planning/phases/22-market-regime-and-source-weights/22-VALIDATION.md
@prisma/schema.prisma
@src/lib/labels/compute.ts

<interfaces>
<!-- Extracted from prisma/schema.prisma — exact line ranges per RESEARCH §A. -->
<!-- Executor edits these existing models additively; NEVER drops or retypes columns. -->

From prisma/schema.prisma:42-65 (SentimentSnapshot):
- Has `ticker String`, `scanned_at DateTime`, sentiment fields, source provenance JSON.
- Has `@@unique([ticker, scanned_at])` — DO NOT touch.
- Has `@@index([ticker, scanned_at(sort: Desc)])` — extend with regime variant.

From prisma/schema.prisma:122-155 (LearnedPattern):
- Composite key today: `@@unique([signal_class, pattern_key, cap_class, horizon_days])`.
- DO NOT flip the constraint in Wave 0 — Wave 5 owns that. Only ADD the column.

From prisma/schema.prisma:351-368 (PerSourceIC):
- Per-source rolling Spearman IC panel. Has `source_id`, `computed_at`, `horizon_days`, IC scalar, n_observations.
- Today `@@unique([source_id, computed_at, horizon_days])`. Same rule — column add only in Wave 0.

From prisma/schema.prisma:582-594 (SourceTier):
- Has `source_id String`, `computed_at DateTime`, `weight Float`, `tier String`.
- Today `@@index([source_id, computed_at(sort: Desc)])`. Add regime + shrinkage_strength columns.

From src/lib/labels/compute.ts:1-22 (header pattern for new module headers):
- @knowable_at annotation is mandatory (CLAUDE.md rule #6).
- "There is NO parallel/forked label logic" — same discipline applies to the regime classifier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive regime + shrinkage_strength columns to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <behavior>
    Per RESEARCH §A file-by-file change map (line-anchored):
    - SentimentSnapshot (L42-65): add `regime String @default("ALL")`, `regime_vix_level Float?`, `regime_vix_pctile Float?`, `regime_ma_diff Float?`. Add `@@index([ticker, regime, scanned_at(sort: Desc)])`. DO NOT change `@@unique([ticker, scanned_at])`.
    - LearnedPattern (L122-155): add `regime String @default("ALL")`. Keep existing `@@unique([signal_class, pattern_key, cap_class, horizon_days])` until Wave 5 (D-11 step 4).
    - PerSourceIC (L351-368): add `regime String @default("ALL")`. Keep existing `@@unique` until Wave 5.
    - SourceTier (L582-594): add `regime String @default("ALL")`, `shrinkage_strength Float?`. Add `@@index([source_id, regime, computed_at(sort: Desc)])`. Keep existing `@@index([source_id, computed_at(sort: Desc)])` until Wave 5.
    - LearningEvent (per RESEARCH §Q3): add `regime String?` as additive nullable top-level column (mirrors existing top-level `signal_class`, `pattern_key`, `cap_class`, `horizon_days`).
    All changes are reversible (additive). Use `// PHASE 22 (D-06, D-11 step 1):` line comments on each new column for traceability.
  </behavior>
  <action>
    Edit prisma/schema.prisma at the 5 named line ranges. After editing, run `npx prisma format` and `npx prisma validate`. DO NOT run `db push` in this task — that is a separate [BLOCKING] task below per the schema_push_requirement.

    Per D-02: this is a 4-bucket regime — the column type is `String` (free-form union enforced at the application layer via `src/lib/regime/types.ts` in Wave 1), NOT a Postgres enum. Justification: P20-B-04 tier values are also strings; matches established convention; avoids a destructive enum-redefinition migration when Phase 22.5 reconsiders bull/bear/chop 3-state (deferred per CONTEXT).

    Per D-15 RESEARCH §A LearningEvent extension: the `regime String?` column on LearningEvent is what Wave 4's `cell_promoted` / `cell_demoted` writes will key on. Without it, dashboards cannot filter by regime.
  </action>
  <verify>
    <automated>npx prisma format &amp;&amp; npx prisma validate &amp;&amp; grep -E 'regime\s+String\s+@default\("ALL"\)' prisma/schema.prisma | wc -l | grep -E '^\s*[4-9]' &amp;&amp; grep -E 'shrinkage_strength\s+Float\?' prisma/schema.prisma</automated>
  </verify>
  <done>
    `npx prisma validate` exits 0. `grep` shows ≥4 occurrences of `regime String @default("ALL")` (SentimentSnapshot, LearnedPattern, PerSourceIC, SourceTier). `shrinkage_strength Float?` appears on SourceTier. No existing `@@unique` constraints are removed or modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed 11 RED test stubs + 2 golden fixtures for Waves 1–5</name>
  <files>
    src/lib/regime/__tests__/classify.test.ts,
    src/lib/regime/__tests__/vix-percentile.test.ts,
    src/lib/evaluation/__tests__/fdr-hierarchical.test.ts,
    src/lib/sentiment/__tests__/source-tier-regime.test.ts,
    src/lib/sentiment/__tests__/source-tier-eb.test.ts,
    src/lib/sentiment/__tests__/aggregator-regime.test.ts,
    src/app/api/cron/__tests__/backfill-regime.test.ts,
    src/app/api/cron/__tests__/sentiment-scan-regime.test.ts,
    src/app/api/cron/__tests__/learn-transition-exclusion.test.ts,
    src/lib/evaluation/__tests__/regime-done-gate.test.ts,
    src/lib/__tests__/learned-pattern-regime.test.ts,
    tests/fixtures/regime/vix-history.json,
    tests/fixtures/regime/spy-history.json
  </files>
  <behavior>
    Each RED stub MUST:
    - Import the target module (will fail to resolve → RED).
    - Declare at least 2 `it.todo(...)` or `it(...)` blocks naming the contract under test (e.g. `it('returns regime="ALL" when VIX history is null (D-09 cold-start)')`).
    - Cite the D-XX or CORE-ML-XX it covers in a leading file-header comment.
    - Have NO real assertions yet — Wave 0 is the contract-skeleton wave, not implementation.

    The two JSON fixtures must contain:
    - `vix-history.json`: 65 trading days of `{ date, close }` with known 60d percentile boundary values (50th percentile crosses at index 31 → bull-low-vol/bull-high-vol flip).
    - `spy-history.json`: 220 trading days with `{ date, close }` where MA50 - MA200 crosses zero at a known index (so a Wave 1 unit test can assert the trend axis flip).
  </behavior>
  <action>
    For each test file, write:

    ```ts
    /**
     * PHASE 22 Wave 0 RED stub — Wave {N} delivers GREEN.
     * Covers: {CORE-ML-XX} per 22-VALIDATION.md §Wave 0 Gaps.
     * Decision refs: D-XX, D-YY per 22-CONTEXT.md.
     */
    import { describe, it } from 'vitest';
    // Intentionally imports the target module so the file fails to resolve until Wave N lands.
    import { classifyRegimeAt } from '../classify'; // adjust per file

    describe('classifyRegimeAt — Wave 1 contract', () => {
      it.todo('returns regime="ALL" when VIX history is null (D-09 cold-start)');
      it.todo('returns bull-low-vol when MA50 > MA200 AND VIX < 60d 50th-pct (D-02 + D-04)');
      it.todo('returns bear-high-vol when MA50 < MA200 AND VIX >= 60d 50th-pct');
      it.todo('annotates result with vix_60d_percentile + spy_ma_50_minus_200 for audit');
    });
    ```

    Match the import paths to RESEARCH §B for the classifier suite, §E/F for the sentiment suite, §G for the learn suite, §H for the FDR suite, §I for the done-gate suite.

    For the fixtures, hand-write JSON arrays. VIX values should be a deterministic walk (e.g. `15.0` → `28.5` linearly across 65 entries; 50th percentile at index 31 ≈ `21.75`). SPY values similar.

    DO NOT add `@knowable_at` annotations to test files — only to production code (CLAUDE.md rule #6 + `scripts/check-feature-asof.ts`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/regime/__tests__/ src/lib/evaluation/__tests__/fdr-hierarchical.test.ts src/lib/evaluation/__tests__/regime-done-gate.test.ts src/lib/sentiment/__tests__/source-tier-regime.test.ts src/lib/sentiment/__tests__/source-tier-eb.test.ts src/lib/sentiment/__tests__/aggregator-regime.test.ts src/app/api/cron/__tests__/backfill-regime.test.ts src/app/api/cron/__tests__/sentiment-scan-regime.test.ts src/app/api/cron/__tests__/learn-transition-exclusion.test.ts --reporter=dot 2&gt;&amp;1 | grep -E 'Cannot find module|todo' | wc -l | grep -E '^\s*[1-9][0-9]*'</automated>
  </verify>
  <done>
    Every stub file exists. `npx vitest run` over them produces either module-resolution errors (RED state proving the implementation doesn't exist yet) OR `todo` markers — never silent pass. Both fixture JSON files parse with `node -e "JSON.parse(require('fs').readFileSync('...'))"`. The live-DB stub `learned-pattern-regime.test.ts` is gated on `RUN_LIVE_INTEGRATION` and skips by default.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: Operator runs `npx prisma db push` against Neon</name>
  <what-built>
    Additive schema migration (Task 1) is committed but not yet pushed to Neon. Per D-11 step 1, this is the **reversible** column-add migration — it adds 8 nullable-or-defaulted columns and 2 indexes. No data loss possible.
  </what-built>
  <how-to-verify>
    1. Open a terminal at the repo root.
    2. Confirm `DATABASE_URL` in `.env.local` points at the production Neon branch (per `[[feedback_env_parity]]` and `[[feedback_deploy_after_fix]]` — the user keeps prod + .env.local in lockstep).
    3. Run: `npx prisma db push`
    4. Confirm output includes "Your database is now in sync with your Prisma schema" and lists the new columns + indexes.
    5. Run: `npx prisma db pull --print | grep -E 'regime|shrinkage_strength' | head -20` — confirm Neon now reports the columns.
    6. Manually spot-check via Neon dashboard or psql: `SELECT COUNT(*) FROM "SentimentSnapshot" WHERE regime = 'ALL';` — should return total row count (every existing row defaulted to `'ALL'`).
  </how-to-verify>
  <resume-signal>Type "pushed" once `prisma db push` reports success AND the `regime='ALL'` row count matches the table's total.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Neon | The `prisma db push` command writes DDL to production Postgres. Single elevation point for this wave. |
| Test stubs → Production code | RED stubs intentionally import non-existent modules. No untrusted input crosses here, but mis-named imports can mask real bugs in later waves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-00-01 | Tampering | `prisma db push` against wrong DB branch | mitigate | Task 3 [BLOCKING] explicitly checks `DATABASE_URL` and the post-push row-count smoke check. Operator-gated, not autonomous. |
| T-22-00-02 | Denial of Service | Schema lock during `prisma db push` on a hot table | accept | Additive nullable-or-defaulted columns are non-blocking in Postgres ≥ 11 (Neon is on 16). RESEARCH §A "Migration sequencing" confirms zero downtime. |
| T-22-00-03 | Information Disclosure | Test fixtures contain market data | accept | VIX + SPY closes are public data. No PII, no API keys, no credentials. |
| T-22-00-04 | Repudiation | Schema change shipped without traceability | mitigate | Every new column carries a `// PHASE 22 (D-XX):` line comment per Task 1. Git blame + the Wave 0 commit hash close the audit trail. |
</threat_model>

<verification>
End of Wave 0:
1. `npx prisma validate` exits 0.
2. `grep -E 'regime String @default\("ALL"\)' prisma/schema.prisma | wc -l` ≥ 4 (SentimentSnapshot, LearnedPattern, PerSourceIC, SourceTier).
3. `grep -E 'shrinkage_strength Float\?' prisma/schema.prisma` returns the SourceTier line.
4. Operator confirms `prisma db push` succeeded against Neon (Task 3 resume signal).
5. `npx vitest run src/lib/regime/ src/lib/evaluation/__tests__/fdr-hierarchical.test.ts src/lib/evaluation/__tests__/regime-done-gate.test.ts src/lib/sentiment/__tests__/source-tier-regime.test.ts src/lib/sentiment/__tests__/source-tier-eb.test.ts src/lib/sentiment/__tests__/aggregator-regime.test.ts src/app/api/cron/__tests__/backfill-regime.test.ts src/app/api/cron/__tests__/sentiment-scan-regime.test.ts src/app/api/cron/__tests__/learn-transition-exclusion.test.ts --reporter=dot` exits non-zero (RED proves implementation is pending).
6. `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/regime/vix-history.json'))"` exits 0.
7. `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/regime/spy-history.json'))"` exits 0.
</verification>

<success_criteria>
- 4 Prisma models carry the additive `regime` column with `DEFAULT 'ALL'` (CORE-ML-06, CORE-ML-08).
- SourceTier carries the additional `shrinkage_strength Float?` column (D-07).
- LearningEvent carries `regime String?` (RESEARCH §Q3).
- Neon is in sync; no production rows lost or rewritten.
- 11 RED stubs land, each citing its D-XX / CORE-ML-XX.
- 2 deterministic JSON fixtures land at `tests/fixtures/regime/`.
- All existing `@@unique` constraints UNCHANGED (Wave 5 owns the flip per D-11 step 4).
</success_criteria>

<output>
After completion, create `.planning/phases/22-market-regime-and-source-weights/22-00-SUMMARY.md` documenting:
- Which 4 (5 with LearningEvent) Prisma models gained columns
- Exact `prisma db push` output excerpt
- Row counts post-push (every model's `regime='ALL'` count = total row count)
- List of 11 RED stub files with their target wave/decision
- Hand-off to Wave 1: "Wave 1 imports `tests/fixtures/regime/{vix,spy}-history.json` for deterministic classifier tests."
</output>

---
phase: 22-market-regime-and-source-weights
plan: 05
type: execute
wave: 5
depends_on: ["22-03", "22-04"]
files_modified:
  - src/lib/evaluation/regime-done-gate.ts
  - src/lib/evaluation/__tests__/regime-done-gate.test.ts
  - scripts/phase-22-status.ts
  - package.json
  - prisma/schema.prisma
  - src/lib/engine-context.ts
  - src/components/EngineCalibrationPanel.tsx
  - src/components/SourceMixExpanded.tsx
  - src/components/__tests__/EngineCalibrationPanel.test.tsx
  - tests/e2e/source-mix-row.spec.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - docs/paper/methodology.md
autonomous: false
requirements:
  - CORE-ML-06
  - CORE-ML-09
  - CORE-ML-21
  - CORE-ML-22
must_haves:
  truths:
    - "`regimeDoneGate(cellEvals)` computes Brier-lift = `brier_all - brier_regime` per cell, applies `bootstrapBCa(n=10000)`, promotes regime-flipped cells where `point > 0.005` AND 95% CI excludes 0 (D-14)."
    - "`scripts/phase-22-status.ts` exits 0 IF (a) the soak window (D-13, 14 calendar days) has elapsed AND (b) the done-gate either passes (≥1 cell promoted) OR honestly reports 0 ACTIVE regime cells per D-16."
    - "Migration 2 flips `@@unique` to include `regime` on LearnedPattern, PerSourceIC, and SourceTier — irreversible cutover per D-11 step 4. Operator-gated."
    - "EngineCalibrationPanel renders the new always-visible \"Source mix\" row per UI-SPEC: regime pill + top-3 sources + \"Show full ranking\" disclosure. Cold-start regime='ALL' shows the `REGIME-UNCONDITIONAL` variant (UI-SPEC Empty / Null states)."
    - "`engine-context.ts` surfaces `calibration.source_mix.{regime, top_sources}` with weights, drift, and shrinkage_strength — authoritative numerics rule preserved (UI never computes numbers)."
    - "ROADMAP.md updated to '4-bucket regime (bull/bear × low-vol/high-vol)' (D-02 — resolves the prior REQUIREMENTS↔ROADMAP conflict)."
    - "REQUIREMENTS.md CORE-ML-06..10 + CORE-ML-20..22 marked complete; `docs/paper/methodology.md` Phase 22 section appended (CLAUDE.md Statistical-Methods Reference rule — cite Benjamini-Bogomolov 2014, Hamilton 1989, Ang-Bekaert 2002, Efron 1987)."
  artifacts:
    - path: "src/lib/evaluation/regime-done-gate.ts"
      provides: "`regimeDoneGate(cellEvals)` per D-14"
      exports: ["regimeDoneGate", "DoneGateResult"]
    - path: "scripts/phase-22-status.ts"
      provides: "Composite done-gate runnable via `npm run phase-22-status`"
    - path: "src/components/EngineCalibrationPanel.tsx"
      provides: "Always-visible 'Source mix' row (UI-SPEC §Surface Inventory)"
    - path: "src/components/SourceMixExpanded.tsx"
      provides: "Client-island expand panel with full 8-source ranking + 30d weight-drift sparkline (UI-SPEC §Render Boundaries)"
    - path: "src/lib/engine-context.ts"
      provides: "`calibration.source_mix` extraction per UI-SPEC Data Contract"
  key_links:
    - from: "scripts/phase-22-status.ts"
      to: "src/lib/evaluation/regime-done-gate.ts + src/lib/evaluation/bootstrap.ts"
      via: "Composite gate: soak duration check + done-gate evaluation + per-regime cell-promotion counts"
      pattern: "regimeDoneGate\\(.*\\)|bootstrapBCa"
    - from: "src/components/EngineCalibrationPanel.tsx"
      to: "src/lib/engine-context.ts (calibration.source_mix)"
      via: "Props-down render of source_mix; component does NO math (UI-SPEC §Data Contract)"
      pattern: "source_mix\\.top_sources"
    - from: "prisma/schema.prisma (Migration 2)"
      to: "Neon"
      via: "Unique-constraint flip — irreversible per D-11 step 4"
      pattern: "@@unique\\(\\[.*regime\\]\\)"
---

<objective>
Wave 5 closes Phase 22. Four coupled deliverables:

1. **Done-gate** (`regimeDoneGate` + `scripts/phase-22-status.ts`) — D-14 Brier-lift on regime-flipped cells ≥ 0.005 with BCa CI excluding 0. Composite script also verifies the 14-day soak window has elapsed (D-13). Per D-16: 0 ACTIVE regime cells is a VALID IS-paper finding — script reports honestly either way.

2. **Migration 2 — unique-constraint flip** ([BLOCKING] checkpoint per R5). Per D-11 step 4: flip `@@unique` on `LearnedPattern`, `PerSourceIC`, `SourceTier` to include `regime`. **Irreversible.** Operator confirms after done-gate result before pushing.

3. **EngineCalibrationPanel "Source mix" row** (D-17, CORE-ML-22) — implements the 22-UI-SPEC contract verbatim: always-visible row + click-to-expand panel + 30d weight-drift sparkline. RSC + 1 client island. Engine-context.ts surfaces the authoritative `source_mix` payload.

4. **Phase bookkeeping** — ROADMAP fix (D-02 2-bucket→4-bucket), REQUIREMENTS completion markers, methodology paper section append (CLAUDE.md Statistical-Methods Reference rule).

Wave 5 cannot start until `22-04-SOAK.md` records ≥14 calendar days elapsed from the first relearn (D-13). Done-gate evaluation runs BEFORE migration 2 — if the done-gate fails AND the user is satisfied with the "0 ACTIVE cells" IS-paper finding, the operator may still push migration 2 (cell space is still useful for the source-mix UI). If the operator wants to abort, migration 2 simply doesn't run (the schema stays additive-only, fully reversible).

Output: done-gate primitive + script; migration 2 [BLOCKING]; source-mix UI; methodology paper section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-market-regime-and-source-weights/22-CONTEXT.md
@.planning/phases/22-market-regime-and-source-weights/22-RESEARCH.md
@.planning/phases/22-market-regime-and-source-weights/22-UI-SPEC.md
@.planning/phases/22-market-regime-and-source-weights/22-03-SUMMARY.md
@.planning/phases/22-market-regime-and-source-weights/22-04-SUMMARY.md
@.planning/phases/22-market-regime-and-source-weights/22-04-SOAK.md
@src/lib/evaluation/bootstrap.ts
@src/lib/engine-context.ts
@src/components/EngineCalibrationPanel.tsx
@docs/paper/methodology.md

<interfaces>
<!-- From src/lib/evaluation/bootstrap.ts:120 (P21.1 Wave 1). -->

Existing primitive:
```typescript
export interface BCaResult {
  point: number;
  low: number;     // 95% CI low
  high: number;    // 95% CI high
  n: number;
  method: 'BCa' | 'percentile';  // n<10 falls back to percentile
}

export function bootstrapBCa(
  samples: number[],
  statistic: (xs: number[]) => number,
  nResamples: number,
  alpha: number,
): BCaResult;
```

Target shape:
```typescript
import { bootstrapBCa } from './bootstrap';
import type { CellEvalResult } from '@/app/api/cron/learn/route';
import type { RegimeLabel } from '@/lib/regime/types';

export interface DoneGateResult {
  promoted_cells: Array<{
    signal_class: string;
    pattern_key: string;
    cap_class: string;
    horizon_days: number;
    regime: RegimeLabel;
    brier_lift: BCaResult;  // point, low, high
  }>;
  total_evaluated: number;
  threshold_used: 0.005;
}

/**
 * Phase 22 D-14 done-gate. Computes Brier-lift = brier_all - brier_regime
 * per cell that has both a regime-specific row AND an 'ALL' row. Promotes
 * if point > 0.005 AND BCa 95% CI low > 0.
 */
export function regimeDoneGate(cellEvals: CellEvalResult[]): DoneGateResult;
```

UI-SPEC contract (22-UI-SPEC.md):
- Always-visible row inserted between Concept Drift row (L1004-1017) and AlignmentDisagreementBlocks (L1020).
- RSC `SourceMixRow` (defined inline in `EngineCalibrationPanel.tsx`) + client island `SourceMixExpanded` (new file).
- Data contract: `calibration.source_mix.{regime, top_sources[{source_id, weight, weight_unconditional, weight_drift_30d, drift_direction, delta_pp_30d, is_cold_start_fallback}]}`.
- Test hooks: `source-mix-row`, `source-mix-regime-pill`, `source-mix-pill-1..3`, `source-mix-disclosure`, `source-mix-expanded`, `source-mix-sparkline-{source_id}`, `source-mix-cold-start-banner`, `source-mix-empty`.
- Copywriting + spacing + color tokens specified in UI-SPEC; component does ZERO math (engine-context.ts is the authoritative numerics boundary).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `regimeDoneGate` + `scripts/phase-22-status.ts`</name>
  <files>
    src/lib/evaluation/regime-done-gate.ts,
    src/lib/evaluation/__tests__/regime-done-gate.test.ts,
    scripts/phase-22-status.ts,
    package.json
  </files>
  <behavior>
    - `regime-done-gate.ts`: implement per the §Interfaces shape.
      - Input: array of `CellEvalResult` (from Wave 4 learn cron).
      - Group by 4-tuple `(signal_class, pattern_key, cap_class, horizon_days)`. For each group with both a regime-specific row AND a corresponding `'ALL'` row:
        - Compute the per-observation Brier-lift series = `[brier_all_i - brier_regime_i for i in matched_obs]`.
        - Apply `bootstrapBCa(brier_lift_series, mean, nResamples=10000, alpha=0.05)`.
        - Promote if `result.point > 0.005` AND `result.low > 0` (D-14 magnitude + CI excludes 0).
      - Return `DoneGateResult` shape.
    - `scripts/phase-22-status.ts`: composite gate.
      - Reads `22-04-SOAK.md` for the first-relearn timestamp; computes `days_elapsed`. EXITS 1 with a clear message if `days_elapsed < 14`.
      - Reads the latest `LearningEvent` rows (or queries Neon directly via Prisma) to assemble the `CellEvalResult[]` array.
      - Calls `regimeDoneGate(...)`. Prints a per-regime promotion summary table.
      - EXITS 0 if (a) ≥1 cell promoted, OR (b) `--allow-null-finding` flag is set AND 0 cells promoted (D-16 honest reporting).
      - EXITS 1 otherwise (no cells, no flag — operator must explicitly accept the null finding).
    - `package.json`: add `"phase-22-status": "tsx scripts/phase-22-status.ts"` (mirrors `phase-21.1-status` precedent).

    Tests (turning Wave 0 stub GREEN):
    - Synthetic CellEvalResult input with one regime-cell that has clear Brier-lift (`brier_all - brier_regime` series with mean +0.01, low CI +0.003) → promoted.
    - Synthetic input with a noisy regime-cell (mean +0.001, CI spans 0) → NOT promoted (CI includes 0).
    - Synthetic input with a regime-cell that has mean +0.01 but the BCa low is +0.001 → promoted (low > 0).
    - Empty input → `{ promoted_cells: [], total_evaluated: 0 }` (does NOT throw).
    - Script integration: missing `22-04-SOAK.md` → exit 1 with clear error.
    - Script integration: soak elapsed AND ≥1 cell promoted → exit 0.
    - Script integration: soak elapsed AND 0 cells + `--allow-null-finding` → exit 0 with honest "0 ACTIVE cells, IS-paper valid null finding" message.
  </behavior>
  <action>
    Implement `regime-done-gate.ts` per the §Interfaces shape. The `mean` statistic for BCa is just `(xs) => xs.reduce((a, b) => a + b, 0) / xs.length` — small helper.

    For the script: use `tsx` (already in devDependencies for `phase-21.1-status`). Read `22-04-SOAK.md` by parsing the `First relearn timestamp:` line. Compute days elapsed via `(Date.now() - parsed) / (1000 * 60 * 60 * 24)`.

    Per D-14 magnitude: the `0.005` threshold is the SAME constant as P21.1's `BRIER_LIFT_THRESHOLD`. Reuse it: `import { BRIER_LIFT_THRESHOLD } from '@/lib/evaluation/constants';` if such a constant file exists, else inline the literal with a `// D-14: matches BRIER_LIFT_THRESHOLD from P21.1` comment.

    Per Q7 in RESEARCH: the script compares against the LIVE `'ALL'` row (apples-to-apples within the same fold, same observation pool), NOT a frozen pre-P22 snapshot. The `CellEvalResult[]` input MUST include both regime-cell AND `'ALL'`-cell rows from the same relearn cycle.

    Composite gate output format:
    ```
    Phase 22 status:
      Soak window: 14.2 days elapsed (target: 14.0)  ✓
      Done-gate:
        Total cells evaluated: 124
        Promoted (Brier-lift > 0.005, CI > 0): 3
          - sentiment_class=stocktwits pattern=high_call_ratio cap=small horizon=7d regime=bear-high-vol  lift=+0.012 [+0.004, +0.021]
          - sentiment_class=options pattern=term_structure_inversion cap=mid horizon=14d regime=bull-high-vol  lift=+0.008 [+0.002, +0.015]
          - sentiment_class=reddit pattern=mention_spike cap=small horizon=3d regime=bear-low-vol  lift=+0.007 [+0.001, +0.014]
        Demoted (lift not significant): 121

    EXIT 0 — regime conditioning earned its sparsity cost
    ```
  </action>
  <verify>
    <automated>npx vitest run src/lib/evaluation/__tests__/regime-done-gate.test.ts --reporter=dot &amp;&amp; test -f scripts/phase-22-status.ts &amp;&amp; grep -E 'phase-22-status' package.json</automated>
  </verify>
  <done>
    Unit tests GREEN. Script exists and is registered in `package.json`. Manual smoke run: `npm run phase-22-status` returns either exit 0 (promotion or accepted null finding) or exit 1 with a clear "soak not elapsed" / "no promotions without --allow-null-finding" message. Wave 0 stub now reports GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface `calibration.source_mix` in `engine-context.ts` (UI-SPEC Data Contract)</name>
  <files>src/lib/engine-context.ts</files>
  <behavior>
    Extend the EngineCalibration assembly path in `engine-context.ts` (per RESEARCH §J line 404) to populate `calibration.source_mix` per UI-SPEC §Data Contract:

    ```typescript
    interface SourceMix {
      regime: 'bull-low-vol' | 'bull-high-vol' | 'bear-low-vol' | 'bear-high-vol' | 'ALL';
      top_sources: Array<{
        source_id: 'stocktwits' | 'options_term_structure' | 'finsentllm_ensemble' | 'reddit'
                 | 'hackernews' | 'news_analyst' | 'quiver_insider' | 'quiver_congressional';
        weight: number;
        weight_unconditional: number;
        weight_drift_30d: number[];  // length 30, fractions in [0, 1]
        drift_direction: 'rising' | 'falling' | 'flat';
        delta_pp_30d: number;
        is_cold_start_fallback: boolean;
      }>;
    }
    ```

    Implementation:
    - Read the report's `regime` (from the relevant SentimentSnapshot row that drove the analysis).
    - Query `SourceTier` for the most recent rows WHERE `regime = ?` for each of the 8 sources.
    - For each source, query the 30 most recent SourceTier rows in this regime (sorted by `computed_at`) to build `weight_drift_30d`. If fewer than 30 rows exist, pad with the unconditional `'ALL'` weight (per UI-SPEC cold-start row treatment).
    - Compute `delta_pp_30d = (weight_drift_30d[29] - weight_drift_30d[0]) * 100`. `drift_direction = 'rising' if delta > 1, 'falling' if delta < -1, else 'flat'`.
    - Set `is_cold_start_fallback = true` for any source whose `(source, regime)` query returned no rows (so the UI must use `(source, 'ALL')` weights per D-09).
    - Sort `top_sources` DESCENDING by `weight`. UI assumes pre-sorted (UI-SPEC §Render Boundaries).

    The CLAUDE.md "Authoritative numerics from `engine-context.ts`" rule is enforced here — the UI component MUST NOT compute weights or drift; everything is pre-computed here.
  </behavior>
  <action>
    Edit `engine-context.ts`. Locate the EngineCalibration builder. Add the `source_mix` computation in a new helper `buildSourceMix(regime: RegimeLabel, asOf: Date)`.

    Per UI-SPEC §Data Contract: the array is sorted DESC by weight before returning. The component "TRUSTS the `drift_direction` field" — no re-computation.

    Per UI-SPEC §Empty / null states: when the regime classifier was cold-start (`regime='ALL'`), the `source_mix.regime` field is `'ALL'`. The 8 sources still produce rows (with the `'ALL'` weights). UI renders the `REGIME-UNCONDITIONAL` variant.

    Per UI-SPEC §Cold-start row: when `is_cold_start_fallback = true` for a particular source, the sparkline still renders as flat at the `'ALL'` weight (UI handles the visual treatment).

    Threading: the `regime` value to pass to `buildSourceMix` comes from the same `SentimentSnapshot.regime` that the aggregator used (Wave 3). If the report aggregated multiple regimes, pick the regime that drove the MAJORITY of the components — this is a single-line decision, document it in `engine-context.ts` header.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/lib/__tests__/engine-context.test.ts --reporter=dot 2&gt;&amp;1 | grep -v 'No test files found' || true</automated>
  </verify>
  <done>
    `engine-context.ts` exports a calibration object with `source_mix` populated for new reports. Type-check passes. UI components downstream can consume `calibration.source_mix` per the UI-SPEC contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement EngineCalibrationPanel "Source mix" row + client-island expand panel</name>
  <files>
    src/components/EngineCalibrationPanel.tsx,
    src/components/SourceMixExpanded.tsx,
    src/components/__tests__/EngineCalibrationPanel.test.tsx,
    tests/e2e/source-mix-row.spec.ts
  </files>
  <behavior>
    Per 22-UI-SPEC verbatim:

    - `EngineCalibrationPanel.tsx`: insert the new `<SourceMixRow source_mix={calibration.source_mix} />` JSX **between** the "Concept drift" row (L1004-1017) and `<AlignmentDisagreementBlocks>` (L1020). RSC — no `'use client'`.
    - `SourceMixRow` (inline in EngineCalibrationPanel.tsx, per UI-SPEC §Surface Inventory): always-visible row, structure per UI-SPEC §Layout & Interaction Contract:
      - `data-testid="source-mix-row"`
      - Tailwind: `mt-3 bg-surface-container-high p-3 rounded-lg flex items-center justify-between gap-3 flex-wrap`
      - Left cluster: SOURCE MIX eyebrow + regime pill + "·" separator + 3 source pills (`source-mix-pill-1/2/3`).
      - Right cluster: `<SourceMixExpanded sources={...} regime={...} />` (the disclosure button + expand panel live inside the client island).
      - Empty state per UI-SPEC §Empty / null states: when `source_mix` is undefined → render nothing (graceful back-compat); when `source_mix.top_sources.length === 0` → render `<span data-testid="source-mix-empty">No source-weight observations yet · awaiting first /api/cron/learn cycle in this regime</span>` and suppress the disclosure.
      - Cold-start regime='ALL': render the `REGIME-UNCONDITIONAL` pill variant (`data-testid="source-mix-cold-start-banner"` AND the regime pill carries both test IDs).
    - `SourceMixExpanded.tsx` (NEW file, client island per UI-SPEC §Render Boundaries):
      - `'use client'` at top.
      - Manages local `expanded` boolean state.
      - Renders the disclosure `<button data-testid="source-mix-disclosure" aria-expanded={expanded} aria-controls="source-mix-expanded">Show/Hide full ranking + expand_more/expand_less icon</button>`.
      - When `expanded`: renders the table per UI-SPEC §Expand panel structure (5 columns: Rank / Source / Weight / 30d Drift / Δ vs 30d).
      - Per-source sparkline = hand-rolled SVG, mirrors `Sparkline` at `EngineCalibrationPanel.tsx:233-250`. Stroke color per UI-SPEC §Drift sparkline colors (rising = `text-secondary`, falling = `text-error`, flat = `text-on-surface-variant`). Dashed `(source, 'ALL')` baseline.
      - Cold-start row per UI-SPEC §Cold-start row inside the expanded panel: sparkline = flat horizontal at `'ALL'` weight; delta column shows `regime-unconditional` italic.
      - Accessibility per UI-SPEC §Focus management: `aria-expanded`, `aria-controls`, `role="region"`, `aria-label`, `<title>` on every sparkline, tooltip on regime pill.
      - Print stylesheet per UI-SPEC §Print / PDF behavior: expand panel always visible when printing; disclosure button hidden.

    Tests (per UI-SPEC §Test Hooks):
    - RTL: with a populated `source_mix` fixture (regime='bear-high-vol', 5 sources), assert `source-mix-row`, `source-mix-regime-pill`, `source-mix-pill-1..3`, `source-mix-leading-star` render. Click `source-mix-disclosure` → assert `source-mix-expanded` is visible with `aria-expanded="true"`.
    - RTL: cold-start fixture (regime='ALL') → assert `source-mix-cold-start-banner` renders.
    - RTL: empty fixture (top_sources.length=0) → assert `source-mix-empty` renders and disclosure is absent.
    - RTL: undefined source_mix (legacy report) → assert NOTHING renders for this row (graceful back-compat).
    - Playwright (per CLAUDE.md mandatory testing): render a fixture for each of the 5 regimes; screenshot the collapsed row + the expanded panel; read the screenshots back via the Read tool to visually confirm the regime-color encoding renders correctly in both light + dark mode.
  </behavior>
  <action>
    Per CLAUDE.md global instructions: BEFORE writing this row, invoke `superpowers:brainstorming` + `ui-ux-pro-max` + `frontend-design:frontend-design` mentally — but the UI-SPEC has already fixed every visual decision (color, spacing, typography, copywriting) via the autonomous UI-phase researcher. Treat the UI-SPEC as the LOCKED contract; do not re-litigate visual choices.

    Per UI-SPEC §Anti-pattern checklist:
    - Do NOT introduce a chart library — hand-rolled SVG sparkline only.
    - Do NOT introduce a modal / drawer / dialog.
    - Do NOT push to the router on expand/collapse.
    - Do NOT use any color outside the Material-3 semantic tokens.
    - Do NOT render at the top of the panel — the row sits between Concept Drift and AlignmentDisagreementBlocks.
    - Do NOT increase panel scroll height by more than ~80px in collapsed state.
    - Do NOT compute regime labels, weights, or drift values in the component (engine-context.ts is the boundary).

    Per UI-SPEC §Copywriting Contract: use the verbatim copy. `BULL · LOW-VOL`, `REGIME-UNCONDITIONAL`, `Show full ranking`, `Hide full ranking`, etc. No abbreviation, no localization.

    Per CLAUDE.md global instructions §Testing: write Playwright tests + read the screenshots back via the Read tool. Iterate until tests pass AND screenshots visually confirm correctness.
  </action>
  <verify>
    <automated>npx vitest run src/components/__tests__/EngineCalibrationPanel.test.tsx --reporter=dot &amp;&amp; npx playwright test tests/e2e/source-mix-row.spec.ts --reporter=dot</automated>
  </verify>
  <done>
    All RTL test hooks resolve (data-testids match UI-SPEC §Test Hooks). Playwright suite GREEN across all 5 regime fixtures, light + dark mode. Screenshots read back via Read tool show the regime-color encoding correctly (calm teal / saturated teal / calm rose / saturated rose / neutral grey). Old reports without `source_mix` render the panel unchanged (regression-free per UI-SPEC §Empty states). Existing `EngineCalibrationPanel.test.tsx` tests still pass (no regression).
  </done>
</task>

<task type="auto">
  <name>Task 4: Update Prisma schema for Migration 2 (unique-constraint flip) — files only, no push yet</name>
  <files>prisma/schema.prisma</files>
  <behavior>
    Per D-11 step 4 + RESEARCH §A migration sequencing:
    - `LearnedPattern`: flip `@@unique([signal_class, pattern_key, cap_class, horizon_days])` to `@@unique([signal_class, pattern_key, cap_class, horizon_days, regime])`.
    - `PerSourceIC`: flip the existing `@@unique` to include `regime`.
    - `SourceTier`: flip the existing `@@index([source_id, computed_at(sort: Desc)])` to include `regime` as the secondary key (the regime-only index from Wave 0 stays as-is).

    Add a `// PHASE 22 Wave 5 (D-11 step 4) — IRREVERSIBLE cutover` comment above each flipped constraint for traceability.

    DO NOT run `prisma db push` in this task — that is the next [BLOCKING] task.
  </behavior>
  <action>
    Edit `prisma/schema.prisma` at the 3 named constraint lines. Run `npx prisma format` and `npx prisma validate`.

    Per R5 (irreversibility): the operator-confirmed checkpoint in Task 5 below is the gate. If the operator decides to abort (e.g., done-gate failed AND user does not accept the null finding), this task's diff can simply be reverted from git — the schema stays additive-only and the soak just continues.
  </action>
  <verify>
    <automated>npx prisma format &amp;&amp; npx prisma validate &amp;&amp; grep -E '@@unique\(\[.*regime\]\)' prisma/schema.prisma | wc -l | grep -E '^\s*[2-9]'</automated>
  </verify>
  <done>
    `npx prisma validate` exits 0. `grep` shows ≥2 `@@unique([..., regime])` constraints. Comments mark each as IRREVERSIBLE.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 [BLOCKING]: Operator runs `npx prisma db push` for Migration 2 (irreversible cutover)</name>
  <what-built>
    Migration 2 (Task 4) is committed but not yet pushed. Per D-11 step 5 + R5: this is the IRREVERSIBLE cutover. Once flipped, rolling back requires consolidating multiple per-regime rows into a single unconditional row — destructive and complex.

    The operator should run this ONLY IF:
    (a) The 14-day soak window has elapsed (Task 1 `npm run phase-22-status` confirms).
    (b) Either the done-gate passes (≥1 cell promoted) OR the operator accepts the "0 ACTIVE cells, IS-paper valid null finding" outcome (D-16).

    If the operator declines, this task is SKIPPED. The phase still ships — the additive schema + UI surface + methodology paper section are all valid deliverables. The unique-constraint flip can be deferred to a future phase (or never).
  </what-built>
  <how-to-verify>
    1. Run `npm run phase-22-status` — confirm exit 0 (either promotion or accepted null finding).
    2. Confirm `DATABASE_URL` in `.env.local` points at production Neon (per `[[feedback_env_parity]]`).
    3. Run `npx prisma db push` and confirm output reports "Your database is now in sync with your Prisma schema."
    4. Spot-check the constraint flip via psql:
       ```sql
       SELECT conname FROM pg_constraint WHERE conrelid = '"LearnedPattern"'::regclass AND contype = 'u';
       ```
       Expected: the unique constraint name includes the new column set (or you see a NEW constraint name and the OLD one is gone).
    5. Confirm no data loss: `SELECT COUNT(*) FROM "LearnedPattern";` matches the pre-migration row count.
    6. Smoke test the live UI: open `/research/AAPL` and confirm the EngineCalibrationPanel renders the new "Source mix" row.
  </how-to-verify>
  <resume-signal>Type "flipped" once `prisma db push` reports success AND no data loss AND the live UI smoke renders the source-mix row. Type "skip" if declining the cutover.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: ROADMAP + REQUIREMENTS bookkeeping + methodology paper section</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    docs/paper/methodology.md
  </files>
  <behavior>
    Per D-02 + CLAUDE.md Statistical-Methods Reference rule + RESEARCH §L:

    1. `.planning/ROADMAP.md` "### Phase 22:" section: update "2-bucket regime" → "4-bucket regime (bull/bear × low-vol/high-vol)" so REQUIREMENTS CORE-ML-07 ↔ ROADMAP are consistent. Mark Phase 22 as COMPLETE with the cutover date.
    2. `.planning/REQUIREMENTS.md` CORE-ML-06..10 + CORE-ML-20..22: mark as `[shipped 2026-XX-XX, Phase 22]`. Note: CORE-ML-20..22's source-mix UI surface is genuinely new in P22 (per phase_requirements input).
    3. `docs/paper/methodology.md`: append a Phase 22 section. Cite per CLAUDE.md Statistical-Methods Reference rule:
       - Hamilton 1989 (Markov-switching regimes) — D-02 grounding.
       - Ang & Bekaert 2002 (international equity regimes) — D-02 grounding.
       - Benjamini & Bogomolov 2014 (hierarchical FDR) — D-15 implementation.
       - Efron 1987 (BCa bootstrap) — D-14 done-gate reused from P21.1.
       - CS229 "Bias-Variance and Regularization" — D-07 EB shrinkage justification (CLAUDE.md rule #4 application).
       - ISL Ch. 13 "Multiple Testing" — D-15 multi-testing correction grounding.
       Document methodology choices honestly: state whether the done-gate passed or whether the IS paper reports a null finding (D-16).
  </behavior>
  <action>
    Edit each file. For `docs/paper/methodology.md`, append (do not overwrite) a Phase 22 section structured per the established methodology-paper conventions. Cross-reference CLAUDE.md Statistical-Methods Reference table.

    For the IS-paper framing per `[[is-symposium-framing-summer-2026]]`: this section is one of the depth-over-features deliverables. State the result honestly — "We tested 4 buckets; N cells cleared all 5 gates; M produced statistically significant Brier-lift over unconditional baseline" is a valid finding whether N and M are 0 or 50.
  </action>
  <verify>
    <automated>grep -E '4-bucket regime|bull/bear × low-vol/high-vol' .planning/ROADMAP.md &amp;&amp; grep -E 'shipped.*Phase 22' .planning/REQUIREMENTS.md &amp;&amp; grep -E 'Phase 22' docs/paper/methodology.md</automated>
  </verify>
  <done>
    ROADMAP says "4-bucket regime"; REQUIREMENTS marks CORE-ML-06..10 + 20..22 as shipped; methodology paper appends a Phase 22 section citing the 6 references above.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Neon (Migration 2 push) | The IRREVERSIBLE cutover. Single elevation point — Task 5 is operator-gated. |
| `engine-context.ts` → EngineCalibrationPanel (props) | Server-computed numerics flow to the RSC. UI never computes. |
| `SourceMixExpanded.tsx` (client island) | Local state only; no API calls; no external data fetches; no router pushes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Denial of Service | Migration 2 race during soak (cron tries to write to OLD constraint mid-flip) | mitigate | Phased cutover D-11: Migration 2 is operator-gated (Task 5 BLOCKING checkpoint). The cron is idempotent — re-runs after the flip pick up the new constraint. Single-instance cron via Vercel's scheduler prevents concurrent execution. |
| T-22-05-02 | Tampering | UI computes weights or regime label client-side (CLAUDE.md authoritative-numerics violation) | mitigate | UI-SPEC §Cross-cutting compliance — "The component does zero math." Code review on Task 3 enforces; CI lint can grep for arithmetic operators in the JSX file if needed. |
| T-22-05-03 | Information Disclosure | Sparkline accidentally leaks PII via title attribute | accept | Sparkline `<title>` body contains source labels + percentage points only — public data, no PII. |
| T-22-05-04 | Tampering | Methodology paper cites incorrect references | mitigate | CLAUDE.md Statistical-Methods Reference table is the source-of-truth list. Cross-checked in Task 6. |
| T-22-05-05 | Repudiation | Done-gate result not preserved for audit | mitigate | `scripts/phase-22-status.ts` output is captured in the SUMMARY.md; the script can be re-run to reproduce the gate decision. |
| T-22-05-06 | Elevation of Privilege | Operator runs Migration 2 against wrong DB | mitigate | Task 5 `[BLOCKING]` checkpoint Step 2 verifies `DATABASE_URL` scope before push. |
</threat_model>

<verification>
End of Wave 5 (phase complete):
1. `npm run phase-22-status` exits 0 (promotion OR `--allow-null-finding` accepted).
2. Migration 2 either applied (operator typed "flipped") OR explicitly skipped (operator typed "skip" — phase still complete per D-16 honest-reporting principle).
3. `npx vitest run --reporter=dot` exits 0 across the full unit suite.
4. `npx playwright test tests/e2e/source-mix-row.spec.ts --reporter=dot` exits 0 across all 5 regime fixtures.
5. `npx tsc --noEmit` exits 0.
6. Live smoke: `/research/AAPL` renders the "Source mix" row above the alignment/disagreement blocks.
7. ROADMAP + REQUIREMENTS + methodology paper bookkeeping landed.
8. `npm run check-feature-asof` exits 0.
9. CI guard `no-hand-curated-tier-weights` exits 0.
</verification>

<success_criteria>
- `regimeDoneGate` + `scripts/phase-22-status.ts` ship and report honestly per D-14 + D-16.
- Migration 2 either applies the unique-constraint flip cleanly OR is explicitly skipped (D-11 step 4 honored).
- EngineCalibrationPanel renders the "Source mix" row per the locked UI-SPEC (CORE-ML-22).
- `engine-context.ts` is the authoritative numerics boundary — UI component does zero math.
- ROADMAP D-02 conflict resolved; REQUIREMENTS CORE-ML-06..10 + CORE-ML-20..22 marked shipped.
- Methodology paper appends a Phase 22 section citing the 6 statistical-methods references.
- All P21.1 baselines + tests still green (no regression).
- Phase 22 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/22-market-regime-and-source-weights/22-05-SUMMARY.md` documenting:
- Done-gate outcome — promoted cell list with Brier-lift CIs, OR honest null finding per D-16
- Whether Migration 2 was applied (operator decision)
- Live deployment URL + screenshot of the "Source mix" row in each of the 5 regime variants
- ROADMAP / REQUIREMENTS / methodology paper update commit hashes
- Phase 22 retrospective notes feeding into `.planning/RETROSPECTIVE.md`
- IS-paper hand-off: which Phase 22 results (promoted-cell list, hierarchical FDR table, EB shrinkage strengths) feed which IS-paper chapter
</output>

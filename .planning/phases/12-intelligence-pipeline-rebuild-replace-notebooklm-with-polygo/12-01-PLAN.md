---
phase: 12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/types.ts
  - tests/unit/analysis-schema.test.ts
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02
  - INTEL-03

must_haves:
  truths:
    - "`ai` and `@mendable/firecrawl-js` are in package.json dependencies"
    - "`AnalysisResult.bullish_signals` and `bearish_signals` are typed as 1-5 items (not fixed 3)"
    - "`AnalysisResult.price_target` exists as `string | null | undefined` (optional, backward-compatible)"
    - "Unit tests for the new schema fields exist and pass with `npm test`"
    - "`StoredReport` structure is unchanged — existing persisted reports can be deserialized"
  artifacts:
    - path: "package.json"
      provides: "ai@6.0.168 and @mendable/firecrawl-js@4.18.3 in dependencies"
      contains: "\"ai\":"
    - path: "src/lib/types.ts"
      provides: "AnalysisResult with price_target optional field"
      contains: "price_target"
    - path: "tests/unit/analysis-schema.test.ts"
      provides: "Wave 0 test stubs for schema evolution"
      min_lines: 30
  key_links:
    - from: "src/lib/types.ts"
      to: "src/app/api/analysis/[ticker]/route.ts"
      via: "AnalysisResult import"
      pattern: "import.*AnalysisResult.*from"
---

<objective>
Install new dependencies and evolve the AnalysisResult type contract to support Phase 12 requirements.

Purpose: The AI SDK (`ai`) package is not yet installed — it blocks all subsequent compilation. The AnalysisResult schema must be extended with `price_target` (D-10) and the signal arrays must be updated to support 1-5 signals instead of exactly 3 (D-09) before the analysis route is rewritten. Test stubs lock the contract before implementation.

Output: `ai` and `@mendable/firecrawl-js` installed, `src/lib/types.ts` updated with backward-compatible additions, `tests/unit/analysis-schema.test.ts` created with schema expectation stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-CONTEXT.md
@.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-RESEARCH.md

<interfaces>
<!-- Current AnalysisResult (from src/lib/types.ts) — read before modifying -->
export interface AnalysisResult {
  ticker: string;
  company_name: string;
  analyzed_at: string;         // ISO 8601
  market_sentiment: 'bullish' | 'neutral' | 'bearish';
  sentiment_reasoning: string;
  bullish_signals: AnalysisSignal[];   // comment says "exactly 3" — changing to 1-5
  bearish_signals: AnalysisSignal[];   // comment says "exactly 3" — changing to 1-5
  assessment: BuySellBreakdown;
  confidence_level: 'Low' | 'Medium' | 'High';
  confidence_explanation: string;
  sources_used: AnalysisSource[];
  source_warnings: string[];
  market_snapshot?: MarketSnapshot;    // optional
  security_type?: SecurityType;        // optional
  // ADD: price_target?: string | null   ← per D-10
}

export interface AnalysisSignal {
  signal: string;
  source_citation: string;
}

export interface AnalysisSource {
  name: string;
  key_fact: string;
}

export interface StoredReport {
  ticker: string;
  company_name: string;
  analyzed_at: string;
  market_sentiment: 'bullish' | 'neutral' | 'bearish';
  confidence_level: 'Low' | 'Medium' | 'High';
  analysis: AnalysisResult;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK dependencies</name>
  <read_first>
    - /Users/tj/Desktop/Cipher/package.json (current dependencies before modification)
  </read_first>
  <files>package.json</files>
  <action>
Run the following install command from the project root:

```
npm install ai@6.0.168 @mendable/firecrawl-js@4.18.3
```

This installs:
- `ai@6.0.168` — Vercel AI SDK providing `generateText`, `Output`, `NoObjectGeneratedError`. Auth via `VERCEL_OIDC_TOKEN` (already in `.env.local`). No provider-specific package needed — plain model strings route through the Vercel AI Gateway automatically.
- `@mendable/firecrawl-js@4.18.3` — Firecrawl TypeScript SDK for scraping community sentiment pages to markdown.

After install, also remove the `prestart` hook from `package.json` scripts because `scripts/setup.sh` checks for Python/NotebookLM prerequisites that no longer exist after Phase 12. Edit `package.json` scripts to:
- Remove the `"prestart"` key entirely (the `npm start` script `"next build && next start"` remains unchanged)
- Keep all other scripts unchanged: `dev`, `build`, `postinstall`, `start`, `lint`, `test`, `test:watch`, `test:e2e`, `test:e2e:debug`, `debug`, `debug:auth`, `debug:signin`, `debug:setup`, `debug:pipeline`, `debug:record`

Verify install succeeded:
```
npm ls ai
npm ls @mendable/firecrawl-js
```
  </action>
  <verify>
    <automated>npm ls ai && npm ls @mendable/firecrawl-js</automated>
  </verify>
  <done>
    - `package.json` contains `"ai": "6.0.168"` in dependencies
    - `package.json` contains `"@mendable/firecrawl-js": "4.18.3"` in dependencies
    - `"prestart"` key is absent from scripts
    - `npm ls ai` exits 0 with the version shown
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evolve AnalysisResult schema + write test stubs</name>
  <read_first>
    - /Users/tj/Desktop/Cipher/src/lib/types.ts (full current file — read before modifying)
  </read_first>
  <files>src/lib/types.ts, tests/unit/analysis-schema.test.ts</files>
  <behavior>
    - Test 1: AnalysisResult with 5 bullish_signals and 5 bearish_signals is valid
    - Test 2: AnalysisResult with 1 bullish_signal and 1 bearish_signal is valid (not exactly 3)
    - Test 3: AnalysisResult without `price_target` field is valid (backward-compat)
    - Test 4: AnalysisResult with `price_target: "$185-$200"` is valid
    - Test 5: AnalysisResult with `price_target: null` is valid
    - Test 6: StoredReport wrapping an AnalysisResult with new fields is serializable to/from JSON without data loss
  </behavior>
  <action>
**Step A — Update `src/lib/types.ts`:**

Make these targeted changes (leave all other fields, interfaces, and comments intact):

1. In `AnalysisResult`, update the comment on `bullish_signals` from `// exactly 3` to `// 1-5 signals` and update `bearish_signals` similarly.

2. Add `price_target` as an optional field after `confidence_explanation`:
   ```typescript
   price_target?: string | null;  // analyst-consensus price target or range — optional for backward compat (D-10)
   ```

3. Add `community_sentiment_available?: boolean` after `source_warnings` — optional boolean flag indicating whether Firecrawl community content was included in this analysis run (useful for debugging and display). Default is `undefined` (treated as false). This is at Claude's discretion per D-11.

4. The `AnalysisSource` interface already has `name` and `key_fact`. Add an optional `url?: string` field for richer source attribution (D-11 — Claude's discretion):
   ```typescript
   export interface AnalysisSource {
     name: string;
     key_fact: string;
     url?: string;  // optional source URL for direct attribution (D-11)
   }
   ```

5. Update the section comment above `AnalysisResult` from `// ---- AnalysisResult types (Phase 2 — NotebookLM research output) ----` to `// ---- AnalysisResult types (Phase 12 — Gemini direct intelligence pipeline) ----`

**Step B — Create `tests/unit/analysis-schema.test.ts`:**

Create the test file with vitest imports. Write tests using plain object construction (no Zod in this file — just TypeScript type satisfaction and runtime checks).

The test file should:
- Import `AnalysisResult`, `StoredReport`, `AnalysisSignal`, `AnalysisSource` from `'@/lib/types'`
- Use a `makeSignal(n: number): AnalysisSignal` helper that returns `{ signal: 'Signal ' + n, source_citation: 'Source ' + n }`
- Use a `makeSource(): AnalysisSource` helper returning `{ name: 'Test Source', key_fact: 'Key fact' }`
- Use a `makeBase(): Omit<AnalysisResult, 'bullish_signals' | 'bearish_signals'>` helper for the non-signal fields
- Test 1: `bullish_signals: [makeSignal(1), ...(x5)]` assigned to typed `AnalysisResult` — call `expect(result.bullish_signals).toHaveLength(5)`
- Test 2: `bullish_signals: [makeSignal(1)]` (length 1) — `expect(result.bullish_signals).toHaveLength(1)`
- Test 3: spread result without `price_target` — `expect('price_target' in result).toBe(false)` or `expect(result.price_target).toBeUndefined()`
- Test 4: `price_target: '$185-$200'` — `expect(result.price_target).toBe('$185-$200')`
- Test 5: `price_target: null` — `expect(result.price_target).toBeNull()`
- Test 6: `JSON.parse(JSON.stringify(storedReport))` round-trip — `expect(parsed.analysis.price_target).toBe('$190')`
  </action>
  <verify>
    <automated>npx vitest run tests/unit/analysis-schema.test.ts</automated>
  </verify>
  <done>
    - `src/lib/types.ts` contains `price_target?: string | null;` in AnalysisResult
    - `src/lib/types.ts` contains `url?: string;` in AnalysisSource
    - `src/lib/types.ts` contains `community_sentiment_available?: boolean;` in AnalysisResult
    - Signal array comments updated from "exactly 3" to "1-5 signals"
    - `tests/unit/analysis-schema.test.ts` exists with 6 tests
    - `npx vitest run tests/unit/analysis-schema.test.ts` exits 0 (all 6 tests pass)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → project | Third-party packages introduced into build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | `@mendable/firecrawl-js` supply chain | mitigate | Pin to exact version `4.18.3`; verify `npm ls` post-install; no `^` or `~` prefix |
| T-12-01-02 | Tampering | `ai` SDK supply chain | mitigate | Pin to exact version `6.0.168`; verify `npm ls` post-install |
| T-12-01-03 | Information Disclosure | `price_target` optional field | accept | Field is analysis output only — not user PII; low-value target |
</threat_model>

<verification>
After both tasks complete:

1. `npm ls ai` shows `ai@6.0.168`
2. `npm ls @mendable/firecrawl-js` shows `@mendable/firecrawl-js@4.18.3`
3. `grep "price_target" src/lib/types.ts` returns the optional field line
4. `grep "prestart" package.json` returns nothing (key removed)
5. `npx vitest run tests/unit/analysis-schema.test.ts` exits 0
6. `npm run build` exits 0 (no type errors from schema changes — existing code still satisfies the interface since new fields are optional)
</verification>

<success_criteria>
- `ai` and `@mendable/firecrawl-js` are installed and in `package.json`
- `AnalysisResult` has `price_target?: string | null` (optional, backward-compatible)
- `AnalysisResult` has `community_sentiment_available?: boolean` (optional)
- `AnalysisSource` has `url?: string` (optional)
- Signal array comments updated from "exactly 3" to "1-5 signals"
- `prestart` hook removed from `package.json`
- 6 schema tests pass with `npx vitest run tests/unit/analysis-schema.test.ts`
- `npm run build` exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-01-SUMMARY.md`
</output>

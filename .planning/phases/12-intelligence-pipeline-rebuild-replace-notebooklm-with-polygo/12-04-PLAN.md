---
phase: 12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo
plan: 04
type: execute
wave: 4
depends_on:
  - "12-03"
files_modified:
  - .env.local.example
  - .planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-VALIDATION.md
autonomous: false
requirements:
  - INTEL-10

must_haves:
  truths:
    - "`npm run build` exits 0 with no TypeScript or Next.js errors"
    - "`npm test` exits 0 — all unit tests green"
    - "`.env.local.example` documents FIRECRAWL_API_KEY and removes CONTAINER_* vars"
    - "A real ticker research run (AAPL or similar) produces a report with 5 bullish and 5 bearish signals"
    - "The ResearchProgress stepper advances through all steps during the real run"
  artifacts:
    - path: ".env.local.example"
      provides: "Updated env var documentation with FIRECRAWL_API_KEY, no CONTAINER_* vars"
      contains: "FIRECRAWL_API_KEY"
  key_links:
    - from: ".env.local.example"
      to: "src/lib/gemini-analysis.ts"
      via: "FIRECRAWL_API_KEY env var"
      pattern: "FIRECRAWL_API_KEY"
---

<objective>
Final validation gate: verify the full build is clean, all tests pass, env var documentation is updated, and a real end-to-end ticker research run produces a correct report. Human checkpoint confirms visual progress stepper and report quality.

Purpose: Phase 12 is a significant rewrite — a full integration test against live APIs is required before considering the phase complete. The build check and test suite catch regressions; the human checkpoint catches UX and output quality issues that automated tests cannot.

Output: Updated `.env.local.example`, clean build + test suite, human-verified end-to-end run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-CONTEXT.md
@.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and env var documentation update</name>
  <read_first>
    - /Users/tj/Desktop/Cipher/.env.local.example (full current file — read before modifying)
    - /Users/tj/Desktop/Cipher/package.json (verify prestart is removed)
  </read_first>
  <files>.env.local.example</files>
  <action>
**Step A — Run full build and test suite:**

```bash
cd /Users/tj/Desktop/Cipher && npm run build 2>&1
```
If build fails, read the error output and fix any TypeScript or import errors before proceeding. Common failures at this stage:
- Import of a deleted file (check that no remaining TS file imports from deleted scripts or routes)
- Missing type from `src/lib/types.ts` changes (check that AnalysisResult new optional fields don't break existing callers)
- `ai` module type errors (check `node_modules/ai/dist/index.d.ts` for correct export names)

```bash
cd /Users/tj/Desktop/Cipher && npm test 2>&1
```
If any tests fail, read the failure output and fix. Common failures:
- Old container proxy tests still present in `route.test.ts` (should have been cleaned in Plan 02)
- Missing mock for new `scrapeCommunitySentiment` function

**Step B — Update `.env.local.example`:**

Read the current `.env.local.example` file first. Then make these targeted changes:
1. Remove the entire CONTAINER block: `CONTAINER_URL`, `CONTAINER_SECRET`, `CONTAINER_VNC_URL` — these env vars no longer exist after Phase 12
2. Add a new section for Phase 12 additions:
   ```
   # Phase 12: Intelligence Pipeline (Gemini via Vercel AI Gateway)
   # VERCEL_OIDC_TOKEN is written automatically by `vercel env pull` for local dev.
   # On Vercel deployments it is auto-injected by the runtime — no dashboard config needed.
   # VERCEL_OIDC_TOKEN=<written by vercel env pull>

   # Firecrawl — optional. When present, community discussion pages are scraped for sentiment context.
   # Get your API key at: https://firecrawl.dev
   # If absent, analysis proceeds without community sentiment (graceful skip).
   # FIRECRAWL_API_KEY=fc-...
   ```
3. Leave all other env vars unchanged: `ANTHROPIC_API_KEY`, `DATABASE_URL`, `DIRECT_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXT_PUBLIC_DEPLOYMENT_MODE`, `DEPLOYMENT_MODE`, `CREDENTIAL_ENCRYPTION_KEY` (keep for now — table still exists in Neon)

**Step C — Run final build verification to confirm .env.local.example change didn't introduce issues:**
```bash
cd /Users/tj/Desktop/Cipher && npm run build 2>&1 | tail -10
```
  </action>
  <verify>
    <automated>cd /Users/tj/Desktop/Cipher && npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm test` exits 0 (all unit tests pass)
    - `npm run build` exits 0 (no TypeScript errors, no Next.js build errors)
    - `.env.local.example` contains `FIRECRAWL_API_KEY` comment
    - `.env.local.example` does NOT contain `CONTAINER_URL` or `CONTAINER_SECRET`
    - `grep "CONTAINER_URL" .env.local.example` returns nothing
    - `grep "FIRECRAWL_API_KEY" .env.local.example` returns the documented line
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test — verify Gemini pipeline produces correct report</name>
  <what-built>
    Phase 12 complete implementation:
    - AI SDK + Firecrawl installed
    - AnalysisResult schema evolved with price_target, 5-signal support, improved attribution
    - Analysis route rewritten to call Gemini via Vercel AI Gateway (no Python, no subprocess)
    - SSE progress events emitted from TypeScript with correct substrings for stepper
    - Container infrastructure decommissioned (Python scripts, Dockerfiles, VNC routes deleted)
    - setup/status route simplified to session-only
    - .env.local.example updated
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (or `npm start` if you prefer the production build)

    2. Open http://localhost:3000 in your browser

    3. Enter a ticker symbol (AAPL, NVDA, or TSLA recommended — good data coverage)

    4. Click the chart to confirm the ticker, then click "Run Research"

    5. Watch the ResearchProgress stepper — verify ALL steps advance in sequence:
       - Step 0: "Collecting market data" (triggered by 'creating' message)
       - Step 1: "Gathering news & filings" (triggered by 'adding market' message)
       - Step 2: Next step advances (triggered by 'adding news' message)
       - Step 3: "Synthesizing intelligence" (triggered by 'querying sentiment' message)
       - Step 4: "Generating report" (triggered by 'querying confidence' message)
       - Step 5: Final step (triggered by 'cleaning' message)

       If any step sticks and does not advance, check the browser console for SSE message text — the message must contain the exact substring listed above (case-insensitive).

    6. After analysis completes, verify the research report contains:
       - [ ] Exactly 5 bullish signals (not 3)
       - [ ] Exactly 5 bearish signals (not 3)
       - [ ] A Buy/Hold/Sell assessment with percentages summing to 100%
       - [ ] A confidence level (Low/Medium/High)
       - [ ] A Sources section with citations
       - [ ] Optional: a price target if analyst data was available

    7. Verify NO error appears in the browser (no red error state, no "Analysis failed" message)

    8. Check browser DevTools → Network → the `/api/analysis/[ticker]` request:
       - Response Content-Type should be `text/event-stream`
       - Each SSE event should be valid JSON with `type` field

    9. Check the server terminal (where `npm run dev` is running):
       - Should see NO references to "spawn", "python3", or "notebooklm_research"
       - Should see no CONTAINER_URL errors

    10. Optional (if FIRECRAWL_API_KEY is set in .env.local): verify report includes community sentiment context by checking if the `community_sentiment_available` field appears in the raw result (visible in DevTools Network response)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the stepper advances through all steps and the report shows 5 bullish + 5 bearish signals.
    Or describe any issues observed (stuck stepper step, error message, wrong signal count, etc.).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local.example → developer docs | Documentation file only — no secrets committed |
| Live API call → Vercel AI Gateway | OIDC token auth; real Gemini call during smoke test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | `.env.local.example` documents secret variable names | accept | `.env.local.example` contains no actual values — only placeholder comments; this is standard practice for OSS projects |
| T-12-04-02 | Denial of Service | Smoke test consumes Vercel AI Gateway credits | accept | Single test call; `google/gemini-3-flash` is low-cost flash model; acceptable for validation |
</threat_model>

<verification>
Phase 12 complete when all are true:

1. `npm run build` exits 0 — no TypeScript or Next.js errors
2. `npm test` exits 0 — all unit tests pass
3. `.env.local.example` has `FIRECRAWL_API_KEY` documented, no `CONTAINER_*` vars
4. Human checkpoint approved: stepper advances, 5 bullish + 5 bearish signals, no errors
5. `grep "CONTAINER_URL" src/ -r --include="*.ts"` returns nothing
6. `grep "notebooklm_research" src/ -r --include="*.ts"` returns nothing
7. `ls scripts/notebooklm_research.py 2>/dev/null` returns nothing
</verification>

<success_criteria>
- Build and full test suite green
- `.env.local.example` updated (no CONTAINER_*, has FIRECRAWL_API_KEY)
- Human-verified end-to-end run: Gemini returns analysis with 5+5 signals, stepper advances all steps
- No dead container code in src/
</success_criteria>

<output>
After completion, create `.planning/phases/12-intelligence-pipeline-rebuild-replace-notebooklm-with-polygo/12-04-SUMMARY.md`
</output>

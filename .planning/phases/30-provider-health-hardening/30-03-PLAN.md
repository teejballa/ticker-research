---
phase: 30
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/lib/data/yahoo.ts
  - src/lib/data/polygon.ts
  - src/lib/data/finnhub.ts
  - src/lib/data/anthropic-search.ts
  - src/lib/data/lightweight-community-scan.ts
  - src/lib/data/cache/cache-keys.ts
  - src/lib/data/merge.ts
  - src/lib/data/source-package.ts
  - src/lib/data/__tests__/merge.test.ts
  - src/components/ResearchReport.tsx
  - src/lib/research-brief.ts
  - src/lib/gemini-analysis.ts
  - src/lib/sentiment/per-doc-classifier.ts
  - src/lib/telemetry/withTelemetry.ts
  - tests/unit/source-package.fallback.unit.test.ts
  - tests/unit/gemini-analysis.model-pin.unit.test.ts
  - tests/integration/cost-anomaly-breaker.integration.test.ts
  - tests/integration/lightweight-community-scan.breaker.integration.test.ts
autonomous: true
requirements_addressed:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-11
  - D-14
  - D-15
  - D-23
must_haves:
  truths:
    - "Every adapter in src/lib/data/*.ts wraps external calls with withBreaker between withTelemetry and withRetry"
    - "merge.ts cascade for Polygon+Finnhub-covered fields goes polygon then finnhub then yahoo (Yahoo demoted)"
    - "SourcePackage carries a fallback_summary array per D-09"
    - "Yahoo quote cache TTL is 60 seconds (down from 300)"
    - "Yahoo fundamentals cache TTL is 86400 (unchanged) and explicitly documented"
    - "All generateObject and generateText call sites have explicit model pinned per D-14 using 3-tier slugs (not 2.5)"
    - "withTelemetry post-success path increments cost_anomaly:gemini Upstash counter when cost_usd > 1.00 and trips a 1h breaker at count >= 3"
    - "lightweight-community-scan wraps Firecrawl calls in withBreaker so a Firecrawl outage does not fail the cron"
  artifacts:
    - path: "src/lib/data/merge.ts"
      provides: "Reversed cascade (polygon then finnhub then yahoo) for shared fields; fallback_summary tracking; unavailable emission"
      contains: "fallback_summary"
    - path: "src/lib/data/source-package.ts"
      provides: "SourcePackage.fallback_summary populated by buildSourcePackageOldLadder and buildSourcePackageNewLadder"
      contains: "fallback_summary"
    - path: "src/lib/data/cache/cache-keys.ts"
      provides: "TTL_SECONDS.quote = 60"
      contains: "quote: 60"
    - path: "src/lib/gemini-analysis.ts"
      provides: "Hard-pinned model: google/gemini-3-pro for main analysis call"
      contains: "google/gemini-3-pro"
    - path: "src/lib/telemetry/withTelemetry.ts"
      provides: "Post-success cost-anomaly check for gemini greater than $1.00"
      contains: "cost_anomaly:gemini"
  key_links:
    - from: "src/lib/data/yahoo.ts"
      to: "src/lib/data/circuit-breaker.ts"
      via: "withBreaker yahoo then withRetry"
      pattern: "withBreaker.*yahoo"
    - from: "src/lib/data/lightweight-community-scan.ts"
      to: "src/lib/data/circuit-breaker.ts"
      via: "withBreaker firecrawl then withRetry"
      pattern: "withBreaker.*firecrawl"
    - from: "src/lib/data/merge.ts"
      to: "src/lib/types.ts"
      via: "FallbackSummaryEntry import"
      pattern: "FallbackSummaryEntry"
    - from: "src/lib/telemetry/withTelemetry.ts"
      to: "src/lib/data/cache/upstash.ts"
      via: "getRedis to INCR cost_anomaly:gemini"
      pattern: "cost_anomaly:gemini"
---

<objective>
Wire the Wave-1 primitives into every external call site, demote Yahoo in the merge cascade, lock Gemini model slugs, and install the cost-anomaly trip line.

Purpose: After this plan, the entire `src/lib/data/` adapter surface is breaker-protected, the merge ladder prioritizes Polygon over Yahoo on shared fields, SourcePackage emits the `fallback_summary` plumbing telemetry, and Gemini cost cannot silently blow past the $1.00/call threshold three times in an hour without tripping.

Output: 6 adapter files wrapped, 1 merge order reversed, 2 cache values tuned, 2 Gemini call sites pinned, 1 cost-anomaly check installed. All Plan-01 RED stubs for D-09, D-14, D-15, D-23 turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-provider-health-hardening/30-CONTEXT.md
@.planning/phases/30-provider-health-hardening/30-RESEARCH.md
@.planning/phases/30-provider-health-hardening/30-02-SUMMARY.md

@src/lib/data/circuit-breaker.ts
@src/lib/data/retry.ts
@src/lib/telemetry/withTelemetry.ts
@src/lib/data/merge.ts
@src/lib/data/source-package.ts
@src/lib/types.ts
@src/lib/gemini-analysis.ts
</context>

<interfaces>
Primitives from Plan 02 this plan composes:

- `withBreaker(provider_id, fn, overrides?)` — composes between withTelemetry and withRetry
- `BreakerOpenError` — non-retryable, has `provider_id` and `opened_at`
- `FallbackSummaryEntry { field, tried: ProviderId[], resolved_by: ProviderId | 'unavailable' }`
- `FieldOrigin` now includes `'unavailable'` (null preserved)

Canonical composition order (LOAD-BEARING, CONTEXT.md D-07, research R-1):
External call sites become `withTelemetry(provider, () => withBreaker(provider, () => withRetry(() => fn())))`.

```typescript
// From src/lib/telemetry/withTelemetry.ts (canonical signature — DO NOT rename the
// parameter; downstream tasks reference `provider_id` exactly as below):
export async function withTelemetry<T>(
  provider_id: ProviderId,
  fn: () => Promise<T>,
  opts?: { ticker?: string },
): Promise<T>;

// `provider_id` (snake_case) is the parameter name on the public signature.
// Inside the function body, the post-success cost-anomaly block (Task 3) reads
// `provider_id === 'gemini'` and `cost_usd` from the already-computed local —
// no rename, no alias.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap data adapters with withBreaker and tighten Yahoo quote TTL</name>
  <files>
    src/lib/data/yahoo.ts,
    src/lib/data/polygon.ts,
    src/lib/data/finnhub.ts,
    src/lib/data/anthropic-search.ts,
    src/lib/data/lightweight-community-scan.ts,
    src/lib/data/cache/cache-keys.ts,
    tests/integration/lightweight-community-scan.breaker.integration.test.ts
  </files>
  <read_first>
    - src/lib/data/circuit-breaker.ts (Plan 02 — withBreaker signature)
    - src/lib/data/yahoo.ts (current withTelemetry sites at quote ~line 67 and fundamentals ~line 118)
    - src/lib/data/polygon.ts (current withTelemetry site at ~line 28-43)
    - src/lib/data/finnhub.ts (current withTelemetry site at ~line 26-43)
    - src/lib/data/anthropic-search.ts (4 withTelemetry sites — grep "withTelemetry\('anthropic-search'")
    - src/lib/data/lightweight-community-scan.ts (line 39 — withTelemetry firecrawl wrap)
    - src/lib/data/cache/cache-keys.ts (existing TTL_SECONDS — D-02 changes quote)
    - 30-RESEARCH.md (lines 240-260 — canonical composition order)
    - 30-RESEARCH.md (lines 462-477 — Firecrawl breaker integration)
    - tests/integration/lightweight-community-scan.breaker.integration.test.ts (Plan 01 RED stubs to fill)
  </read_first>
  <behavior>
    Every external call in `src/lib/data/` becomes `withTelemetry(provider, () => withBreaker(provider, () => withRetry(() => fn())))`. Provider id strings must match between `withTelemetry` and `withBreaker` (both wrap the same provider).

    `TTL_SECONDS.quote` becomes 60 seconds (was 300). Fundamentals stay at 86400.

    Firecrawl breaker integration: existing try/catch in scrapeOne returns empty markdown on any error, including BreakerOpenError — no new handling needed, just a comment cross-reference.
  </behavior>
  <action>
    For each adapter, insert `withBreaker(<id>, ...)` between `withTelemetry(<id>, ...)` and `withRetry(...)`. The id strings must be identical at both layers.

    `src/lib/data/yahoo.ts` — 2 sites (quote at line ~67, fundamentals at line ~118). Add `import { withBreaker } from '@/lib/data/circuit-breaker';` near existing imports. Each site changes from `withTelemetry('yahoo', () => withRetry(...))` to `withTelemetry('yahoo', () => withBreaker('yahoo', () => withRetry(...)))`.

    `src/lib/data/polygon.ts` — 1 site at ~line 28-43. Same pattern with provider id `'polygon'`.

    `src/lib/data/finnhub.ts` — 1 site at ~line 26-43. Same pattern with provider id `'finnhub'`.

    `src/lib/data/anthropic-search.ts` — 4 sites (fetchNews, fetchAnalystSentiment, fetchSecFilingSummary, fetchSocialSentiment). Locate via `grep -n "withTelemetry('anthropic-search'" src/lib/data/anthropic-search.ts`. Wrap each with `withBreaker('anthropic-search', ...)`.

    `src/lib/data/lightweight-community-scan.ts` — 1 site at ~line 39. Wrap with `withBreaker('firecrawl', ...)`. Above the call site add comment: `// Phase 30 D-23 — BreakerOpenError caught by surrounding try/catch in scrapeOne, scan continues with empty markdown`.

    `src/lib/data/cache/cache-keys.ts` — modify TTL_SECONDS:
    - Change `quote: 300` to `quote: 60` with inline comment `// Phase 30 D-02 — short TTL to avoid sentiment-scan reading stale 5-min prices`
    - Add comment above `fundamentals: 86_400`: `// Phase 30 D-02 — fundamentals TTL stays 24h (planner discretion; rarely intra-day)`

    `tests/integration/lightweight-community-scan.breaker.integration.test.ts` — convert all Plan-01 RED stubs to GREEN:
    - Add `vi.mock('@/lib/data/cache/upstash', async () => import('@/lib/data/cache/__mocks__/upstash'))`
    - In each `it` body, seed mock state for `breaker:firecrawl:state` with `{status:'open', opened_at: Date.now()}`
    - Call `lightweightCommunityScan('TSLA')` and assert it resolves (not rejects) and returns a shape-valid (possibly empty) result
    - Spy on the underlying SDK fetch and assert it was NOT called (breaker short-circuited)

    Do NOT touch `src/lib/data/source-package.ts` ordering in this task — Task 2 owns merge cascade.
  </action>
  <verify>
    <automated>npm test -- --run tests/integration/lightweight-community-scan.breaker.integration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "withBreaker" src/lib/data/yahoo.ts` returns at least 2 (quote + fundamentals)
    - `grep -c "withBreaker" src/lib/data/polygon.ts` returns at least 1
    - `grep -c "withBreaker" src/lib/data/finnhub.ts` returns at least 1
    - `grep -c "withBreaker" src/lib/data/anthropic-search.ts` returns at least 4
    - `grep -c "withBreaker" src/lib/data/lightweight-community-scan.ts` returns at least 1
    - `grep "quote: 60" src/lib/data/cache/cache-keys.ts` returns 1 match
    - `grep "Phase 30 D-02" src/lib/data/cache/cache-keys.ts` returns at least 2 matches (one per TTL comment)
    - Every adapter that uses `withBreaker` also has `import.*withBreaker.*circuit-breaker` — verify with grep
    - `npm test -- --run tests/integration/lightweight-community-scan.breaker.integration.test.ts` reports at least 3 passing, 0 failing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 9 adapter sites breaker-wrapped. Yahoo quote TTL is 60s. Firecrawl breaker integration tested end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reverse merge cascade, emit fallback_summary, render unavailable as em-dash</name>
  <files>
    src/lib/data/merge.ts,
    src/lib/data/source-package.ts,
    src/lib/data/__tests__/merge.test.ts,
    tests/unit/source-package.fallback.unit.test.ts,
    src/components/ResearchReport.tsx,
    src/lib/research-brief.ts
  </files>
  <read_first>
    - src/lib/data/merge.ts (lines 1-150 — cascade definition, current order at lines 67-71 and 106-110, unavailable_fields tracking at line 81)
    - src/lib/data/source-package.ts (buildSourcePackageOldLadder at ~line 309, buildSourcePackageNewLadder at ~line 504+)
    - src/lib/types.ts (FieldOrigin union from Plan 02; FallbackSummaryEntry interface; SourcePackage interface)
    - src/components/ResearchReport.tsx (line ~470 — current _field_sources rendering)
    - src/lib/research-brief.ts (audit for `_field_sources.*` reads)
    - 30-RESEARCH.md (lines 137-173 — field coverage matrix; lines 261-303 — D-09/D-11/D-12 details)
    - tests/unit/source-package.fallback.unit.test.ts (Plan 01 RED stubs to fill)
    - src/lib/data/__tests__/merge.test.ts (existing tests to extend)
  </read_first>
  <behavior>
    Per D-01 — merge cascade for shared fields reverses to polygon then finnhub then yahoo. Yahoo-only fields (price, volume, percent_change_today) keep yahoo-primary (no alternative source).

    Per D-09 — every cascade resolution emits a `FallbackSummaryEntry { field, tried, resolved_by }`. When all sources return null, `resolved_by` is `'unavailable'`. The array is attached to SourcePackage's return shape from both old-ladder and new-ladder build paths.

    Per D-11 — when `merge.ts` cannot resolve a field from any source, it sets `FieldOrigin` to `'unavailable'` (the new union value from Plan 02). `null` is preserved for legacy persisted data — renderers must handle both.

    Renderer change (D-11): `_field_sources?.price === 'unavailable'` should render as em-dash `'—'`, not "NaN" or empty. Same logic for `null` (legacy backward-compat). One-line check in `ResearchReport.tsx` and `research-brief.ts`.
  </behavior>
  <action>
    **In `src/lib/data/merge.ts`:**

    Convert `merge.ts` to **sequential short-circuit resolution per merge-eligible field** — call Polygon first, only invoke Finnhub if Polygon returns null for that field, only invoke Yahoo if Finnhub also returns null. The `fallback_summary[].tried` array reflects which providers were ACTUALLY invoked (so `tried=['polygon']` when polygon succeeds first time; `tried=['polygon','finnhub']` only when polygon returned null and finnhub was queried; `tried=['polygon','finnhub','yahoo']` only when both predecessors were null).

    This is a behavioral change from the current parallel-fan-out + first-non-null-wins pattern: the new code resolves shared fields lazily so a healthy primary saves the cost (and quota burn) of secondary calls. Yahoo-only fields (price, volume, percent_change_today) remain a single direct call since they have no fallback chain.

    Locate the cascade definition (current code around lines 67-71 and 106-110). For each shared-field cascade, reverse the array order from `[yahoo, finnhub, polygon]` to `[polygon, finnhub, yahoo]` AND replace the parallel-then-merge logic with a `for (const provider of cascade) { value = await fetch(provider); tried.push(provider); if (value !== null) break; }` short-circuit loop. Add a comment at the top of the file documenting the new order:

    ```
    // Phase 30 D-01 — cascade order for fields Polygon and Finnhub cover:
    //   polygon (primary) -> finnhub (fallback 1) -> yahoo (fallback 2)
    // Yahoo-only fields (price, volume, percent_change_today) keep yahoo-primary
    // because Polygon and Finnhub do not expose them (see field coverage matrix
    // in .planning/phases/30-provider-health-hardening/30-RESEARCH.md).
    ```

    Extend the merge-result return shape to include `fallback_summary: FallbackSummaryEntry[]`. For each resolved field, push one entry:
    ```
    { field: 'market_cap', tried: ['polygon'], resolved_by: 'polygon' }                          // first source won
    { field: 'pe_ratio', tried: ['polygon', 'finnhub'], resolved_by: 'finnhub' }                 // first null, fallback won
    { field: 'debt_to_equity', tried: ['polygon', 'finnhub', 'yahoo'], resolved_by: 'unavailable' }  // all null
    ```

    When all sources are null, set the field's FieldOrigin to `'unavailable'` instead of `null`. Keep the existing `unavailable_fields[]` array populated for back-compat — it shadows fallback_summary but is read elsewhere.

    Add `import type { FallbackSummaryEntry } from '@/lib/types';` near the existing imports.

    **In `src/lib/data/source-package.ts`:**

    Both `buildSourcePackageOldLadder` and `buildSourcePackageNewLadder` must capture the merge.ts fallback_summary output and attach it to the returned SourcePackage as `fallback_summary`. Add the property to the SourcePackage interface in `src/lib/types.ts` if not already present:
    ```
    fallback_summary?: FallbackSummaryEntry[];
    ```
    Marked optional so persisted legacy `Report.analysis` JSONs without this field continue to type-check.

    **In `src/components/ResearchReport.tsx` (~line 470):**

    The current code reads `_field_sources?.<field>` and renders source attribution. Add a branch:
    ```
    if (source === 'unavailable' || source === null) return '—';
    ```
    Place this before any string-rendering branch. Comment cites D-11.

    **In `src/lib/research-brief.ts`:**

    Grep for all reads of `_field_sources.<X>` or `field_origin`. For each, add the `'unavailable'` branch: render as "(no source available)" in the brief text — verbatim wording for the Gemini prompt input. Document with `// Phase 30 D-11` inline.

    **Convert `tests/unit/source-package.fallback.unit.test.ts` RED stubs to GREEN:**

    For each `it.todo`, replace with `it(...)` body. Use mocked adapters that return controllable values:
    - `it('field served by polygon directly: tried=[polygon], resolved_by=polygon')` — mock polygon returns 100, finnhub/yahoo not called
    - `it('polygon null then finnhub returns value: tried=[polygon,finnhub], resolved_by=finnhub')` — polygon null, finnhub returns 50
    - `it('all three nulls: tried=[polygon,finnhub,yahoo], resolved_by=unavailable')` — all null, FieldOrigin set to `'unavailable'`

    **Extend `src/lib/data/__tests__/merge.test.ts`:**

    Add `describe('Phase 30 / D-01 + D-11', () => {...})` block:
    - `it('shared fields cascade polygon → finnhub → yahoo')` — assert fallback_summary[i].tried[0] === 'polygon' for market_cap, pe_ratio, eps, exchange
    - `it('yahoo-only fields still yahoo-primary')` — price, volume, percent_change_today: tried[0] === 'yahoo'
    - `it('unavailable emitted when all sources null')` — assert FieldOrigin set to 'unavailable' for synthetic test field
  </action>
  <verify>
    <automated>npm test -- --run src/lib/data/__tests__/merge.test.ts tests/unit/source-package.fallback.unit.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 30 D-01" src/lib/data/merge.ts` returns at least 1 match
    - `grep "fallback_summary" src/lib/data/merge.ts` returns at least 3 matches (declaration + emissions)
    - `grep "FallbackSummaryEntry" src/lib/data/merge.ts` returns at least 1 match (import)
    - `grep "fallback_summary" src/lib/data/source-package.ts` returns at least 2 matches (both ladders attach it)
    - `grep "'unavailable'" src/lib/data/merge.ts` returns at least 1 match (emitted when all-null)
    - `grep "Phase 30 D-11" src/components/ResearchReport.tsx` returns at least 1 match
    - `grep "Phase 30 D-11" src/lib/research-brief.ts` returns at least 1 match
    - Sequential short-circuit verified: in the `polygon-returns-value` test, the finnhub + yahoo adapter spies are NOT called; in the `polygon-null + finnhub-value` test, the yahoo adapter spy is NOT called; in the `all-null` test, all three adapters were called exactly once each (assertion against vitest `vi.fn()` `.mock.calls.length`).
    - `tried` array on each `FallbackSummaryEntry` matches the providers actually invoked (not the entire cascade roster — so a polygon-successful resolution has `tried=['polygon']`, not `tried=['polygon','finnhub','yahoo']`).
    - `npm test -- --run src/lib/data/__tests__/merge.test.ts` reports new Phase-30 tests passing (no regressions on existing)
    - `npm test -- --run tests/unit/source-package.fallback.unit.test.ts` reports at least 4 passing, 0 todos, 0 failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Merge cascade reversed for shared fields. fallback_summary plumbing wired through SourcePackage. Renderers handle 'unavailable' as em-dash without runtime errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin Gemini model slugs and install cost-anomaly trip line in withTelemetry</name>
  <files>
    src/lib/gemini-analysis.ts,
    src/lib/sentiment/per-doc-classifier.ts,
    src/lib/telemetry/withTelemetry.ts,
    tests/unit/gemini-analysis.model-pin.unit.test.ts,
    tests/integration/cost-anomaly-breaker.integration.test.ts
  </files>
  <read_first>
    - src/lib/gemini-analysis.ts (lines 1140-1230 — current modelString routing logic; line 1212-1217 — ternary that picks flash/pro/haiku)
    - src/lib/sentiment/per-doc-classifier.ts (line 103 — current `'google/gemini-3.1-flash-lite'` pin; verify it remains)
    - src/lib/telemetry/withTelemetry.ts (full file — find cost_usd computation site for the post-success check insertion)
    - src/lib/telemetry/cost-estimators.ts (ProviderId type — confirm 'gemini' is a member)
    - src/lib/data/circuit-breaker.ts (Plan 02 — the cost-anomaly breaker reuses the same `breaker:gemini:state` key as the regular breaker)
    - 30-RESEARCH.md (lines 305-352 — D-14 + D-15 implementation details; R-3 model-id resolution at line 685-688)
    - tests/unit/gemini-analysis.model-pin.unit.test.ts (Plan 01 RED stubs to fill)
    - tests/integration/cost-anomaly-breaker.integration.test.ts (Plan 01 RED stubs to fill)
  </read_first>
  <behavior>
    Per D-14 — explicit per-call-site model pins, no fuzzy routing.
    - Main analysis (`src/lib/gemini-analysis.ts`): `model: 'google/gemini-3-pro'` (NOT 2.5; per the orchestrator's R-3 resolution, the live codebase uses the 3-tier slugs and the CONTEXT.md text is stale).
    - Per-doc classifier (`src/lib/sentiment/per-doc-classifier.ts`): keep `'google/gemini-3.1-flash-lite'` (already pinned — verify only).
    - Any other Gemini call site found via grep: `'google/gemini-3-flash'`.

    Per D-15 — withTelemetry post-success block, after `cost_usd` is computed:
    1. If `provider_id === 'gemini'` and `cost_usd > 1.00`, fire-and-forget Upstash INCR on `cost_anomaly:gemini`.
    2. If the increment returned 1, set EXPIRE to 3600 seconds (1h window).
    3. If the increment returned 3 or higher, write `breaker:gemini:state` with `{status:'open', opened_at: Date.now(), reason: 'cost_anomaly'}` (1h TTL) and DEL the counter (resets after trip).
    4. All of step 1-3 wrapped in `queueMicrotask` so it never blocks the original call's return.
  </behavior>
  <action>
    **In `src/lib/gemini-analysis.ts`:**

    Locate the current ternary at lines ~1212-1217 that picks between `'google/gemini-3-flash'`, `'google/gemini-3-pro'`, and `'anthropic/claude-haiku-4.5'`. The decision logic depends on `routerCtx.modelOverride`.

    Replace with a hard-pinned constant for the main analysis call:
    ```
    // Phase 30 D-14 — explicit model pin, no AI-Gateway fuzzy routing.
    // R-3 resolution: 3-tier slugs reflect the live codebase; CONTEXT.md mentions
    // 2.5 slugs but the live codebase has been on 3-tier since Phase 19-C-09.
    // The haiku fallback branch is removed because D-14 mandates explicit pinning
    // to gemini-3-pro for analysis; haiku routing was previously a fuzzy-routing
    // artifact from the now-deprecated AI-Gateway auto-route path and never
    // reflected an intentional product decision for the main analysis call.
    const modelString = 'google/gemini-3-pro';
    ```
    Remove the `routerCtx?.modelOverride === 'haiku' ? ... : ...` branch entirely from this call site (per the inline comment above — the haiku branch was a fuzzy-routing artifact, never a product decision).

    Per R-12 in research — DO NOT delete `src/lib/reasoning/router.ts`. The router still runs, emits `learning_events` with `event_type='model_router_decision'`, and tracks token usage. Just ignore its `modelOverride` output for the model selection at this call site. Preserve the `routerCtx.usageOut.tokens` write at line ~1247-1249.

    Add a guard comment above the model line:
    ```
    // If you need to change this model, also update tests/unit/gemini-analysis.model-pin.unit.test.ts
    // and re-verify the Phase 30 done-gate cost SQL.
    ```

    **In `src/lib/sentiment/per-doc-classifier.ts` (line 103):**

    Verify the existing pin is `'google/gemini-3.1-flash-lite'`. Add adjacent comment:
    ```
    // Phase 30 D-14 — explicit model pin (lightweight per-doc classifier — Flash-lite tier)
    ```

    **In `src/lib/telemetry/withTelemetry.ts`:**

    After the existing cost_usd computation (locate via grep `cost_usd` — likely near line 85-100), insert the post-success cost-anomaly block:

    ```
    // Phase 30 D-15 — cost-ceiling regression guard.
    // Gemini call costing more than $1.00 increments an Upstash counter; 3 such
    // calls within 1h trip a 1h provider-wide gemini breaker. Reuses the same
    // breaker:gemini:state key that withBreaker reads, so no second breaker class.
    if (provider_id === 'gemini' && typeof cost_usd === 'number' && cost_usd > 1.00) {
      queueMicrotask(async () => {
        try {
          const r = getRedis();
          if (!r) return;
          const key = 'cost_anomaly:gemini';
          const count = await r.incr(key);
          if (count === 1) await r.expire(key, 3600);
          if (count >= 3) {
            await r.set(
              'breaker:gemini:state',
              JSON.stringify({ status: 'open', opened_at: Date.now(), reason: 'cost_anomaly' }),
              { ex: 3600 },
            );
            await r.del(key);
            console.warn('[withTelemetry] gemini cost_anomaly breaker tripped:', { cost_usd, count });
          }
        } catch (err) {
          // Fire-and-forget — never block the caller. Cost-anomaly tracking is best-effort.
          console.warn('[withTelemetry] cost_anomaly tracking failed:', String(err));
        }
      });
    }
    ```

    Add `import { getRedis } from '@/lib/data/cache/upstash';` at top if not already present.

    The block is positioned AFTER cost_usd is computed and BEFORE the call returns — but since the work is in `queueMicrotask`, it does not affect return timing.

    **Convert `tests/unit/gemini-analysis.model-pin.unit.test.ts` RED stubs to GREEN:**

    Use `vi.mock` on the `ai` package (or whatever provides `generateText`/`generateObject`) to spy on the `model` argument:
    - `it('runGeminiAnalysis passes model: google/gemini-3-pro')` — call `runGeminiAnalysis(samplePkg)`, assert the spy was called with `model: 'google/gemini-3-pro'`
    - `it('per-doc-classifier passes model: google/gemini-3.1-flash-lite')` — call the per-doc classifier on a sample message, assert the same spy received the flash-lite slug
    - `it('no dynamic model variable in gemini-analysis.ts main call')` — bash-style grep test using `fs.readFileSync` over `src/lib/gemini-analysis.ts` to assert there is no `model: modelString` or `model: \`...\`` template-literal pattern in the analysis call (the line is now a string literal)

    **Convert `tests/integration/cost-anomaly-breaker.integration.test.ts` RED stubs to GREEN:**

    Use the Upstash mock + a spy on `withTelemetry` to simulate cost_usd values:
    - `it('one $1.01 gemini call increments cost_anomaly:gemini to 1 with 1h TTL')`: drive withTelemetry once with mocked Gemini fn returning success, force cost_usd = 1.01 via test harness; await one tick; assert `mockRedis.get('cost_anomaly:gemini') === '1'` and TTL ~ 3600
    - `it('three $1.01 calls within 1h trip breaker:gemini:state')`: drive 3 times, assert breaker state key is set with `reason: 'cost_anomaly'`, status `'open'`
    - `it('one call then time advance then call does not trip')`: __advanceMockTime by 3700_000 (>1h) between calls so the counter has expired
    - `it('trip resets the counter')`: assert `cost_anomaly:gemini` is deleted after the 3rd increment
  </action>
  <verify>
    <automated>npm test -- --run tests/unit/gemini-analysis.model-pin.unit.test.ts tests/integration/cost-anomaly-breaker.integration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "google/gemini-3-pro" src/lib/gemini-analysis.ts` returns at least 1 match (the new pinned line)
    - `grep "Phase 30 D-14" src/lib/gemini-analysis.ts` returns at least 1 match
    - `grep "Phase 30 D-14" src/lib/sentiment/per-doc-classifier.ts` returns 1 match
    - `grep "google/gemini-3.1-flash-lite" src/lib/sentiment/per-doc-classifier.ts` returns 1 match (unchanged from before this plan)
    - `grep "cost_anomaly:gemini" src/lib/telemetry/withTelemetry.ts` returns at least 1 match
    - `grep "Phase 30 D-15" src/lib/telemetry/withTelemetry.ts` returns at least 1 match
    - `grep "queueMicrotask" src/lib/telemetry/withTelemetry.ts` returns at least 1 match
    - `grep "breaker:gemini:state" src/lib/telemetry/withTelemetry.ts` returns at least 1 match
    - `npm test -- --run tests/unit/gemini-analysis.model-pin.unit.test.ts` reports at least 3 passing, 0 failures
    - `npm test -- --run tests/integration/cost-anomaly-breaker.integration.test.ts` reports at least 4 passing, 0 failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Gemini main analysis pinned to 3-pro, per-doc classifier verified at 3.1-flash-lite, cost-anomaly trip line installed and tested against mocked Upstash time advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → External providers | Now mediated by withBreaker per provider — no direct unguarded calls in `src/lib/data/` |
| withTelemetry → Upstash | Cost-anomaly counter writes; fire-and-forget; failure logged but never blocks caller |
| AI Gateway → Gemini | Model header now explicit on every call — no implicit auto-route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | Caller bypasses breaker by importing raw SDK | mitigate | Adapter modules are the boundary — all consumers go through `src/lib/data/`. Acceptance criteria grep enforces every adapter has withBreaker wrap |
| T-30-03-02 | Spoofing | Wrong provider id in withBreaker vs withTelemetry | mitigate | Composition pattern uses the same string literal for both layers; tests assert provider id match in the integration test |
| T-30-03-03 | Information Disclosure | Cost-anomaly trip log line leaks cost_usd value | accept | `console.warn` only — already a project pattern; cost_usd is not sensitive |
| T-30-03-04 | Denial of Service | Cost-anomaly false-positive trips legitimate Pro analysis | accept | R-7 mitigation: 3 calls in 1h threshold; current 6 scans/day = ~0.04 events/h-window. Counter is fire-and-forget — missed increment does not break engine |
| T-30-03-05 | Tampering | Renderer crashes on `'unavailable'` FieldOrigin in legacy data | mitigate | Renderer handles both `'unavailable'` and `null` (strategy 2 from R-2) — tested in research-brief unit tests |
</threat_model>

<verification>
- `npm test -- --run` exits 0 across all modified test files
- `npx tsc --noEmit` exits 0
- `grep -r "withBreaker" src/lib/data/` returns at least 9 matches across the adapter files (matching Task 1 totals)
- `grep -r "google/gemini" src/lib/` returns exactly the pinned slugs at the call sites identified in Task 3 (no fuzzy routing)
- `grep -r "cost_anomaly:gemini" src/` returns matches in `withTelemetry.ts` only (the trip line owner)
</verification>

<success_criteria>
- Every adapter call site is wrapped withBreaker
- Merge cascade prioritizes Polygon over Yahoo for shared fields
- SourcePackage carries fallback_summary plumbing
- FieldOrigin renders `'unavailable'` as em-dash
- Gemini main analysis hard-pinned to google/gemini-3-pro (no router-driven swap)
- Cost-anomaly trip line installed and exercised against mocked Upstash
- All Plan-01 RED stubs for D-09, D-14, D-15, D-23 are now GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/30-provider-health-hardening/30-03-SUMMARY.md` listing each file modified, the breaker composition pattern used, the model slugs pinned, and confirmation that the cost-anomaly trip line reuses the same `breaker:gemini:state` key as the regular breaker.
</output>

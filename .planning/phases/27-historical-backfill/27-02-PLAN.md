---
phase: 27-historical-backfill
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/lib/backtest/windowing.ts
  - src/lib/backtest/ohlcv-cache.ts
  - scripts/backfill-historical.ts
  - tests/unit/backfill-cache-fetch-once.test.ts
requirements: [COVERAGE-06, COVERAGE-07, COVERAGE-08, COVERAGE-09]
tags: [backfill, data, technical, learning, pit-discipline]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A single command `npx tsx scripts/backfill-historical.ts --dry-run` walks the full universe, computes weekly technical snapshots over >=5y, and exits 0 writing nothing"
    - "Each ticker's raw OHLCV is fetched from Yahoo at most once (disk-cached); re-runs read the cache, not Yahoo"
    - "Backfilled SentimentSnapshot rows carry source='backfill', the real historical scanned_at, and technical_data; PriceOutcome rows carry all three Phase 21 labels with recorded_at = scanned_at + days_after"
    - "The CLI uses computeTechnicalSnapshot (the canonical extractor) and never defines its own indicator math"
    - "A crash 80 tickers in resumes from ticker 81 (per-ticker checkpoint); re-runs do not duplicate rows (createMany skipDuplicates on the (ticker, scanned_at) unique)"
  artifacts:
    - path: "src/lib/backtest/windowing.ts"
      provides: "Pure PIT helpers buildWeeklyAsOfDates + computeOutcomeRecordedAt (turns COVERAGE-07 stub GREEN)"
      exports: ["buildWeeklyAsOfDates", "computeOutcomeRecordedAt"]
    - path: "scripts/backfill-historical.ts"
      provides: "The Phase 27 one-shot CLI (universe walk, disk cache, weekly snapshots, 3-label outcomes, source='backfill') — turns COVERAGE-08 stub GREEN"
      contains: "computeTechnicalSnapshot"
  key_links:
    - from: "scripts/backfill-historical.ts"
      to: "src/lib/backtest/universe.ts"
      via: "import BACKFILL_UNIVERSE"
      pattern: "BACKFILL_UNIVERSE"
    - from: "scripts/backfill-historical.ts"
      to: "src/lib/data/technical.ts"
      via: "import computeTechnicalSnapshot"
      pattern: "computeTechnicalSnapshot"
    - from: "scripts/backfill-historical.ts"
      to: "sentiment_snapshots.source"
      via: "createMany with source: 'backfill'"
      pattern: "source:\\s*'backfill'"
    - from: "scripts/backfill-historical.ts"
      to: "src/lib/data/sector-mapping.ts + sector-prices.ts"
      via: "getSectorETF({asOfDate}) + fetchSectorETFReturn"
      pattern: "fetchSectorETFReturn"
---

<objective>
Build the Phase 27 backfill CLI — the heavy-lift that replays the versioned universe
(Plan 01) through the canonical technical feature path over >=5 years of weekly windows,
writing `source='backfill'` SentimentSnapshot + PriceOutcome rows under strict point-in-time
discipline. This plan turns the COVERAGE-07 (PIT) and COVERAGE-08 (single feature path) RED
stubs GREEN, and makes the COVERAGE-06 (>=100 tickers x >=5y) and COVERAGE-09 (source tag,
end to end) behaviors real and runnable.

Purpose: Bootstrap the prior count N that Phase 23's lift-gate needs — the historical-outcome
population the Purged-K-Fold Brier-lift reads (D-01). Without this, sparse cells (esp. small_cap)
never reach the minimum observation count to be evaluable.

Output: `src/lib/backtest/windowing.ts` (pure PIT helpers) and `scripts/backfill-historical.ts`
(the resumable, disk-cached, batched CLI). No rows are written by this plan's verification — the
operator runs the live backfill after the phase ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-historical-backfill/27-CONTEXT.md
@.planning/phases/27-historical-backfill/27-RESEARCH.md
@.planning/phases/27-historical-backfill/27-01-PLAN.md

<interfaces>
<!-- Exact signatures extracted from the codebase. Use directly — no exploration needed. -->

The single feature path (src/lib/data/technical.ts) — D-11/COVERAGE-08:
```typescript
export interface OhlcvBar { date: Date; open: number; high: number; low: number; close: number; volume: number; }
// period2 = asOf ?? now; period1 = period2 - 365d. Drops bars where any of high/low/close/open/date is null.
export async function fetchOhlcv(ticker: string, asOf?: Date): Promise<OhlcvBar[]>;
// Returns null if no bars fetched. Returns a snapshot with tech_pattern=null when bar_count < 200.
export async function computeTechnicalSnapshot(ticker: string, asOf?: Date): Promise<TechnicalSnapshot | null>;
// The module instantiates `const yahooFinance = new YahooFinance(...)` at module scope and calls
// `(yahooFinance as any).chart(ticker, { period1, period2, interval: '1d' })` inside fetchOhlcv.
```

Cap classification (src/lib/diffusion-trace.ts) — D-08. NOTE: Yahoo quoteSummary returns CURRENT
market cap (RESEARCH A1) — current-cap proxy is the documented simplification:
```typescript
export type CapClass = 'large_cap' | 'mid_cap' | 'small_cap' | 'unknown';
export function classifyCapClass(marketCap: number | null | undefined): CapClass; // >=10B large, >=2B mid, else small
```

Sector labels (D-12) — both already PIT-capable via yf.chart() with month-keyed period1/period2:
```typescript
// src/lib/data/sector-mapping.ts — honors SECTOR_RECONSTITUTIONS (META/GOOGL/GOOG/NFLX/DIS/T/VZ @ 2018-09-28)
export async function getSectorETF(args: { ticker: string; asOfDate?: Date }): Promise<SectorETF>;
// src/lib/data/sector-prices.ts — returns pct return (2.5 == +2.5%) over [fromDate,toDate], or null.
// Internally fetches yf.chart(etf, {period1, period2}) per month-key, so arbitrary historical ranges work.
export async function fetchSectorETFReturn(etf: SectorETF, fromDate: Date, toDate: Date): Promise<number | null>;
```

Hit classification (src/lib/learning.ts) — sector-relative primary, SPY fallback:
```typescript
export function classifyHit(args: { ticker_return_pct: number; spy_return_pct: number | null;
  sector_relative_pct?: number | null; sector_sigma?: number | null; k?: number; threshold_pct?: number; }): boolean;
```

Universe (Plan 01, src/lib/backtest/universe.ts):
```typescript
export const UNIVERSE_VERSION: string;
export interface UniverseEntry { ticker: string; curation_cap: 'large_cap' | 'mid_cap' | 'small_cap'; }
export const BACKFILL_UNIVERSE: readonly UniverseEntry[];
export function tickersByCuration(cap: UniverseEntry['curation_cap']): string[];
```

PriceOutcome columns already present (prisma/schema.prisma): id, report_id?, snapshot_id?,
days_after, price, pct_change, recorded_at, sector_etf?, forward_return_raw?, forward_return_sector_rel?.
SentimentSnapshot now (post Plan 01) has: source String @default("live") + @@unique([ticker, scanned_at]).

Reference CLI shape (scripts/backfill-technical.ts): `loadDotenv({ path: '.env.local' })`,
`new PrismaClient({ adapter: new PrismaNeon({ connectionString: process.env.DATABASE_URL! }) })`,
`process.argv.includes('--dry-run')`, throttle via `await new Promise(r => setTimeout(r, MS))`.
Horizons used by the learn cron: [3, 7, 14, 30, 60, 90].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure PIT windowing helpers (turns COVERAGE-07 GREEN)</name>
  <read_first>
    - tests/unit/backfill-pit-discipline.test.ts (Plan 01 RED stub — the EXACT contract this task satisfies: buildWeeklyAsOfDates strictly-increasing + all in the past; computeOutcomeRecordedAt = scanned_at + days_after)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (§ "Purged-K-Fold CV Pool Compatibility" — scanned_at = historical window date, recorded_at = scanned_at + days_after, NEVER the run date)
    - src/lib/learning.ts (lines 534-566: timeBasedSplit — same `recorded_at: Date` field shape the windowing output feeds into)
  </read_first>
  <files>src/lib/backtest/windowing.ts</files>
  <behavior>
    - buildWeeklyAsOfDates(start, end): returns Date[] stepping +7 days from start, last date <= end, strictly increasing, none in the future (caller passes end <= now).
    - buildWeeklyAsOfDates over 2021-01-01..2021-03-01 returns >= 7 dates.
    - computeOutcomeRecordedAt(scannedAt, daysAfter): returns new Date(scannedAt.getTime() + daysAfter * 86_400_000) — pure arithmetic, independent of Date.now().
    - computeOutcomeRecordedAt(new Date('2021-02-05'), 30) equals 2021-02-05 + 30d, NOT today.
  </behavior>
  <action>
    Create `src/lib/backtest/windowing.ts` with two PURE functions (no Date.now() reads, no I/O) —
    these are the exact exports the Plan 01 COVERAGE-07 stub imports from `@/lib/backtest/windowing`:

    ```typescript
    // src/lib/backtest/windowing.ts
    // Phase 27 D-08/COVERAGE-07 — point-in-time windowing helpers.
    // scanned_at MUST be the historical window date; recorded_at MUST be scanned_at + days_after.
    // Setting either to the backfill RUN date collapses Phase 23's Purged-K-Fold to a single fold
    // (RESEARCH § Purged-K-Fold CV Pool Compatibility). These functions never read Date.now().
    const DAY_MS = 86_400_000;

    /** Weekly (+7d) asOf dates in [start, end], strictly increasing, last <= end. */
    export function buildWeeklyAsOfDates(start: Date, end: Date): Date[] {
      const out: Date[] = [];
      for (let t = start.getTime(); t <= end.getTime(); t += 7 * DAY_MS) {
        out.push(new Date(t));
      }
      return out;
    }

    /** Outcome resolution date = scanned_at + days_after (PIT — never the run date). */
    export function computeOutcomeRecordedAt(scannedAt: Date, daysAfter: number): Date {
      return new Date(scannedAt.getTime() + daysAfter * DAY_MS);
    }
    ```

    Do NOT add any other exports. The weekly cadence (+7d) is the D-cadence default (Claude's
    Discretion in CONTEXT — weekly). The CLI (Task 3) passes `end = new Date(now - safetyDays)` so
    no future dates are produced; this module stays pure and does not enforce that itself.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/backfill-pit-discipline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/backtest/windowing.ts` exports exactly `buildWeeklyAsOfDates` and `computeOutcomeRecordedAt`
    - `grep -c 'Date.now()' src/lib/backtest/windowing.ts` returns 0 (helpers are pure)
    - `npx vitest run tests/unit/backfill-pit-discipline.test.ts` passes (the Plan 01 RED stub is now GREEN)
    - `buildWeeklyAsOfDates(new Date('2021-01-01T00:00:00Z'), new Date('2021-03-01T00:00:00Z')).length >= 7`
  </acceptance_criteria>
  <done>windowing.ts created with two pure helpers; COVERAGE-07 PIT-discipline test flips RED→GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Backfill CLI scaffold — universe walk, disk-cached fetch-once, dry-run, resumable checkpoint (turns COVERAGE-08 GREEN)</name>
  <read_first>
    - tests/unit/backfill-single-feature-path.test.ts (Plan 01 RED stub — asserts scripts/backfill-historical.ts EXISTS, imports computeTechnicalSnapshot, and does NOT `from 'technicalindicators'`)
    - scripts/backfill-technical.ts (lines 19-36, 67-77: the dotenv + PrismaNeon + DRY_RUN + throttle pattern to MIRROR; do NOT log DATABASE_URL)
    - scripts/backfill-snapshot-cap.ts (lines 25-41: yf.quoteSummary → classifyCapClass as-of cap pattern for D-08)
    - src/lib/data/technical.ts (lines 44-71, 180-191: fetchOhlcv calls yahooFinance.chart internally; computeTechnicalSnapshot(ticker, asOf) is the only allowed extractor)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (§ Pattern 1 fetch-once disk cache, § Pattern 3 checkpoint, § Pitfall 3 fetchOhlcv refetch interception)
  </read_first>
  <files>scripts/backfill-historical.ts</files>
  <action>
    Create `scripts/backfill-historical.ts` mirroring `scripts/backfill-technical.ts`'s header,
    dotenv load, PrismaNeon construction, `--dry-run` flag, and throttle pattern. This task builds
    the orchestration shell + fetch/cache layer; Task 4 wires snapshot+outcome writes.

    Required structure (concrete, executable):

    1. Header comment: local one-shot CLI (D-06), off-Vercel rationale, `npx tsx scripts/backfill-historical.ts [--dry-run] [--max-tickers N]`, survivorship caveat ref (D-03), and "after this, trigger /api/cron/learn with $CRON_SECRET for the recompute pass".
    2. `import { config as loadDotenv } from 'dotenv'; loadDotenv({ path: '.env.local' });` then `import { PrismaClient } from '@prisma/client'; import { PrismaNeon } from '@prisma/adapter-neon';` then `import { computeTechnicalSnapshot, type OhlcvBar } from '../src/lib/data/technical';` and `import { BACKFILL_UNIVERSE } from '../src/lib/backtest/universe';` and `import { classifyCapClass } from '../src/lib/diffusion-trace';`. Do NOT import from `'technicalindicators'` (COVERAGE-08 audit forbids it).
    3. Flags: `const DRY_RUN = process.argv.includes('--dry-run');` and parse `--max-tickers N` (default = full universe) for smoke runs.
    4. Constants: `const YEARS = 5;`, `const FETCH_THROTTLE_MS = 1000;`, `const CACHE_DIR = path.join(os.homedir(), '.cipher', 'backfill-cache');`, `const CHECKPOINT_FILE = path.join(CACHE_DIR, 'checkpoint.json');`. (Cache lives under `os.homedir()`, OUTSIDE the repo — Plan 01 also gitignored `.cipher/` defensively.)
    5. Disk-cache fetch-once + prototype interception live in a SEPARATE, IMPORTABLE module so the mechanism is unit-testable (RESEARCH Pattern 1 + Pitfall 3). Create `src/lib/backtest/ohlcv-cache.ts` exporting:
       - `fetchOrLoadOhlcv(ticker, deps?)` — if `${CACHE_DIR}/${ticker}.json` exists, read+parse it (revive `date` strings to Date); else call the ORIGINAL chart ONCE for the full `now-5y → now` daily range, filter to bars where `close != null`, `mkdirSync(CACHE_DIR, {recursive:true})`, write JSON, return. `deps` is an injectable seam `{ chart?: (sym, opts) => Promise<{quotes}>, cacheDir?: string }` so tests can supply a fake chart + temp dir.
       - `installChartCache()` — captures `const _orig = YahooFinance.prototype.chart;` then sets `YahooFinance.prototype.chart = async function (sym, opts) { const all = await fetchOrLoadOhlcv(String(sym), { chart: _orig.bind(this) }); const p1 = +new Date(opts.period1); const p2 = +new Date(opts.period2); return { quotes: all.filter(b => b.date.getTime() >= p1 && b.date.getTime() <= p2) }; };` Returns an `uninstall()` to restore `_orig` (tests use it). `fetchOrLoadOhlcv` memoizes per-ticker in-process so concurrent windows of the same ticker share one fetch.
    6. **fetchOhlcv interception (RESEARCH Pitfall 3 — CRITICAL, the perf-defining mechanism — COMMITTED, no checkpoint):** each weekly `computeTechnicalSnapshot(ticker, asOf)` calls `fetchOhlcv` → `yahooFinance.chart(...)` internally, and `technical.ts` instantiates its OWN module-scope `new YahooFinance(...)`. Intercept at the CLASS prototype so EVERY instance (including technical.ts's) is covered, and do it BEFORE technical.ts loads:
       a. In the CLI, statically `import YahooFinance from 'yahoo-finance2';` and `import { installChartCache } from '../src/lib/backtest/ohlcv-cache';` at top.
       b. Call `installChartCache();` as the FIRST executable statement in `main()` (before anything that pulls in technical.ts).
       c. Import the extractor DYNAMICALLY after the patch: `const { computeTechnicalSnapshot } = await import('../src/lib/data/technical');` — NOT a static top-level import (static imports hoist above the patch). `import type { OhlcvBar }` stays static (types erase). This guarantees ≤1 real Yahoo fetch per ticker regardless of how many weekly `asOf` windows are computed, with ZERO edits to technical.ts and no forked extractor (COVERAGE-08 preserved).
    7. Checkpoint (RESEARCH Pattern 3): `loadCheckpoint(): Set<string>` reads `CHECKPOINT_FILE` (empty Set if absent); `markDone(ticker, done)` adds + writes the JSON array. The main loop skips tickers already in the checkpoint so a crash at ticker 80 resumes at 81.
    8. `main()`: guard `if (!process.env.DATABASE_URL) { console.log('No DATABASE_URL — exiting cleanly.'); return; }` (NEVER echo the URL). Construct prisma via PrismaNeon. Slice universe to `--max-tickers`. For each ticker not in checkpoint: pre-warm `fetchOrLoadOhlcv`, then (Task 4 fills snapshot/outcome work — for THIS task, just log `· ${ticker} cached N bars` and `markDone`). Throttle `FETCH_THROTTLE_MS` between tickers. End with `await prisma.$disconnect()`.
    9. `main().catch(err => { console.error(err); process.exit(1); });`

    Make the COVERAGE-08 stub pass NOW: the file must exist, import `computeTechnicalSnapshot`, and contain NO `from 'technicalindicators'`.
  </action>
    Also write `tests/unit/backfill-cache-fetch-once.test.ts` — the regression that PROVES the fetch-once mechanism (warning-3 gap): inject a spy `chart` via `fetchOrLoadOhlcv(ticker, { chart: spy, cacheDir: <tmpdir> })`, then call it 3× with different `asOf`-derived windows for the SAME ticker; assert the spy fired EXACTLY once (subsequent calls hit the in-process memo / disk cache). Add a second case via `installChartCache()`/`uninstall()` asserting `YahooFinance.prototype.chart` is patched then restored. Use a per-test temp `cacheDir` (e.g. `fs.mkdtempSync`) so the suite never touches `~/.cipher`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/backfill-single-feature-path.test.ts tests/unit/backfill-cache-fetch-once.test.ts && npx tsc --noEmit 2>&1 | grep -v 'node_modules' | grep -E "backfill-historical|ohlcv-cache" || echo "TSC-CLEAN-FOR-CLI"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/backfill-historical.ts` exists; `grep -c "computeTechnicalSnapshot" scripts/backfill-historical.ts` >= 1
    - CLI imports the extractor DYNAMICALLY after the patch: `grep -Ec "await import\(.*technical" scripts/backfill-historical.ts` >= 1 AND there is NO static `import { computeTechnicalSnapshot }` line (`grep -c "import { computeTechnicalSnapshot" scripts/backfill-historical.ts` returns 0; a static `import type { OhlcvBar }` is allowed)
    - `src/lib/backtest/ohlcv-cache.ts` exists and exports `fetchOrLoadOhlcv` + `installChartCache`; `grep -c 'os.homedir' src/lib/backtest/ohlcv-cache.ts` >= 1 (cache lives outside the repo)
    - `grep -c "YahooFinance.prototype.chart" src/lib/backtest/ohlcv-cache.ts` >= 1 (prototype-level interception, covers technical.ts's own instance)
    - `grep -c "from 'technicalindicators'" scripts/backfill-historical.ts` returns 0 AND `grep -c 'from "technicalindicators"' scripts/backfill-historical.ts` returns 0
    - `grep -c 'BACKFILL_UNIVERSE' scripts/backfill-historical.ts` >= 1
    - `grep -Ec 'process.env.DATABASE_URL\s*\)' scripts/backfill-historical.ts` >= 1 AND no `console.log` line prints `DATABASE_URL` value (manual grep: `grep -n 'console.*DATABASE_URL' scripts/backfill-historical.ts` returns no value-printing line)
    - `npx vitest run tests/unit/backfill-single-feature-path.test.ts` passes (COVERAGE-08 RED→GREEN)
    - `npx vitest run tests/unit/backfill-cache-fetch-once.test.ts` passes — asserts the underlying chart fires ≤1× per ticker across multiple asOf windows
  </acceptance_criteria>
  <done>CLI scaffold + extracted testable ohlcv-cache (prototype-patch + dynamic import of technical.ts) + checkpoint + dry-run exist; COVERAGE-08 single-feature-path test GREEN; fetch-once regression GREEN proving ≤1 Yahoo call/ticker; no forked indicator math. Operator smoke-run note: full runtime cache validation also requires `npx tsx scripts/backfill-historical.ts --dry-run --max-tickers 3` with DATABASE_URL SET (logged in 27-VALIDATION.md Manual-Only Verifications).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify fetchSectorETFReturn supports historical ranges; guard if not (RESEARCH Open Question 1 / A4)</name>
  <read_first>
    - src/lib/data/sector-prices.ts (the full file — confirm fetchSectorETFReturn builds month-keyed period1/period2 and calls yf.chart, i.e. arbitrary historical dates work)
    - src/lib/data/sector-mapping.ts (lines 98-153: SECTOR_RECONSTITUTIONS + getSectorETF asOfDate path)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (Open Question 1, Assumption A4, Pitfall 5: pre-2015 sector ETFs fall back to SPY)
  </read_first>
  <files>scripts/backfill-historical.ts</files>
  <action>
    Add a `--probe-sector` mode to `scripts/backfill-historical.ts` that resolves Open Question 1
    BEFORE the heavy outcome path is wired (Task 4). When `process.argv.includes('--probe-sector')`:

    1. Pick a window 5 years in the past: `from = new Date(Date.now() - 5*365*86_400_000)`, `to = new Date(from.getTime() + 30*86_400_000)`.
    2. Resolve a sector ETF as-of that date: `const etf = await getSectorETF({ ticker: 'AAPL', asOfDate: from });` (import getSectorETF from `../src/lib/data/sector-mapping`).
    3. Call `const ret = await fetchSectorETFReturn(etf, from, to);` (import from `../src/lib/data/sector-prices`).
    4. Print `probe: ${etf} ${from.toISOString().slice(0,10)}→${to.toISOString().slice(0,10)} return=${ret}` and `process.exit(ret == null ? 1 : 0)`.

    Then in the OUTCOME computation path (used by Task 4), encode the guard: when
    `fetchSectorETFReturn` returns null for a historical window, fall back to SPY exactly as
    `price-followup/route.ts` `computeSectorLabels` does (sector→SPY→null sentinel), incrementing
    a `sector_fallback_to_spy` counter and logging it at the end (RESEARCH Pitfall 5: XLRE/XLC
    predate-inception windows fall back to SPY; this is documented, not an error). Add a one-line
    comment citing RESEARCH OQ1 confirming sector-prices.ts IS historical-range-capable (month-keyed
    yf.chart), so no new fetch layer is needed — the existing 30-day TTL cache covers re-runs.
  </action>
  <verify>
    <automated>grep -q "probe-sector" scripts/backfill-historical.ts && grep -q "fetchSectorETFReturn" scripts/backfill-historical.ts && grep -q "getSectorETF" scripts/backfill-historical.ts && npx tsc --noEmit 2>&1 | grep "backfill-historical" || echo "TSC-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'probe-sector' scripts/backfill-historical.ts` >= 1
    - `grep -c 'fetchSectorETFReturn' scripts/backfill-historical.ts` >= 1 AND `grep -c 'getSectorETF' scripts/backfill-historical.ts` >= 1
    - A SPY fallback path exists when sector return is null (`grep -c "'SPY'" scripts/backfill-historical.ts` >= 1) with a `sector_fallback_to_spy` counter (`grep -c 'sector_fallback' scripts/backfill-historical.ts` >= 1)
    - `npx tsc --noEmit` reports no errors originating in `scripts/backfill-historical.ts`
  </acceptance_criteria>
  <done>--probe-sector mode added; historical-range support confirmed in-code; SPY fallback + fallback counter wired for the outcome path; OQ1/A4 resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire weekly snapshot + 3-label outcome generation with source='backfill', as-of cap_class, batched idempotent writes (COVERAGE-06/07/09 behaviors real)</name>
  <read_first>
    - scripts/backfill-historical.ts (the scaffold from Tasks 2-3 this task completes)
    - src/lib/backtest/windowing.ts (Task 1 — buildWeeklyAsOfDates + computeOutcomeRecordedAt)
    - src/app/api/cron/price-followup/route.ts (lines 47-87: computeSectorLabels — the EXACT 3-label shape to replicate: forward_return_raw + forward_return_sector_rel + sector_etf + pct_change)
    - scripts/backfill-snapshot-cap.ts (as-of cap pattern — quoteSummary modules ['summaryDetail','price'] → classifyCapClass)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (§ Pattern 4 batched createMany, § Pitfall 1 timestamps, § Pitfall 4 null tech_pattern skip, § Cell-space sizing; uses `close` NOT `adjclose`)
    - .planning/phases/27-historical-backfill/27-CONTEXT.md (D-08 as-of cap, D-12 three labels, weekly cadence)
  </read_first>
  <files>scripts/backfill-historical.ts</files>
  <action>
    Complete the per-ticker loop body in `main()`. For each ticker (not in checkpoint):

    1. **As-of cap_class (D-08):** one `yf.quoteSummary(ticker, { modules: ['summaryDetail', 'price'] })` → `marketCap` → `classifyCapClass(mc)`. This is CURRENT-cap (RESEARCH A1: historical market cap is unavailable free) — assign it once per ticker. ETFs/null marketCap → treat as `large_cap` (mirror backfill-snapshot-cap.ts line 35). Store as `capClass`.
    2. **Weekly windows:** `const end = new Date(Date.now() - 95 * 86_400_000);` (95d safety so even the 90d horizon resolves within history — mirrors price-followup's 95d window). `const start = new Date(Date.now() - YEARS*365*86_400_000);` Then `const asOfs = buildWeeklyAsOfDates(start, end);` (import from `../src/lib/backtest/windowing`).
    3. **Per window — single feature path (D-11):** `const tech = await computeTechnicalSnapshot(ticker, asOf);` (served from the disk cache via the Task 2 interception, so no extra Yahoo calls). **Skip windows where `tech == null` OR `tech.tech_pattern == null`** (RESEARCH Pitfall 4 — bar_count<200 yields null pattern, useless for cells). For a kept window, derive `priceAtScan` from `tech` (the latest close in the window — use the `close` series, NOT `adjclose`; document inline). Build a SentimentSnapshot row object: `{ ticker, scanned_at: asOf, price_at_scan: priceAtScan, community_data: { cap_class: capClass, market_cap: mc }, technical_data: tech as object, source: 'backfill' }`. NOTE scanned_at is the HISTORICAL window date (RESEARCH Pitfall 1), never the run date.
    4. **Three outcome labels per horizon (D-12):** for each `day of [3,7,14,30,60,90]`: `const recordedAt = computeOutcomeRecordedAt(asOf, day);` skip if `recordedAt.getTime() > Date.now()`. Fetch the forward close from the disk-cached bars (nearest bar to `recordedAt`). `const absPct = ((fwdClose - priceAtScan)/priceAtScan)*100;` Then replicate `computeSectorLabels`: `const etf = await getSectorETF({ ticker, asOfDate: asOf });` `const sectorRet = await fetchSectorETFReturn(etf, asOf, recordedAt);` with SPY fallback (Task 3). Build PriceOutcome: `{ days_after: day, price: fwdClose, pct_change: absPct, recorded_at: recordedAt, sector_etf: etf-or-SPY, forward_return_raw: absPct, forward_return_sector_rel: sectorRet != null ? absPct - sectorRet : null }`. (snapshot_id is set after the snapshot is created — see step 5.)
    5. **Batched idempotent writes (RESEARCH Pattern 4):** when NOT dry-run, write the ticker's snapshots via `prisma.sentimentSnapshot.createMany({ data: snapBatch, skipDuplicates: true })` (idempotent on the Plan-01 `@@unique([ticker, scanned_at])`). Because createMany does not return ids, then re-query this ticker's snapshots by `(ticker, scanned_at in asOfs)` to map `scanned_at → id`, attach `snapshot_id` to each PriceOutcome, and `prisma.priceOutcome.createMany({ data: outcomeBatch, skipDuplicates: true })` in chunks of <=1000. (PriceOutcome has no natural unique key; rely on the per-ticker checkpoint + re-run skip to avoid dupes, and document that re-running a half-completed ticker may need the checkpoint cleared — the (ticker,scanned_at) snapshot unique still prevents snapshot dupes.)
    6. In `--dry-run`: compute everything, log counts (`scanned`, `kept`, `null_pattern_skipped`, `outcomes`, `sector_fallback_to_spy`) per ticker, write NOTHING. Print a final histogram of `tech_pattern` (mirror backfill-technical.ts lines 117-120).
    7. `markDone(ticker, done)` after the ticker's writes succeed. Throttle between tickers.
    8. Final log: total snapshots, total outcomes, fallback count, and the "now trigger /api/cron/learn with $CRON_SECRET" reminder.
  </action>
  <verify>
    <automated>grep -q "source: 'backfill'" scripts/backfill-historical.ts && grep -q "createMany" scripts/backfill-historical.ts && grep -q "skipDuplicates" scripts/backfill-historical.ts && grep -q "forward_return_sector_rel" scripts/backfill-historical.ts && grep -q "computeOutcomeRecordedAt" scripts/backfill-historical.ts && npx tsc --noEmit 2>&1 | grep "backfill-historical" || echo "TSC-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "source: 'backfill'" scripts/backfill-historical.ts` >= 1 (COVERAGE-09)
    - `grep -c 'scanned_at: asOf' scripts/backfill-historical.ts` >= 1 (PIT — historical date, COVERAGE-07)
    - `grep -c 'computeOutcomeRecordedAt' scripts/backfill-historical.ts` >= 1 (recorded_at = scanned_at + days_after, COVERAGE-07)
    - All three labels present: `grep -c 'forward_return_raw' && grep -c 'forward_return_sector_rel' && grep -c 'pct_change'` each >= 1 (D-12)
    - `grep -c 'adjclose' scripts/backfill-historical.ts` returns 0 (uses `close`, D-02)
    - `grep -c 'tech_pattern == null' scripts/backfill-historical.ts` >= 1 OR equivalent null-pattern skip (RESEARCH Pitfall 4)
    - `createMany` + `skipDuplicates` both present (RESEARCH Pattern 4)
    - `npx tsc --noEmit` reports no errors in `scripts/backfill-historical.ts`
  </acceptance_criteria>
  <done>CLI writes source='backfill' weekly snapshots with historical scanned_at + as-of cap_class + technical_data, and 3-label PriceOutcome rows with recorded_at = scanned_at + days_after, via batched idempotent createMany; `close` not `adjclose`; null-pattern windows skipped; COVERAGE-06/07/09 behaviors real and runnable via --dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → prod Neon | The CLI writes ~tens of thousands of rows into the production database |
| Yahoo OHLCV/quoteSummary → CLI | Untrusted external data; bar fields and marketCap can be null/malformed |
| disk cache (`~/.cipher/backfill-cache/`) → repo/deploy | Price-only cache must never enter git or the Vercel bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Information Disclosure | `DATABASE_URL` in the CLI | mitigate | Loaded from `.env.local` (gitignored); the `if (!process.env.DATABASE_URL)` guard never echoes the value; no `console.*` line prints it (acceptance grep enforces) |
| T-27-07 | Tampering / Input Validation (ASVS V5) | Yahoo bar `close`/`marketCap` null or non-numeric | mitigate | fetchOrLoadOhlcv filters `close != null`; computeTechnicalSnapshot already drops null bars; null marketCap → `large_cap` default; null/future forward close → skip the outcome (no NaN rows) |
| T-27-08 | Information Disclosure | OHLCV disk cache leaking into repo/bundle | mitigate | Cache lives under `os.homedir()` OUTSIDE the repo tree; Plan 01 also added `.cipher/` to `.gitignore` as defense-in-depth |
| T-27-09 | data integrity | Backfill rows masquerading as live | mitigate | Every backfill row written with literal `source: 'backfill'`; Plan 03 live-only gate is the enforcement built on this tag; createMany `skipDuplicates` on `@@unique([ticker, scanned_at])` prevents re-run dupes |
| T-27-10 | data integrity (lookahead bias) | Wrong timestamps collapsing Purged-K-Fold | mitigate | scanned_at = historical asOf; recorded_at = scanned_at + days_after via pure helpers that never read Date.now() (Task 1); acceptance greps enforce |
| T-27-11 | Spoofing/Auth/Session (ASVS V2/V3/V4) | network surface | accept | N/A — local CLI, no network-exposed endpoint added |
</threat_model>

<verification>
- `npx vitest run tests/unit/backfill-pit-discipline.test.ts` GREEN (COVERAGE-07, was RED in Plan 01)
- `npx vitest run tests/unit/backfill-single-feature-path.test.ts` GREEN (COVERAGE-08, was RED in Plan 01)
- `npx tsc --noEmit` clean for `scripts/backfill-historical.ts` and `src/lib/backtest/windowing.ts`
- Greps confirm: `source: 'backfill'`, `createMany`+`skipDuplicates`, all 3 labels, no `adjclose`, no `technicalindicators` import, `os.homedir` cache, `computeTechnicalSnapshot` used
- `npm test` runs without breaking suite collection
- (Operator, post-phase) `npx tsx scripts/backfill-historical.ts --dry-run --max-tickers 5` exits 0 and logs scanned/kept/outcome counts + tech_pattern histogram
</verification>

<success_criteria>
- COVERAGE-06: CLI walks >=100-ticker universe over >=5y weekly windows (runnable, dry-run verified)
- COVERAGE-07: scanned_at historical, recorded_at = scanned_at + days_after, cap_class as-of run-time (documented current-cap proxy), `close` not `adjclose`; PIT unit test GREEN
- COVERAGE-08: single computeTechnicalSnapshot path, no forked extractor; static-audit test GREEN
- COVERAGE-09: every backfill row tagged source='backfill' (grep-verified; round-trip proven by Plan 01 integration test post-push)
- Fetch-once disk cache + per-ticker resumable checkpoint + batched idempotent writes implemented
</success_criteria>

<output>
After completion, create `.planning/phases/27-historical-backfill/27-02-SUMMARY.md`
</output>

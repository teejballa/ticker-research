---
phase: 27-historical-backfill
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - docs/paper/methodology.md
  - docs/cards/MODEL-CARD-historical-backfill.md
  - scripts/phase-27-status.ts
  - package.json
requirements: [COVERAGE-06, COVERAGE-07, COVERAGE-08, COVERAGE-09, COVERAGE-10]
tags: [docs, methodology, done-gate, testing]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "The survivorship-bias (D-03) and unadjusted-`close`-price (D-02) caveats are written down in docs/paper/methodology.md, not silently ignored"
    - "A model-card note records the as-of-cap-class proxy limitation and the live-only gate"
    - "`npm run phase-27-status` runs every Phase 27 done-gate condition and exits 0 only when all hold, else prints a punch list and exits 1"
    - "The done-gate verifies: schema source column + unique; universe >=100 & cap-balanced; CLI dry-run exits 0; live-gate enforced; vitest green; tsc clean"
  artifacts:
    - path: "docs/paper/methodology.md"
      provides: "IS-paper methodology section: survivorship + split-adjustment caveats, weekly cadence, single-feature-path, live-only gate, Purged-K-Fold compatibility"
      contains: "survivorship"
    - path: "docs/cards/MODEL-CARD-historical-backfill.md"
      provides: "Model card for the backfill bootstrap: limitations (current-cap proxy A1), provenance, intended use"
      contains: "backfill"
    - path: "scripts/phase-27-status.ts"
      provides: "Composite done-gate (exported runChecks(deps) + require.main entrypoint), mirrors scripts/model-card-status.ts"
      exports: ["runChecks"]
    - path: "package.json"
      provides: "phase-27-status npm script"
      contains: "phase-27-status"
  key_links:
    - from: "package.json"
      to: "scripts/phase-27-status.ts"
      via: "npm run phase-27-status → npx tsx"
      pattern: "phase-27-status"
    - from: "scripts/phase-27-status.ts"
      to: "src/lib/backtest/universe.ts + prisma/schema.prisma + scripts/backfill-historical.ts + src/lib/learning.ts"
      via: "static source/schema greps + universe import + CLI dry-run spawn"
      pattern: "BACKFILL_UNIVERSE"
---

<objective>
Close Phase 27: write the IS-paper methodology caveats the user explicitly required (D-03
survivorship bias, D-02 unadjusted `close` price, current-cap proxy), add a model card for the
backfill bootstrap, and ship a single composite done-gate command (`npm run phase-27-status`)
that mechanically verifies every COVERAGE-06..10 condition holds before `/gsd-verify-work`.

Purpose: The CLAUDE.md statistical-methods mandate requires every caveat to be documented (not
silently ignored) and every result reproducible from a single command. This plan makes the
phase's honesty explicit (paper + model card) and its completeness checkable (done-gate),
mirroring the proven `scripts/model-card-status.ts` gate pattern from Phase 19.

Output: `docs/paper/methodology.md`, `docs/cards/MODEL-CARD-historical-backfill.md`,
`scripts/phase-27-status.ts` (testable `runChecks(deps)` + entrypoint), and the `phase-27-status`
npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-historical-backfill/27-CONTEXT.md
@.planning/phases/27-historical-backfill/27-RESEARCH.md
@.planning/phases/27-historical-backfill/27-01-PLAN.md
@.planning/phases/27-historical-backfill/27-02-PLAN.md
@.planning/phases/27-historical-backfill/27-03-PLAN.md

<interfaces>
<!-- Done-gate pattern extracted from scripts/model-card-status.ts. Mirror it. -->

scripts/model-card-status.ts exports a testable, dependency-injected check runner:
```typescript
export type Check = { name: string; ok: boolean; detail: string };
export type RunChecksDeps = { prisma: {...}; /* + fs/exec surfaces */ };
export async function runChecks(deps: RunChecksDeps): Promise<Check[]>;
// Bottom of file:
//   if (require.main === module) { /* wire real prisma/fs/exec */; const checks = await runChecks(realDeps);
//     const failed = checks.filter(c => !c.ok); print punch list; process.exit(failed.length ? 1 : 0); }
```
The script is `#!/usr/bin/env tsx` and registered in package.json as a `npx tsx scripts/...` command.

Phase 27 artifacts the gate inspects (all shipped by Plans 01-03):
- prisma/schema.prisma — `source String @default("live")` + `@@unique([ticker, scanned_at])` on SentimentSnapshot (Plan 01)
- src/lib/backtest/universe.ts — `BACKFILL_UNIVERSE` (>=100, ~40 each large/mid/small via `tickersByCuration`), `UNIVERSE_VERSION` (Plan 01)
- scripts/backfill-historical.ts — the CLI; `--dry-run --max-tickers N` exits 0 (Plan 02)
- src/lib/learning.ts — `enforceLiveOnlyGate` + `LIVE_OUTCOME_THRESHOLD = 10` (Plan 03)
- src/app/api/cron/learn/route.ts — `enforceLiveOnlyGate` applied + `source: outcome.source ?? 'live'` in delta (Plan 03)

D-02: Yahoo `close` is split-adjusted (correct for technical patterns), NOT `adjclose` (which
applies backward dividend adjustment → forward-looking bias). D-03: Yahoo returns nothing for
delisted tickers → currently-listed universe → survivorship bias, documented not ignored.
RESEARCH A1: historical market cap unavailable free → current-cap proxy for cap_class.

CLAUDE.md statistical-methods refs to cite by chapter in methodology.md: CS229 + ISL Ch. 5
(time-series CV / Purged-K-Fold), CLAUDE.md Load-bearing rules #1 (no random k-fold) and #6
(feature-leakage / as-of timestamps).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: methodology.md + model card — survivorship + unadjusted-price + as-of-cap caveats (COVERAGE-07 residual)</name>
  <read_first>
    - .planning/phases/27-historical-backfill/27-CONTEXT.md (D-02 close-not-adjclose, D-03 survivorship, D-05/D-08 cap, D-12 sector labels, weekly cadence)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (§ Pattern 5 + A1 current-cap proxy, § Pitfall 5 sector ETF inception fallback, § Purged-K-Fold CV Pool Compatibility)
    - docs/cards/MODEL-CARD-citation-coverage.md (an existing model-card for the section shape/headers to mirror)
    - CLAUDE.md (§ "Load-bearing rules" #1 + #6, § Statistical-Methods Reference table — cite CS229 + ISL Ch.5 by name)
  </read_first>
  <files>docs/paper/methodology.md, docs/cards/MODEL-CARD-historical-backfill.md</files>
  <action>
    Create the `docs/paper/` directory and `docs/paper/methodology.md` — the IS-paper methodology
    section for the historical backfill. Required sections (real prose, not placeholders):

    1. **Title + scope** — "Historical Backfill Methodology (Phase 27)": replays >=100 tickers x
       >=5y of the technical signal class through the live feature-extraction path to bootstrap
       the lift-gate's CV pool count N. Single reproducible command: `npx tsx scripts/backfill-historical.ts`.
    2. **Universe & survivorship bias (D-03)** — the universe is a curated, versioned,
       cap-balanced static list (`src/lib/backtest/universe.ts`, `UNIVERSE_VERSION`). State
       plainly: Yahoo returns nothing for delisted tickers, so the set is effectively
       currently-listed names → SURVIVORSHIP BIAS. This is a known, documented limitation (not
       silently ignored); truly survivorship-free coverage would require a paid PIT dataset
       (deferred). Note its direction: survivorship inflates apparent returns of the surviving set.
    3. **Price series — split-adjusted `close`, not `adjclose` (D-02)** — explain that Yahoo's
       `close` is split-adjusted (correct for technical patterns) while `adjclose` applies
       backward DIVIDEND adjustment whose factor at time T uses FUTURE dividend info → forward-
       looking bias. We use `close`. Note the minor caveat that split adjustment is itself
       retroactive (a split announced later rewrites old bars) — acceptable for pattern features.
    4. **As-of cap_class proxy (D-08 / RESEARCH A1)** — historical market cap is unavailable on
       the free Yahoo tier, so `cap_class` is assigned using market cap at backfill RUN time
       (`classifyCapClass`), and the universe is hand-curated to avoid names that crossed cap
       buckets over the window. Documented simplification.
    5. **Sector-relative labels (D-12) + ETF-inception fallback (Pitfall 5)** — all three labels
       (`forward_return_raw`, `forward_return_sector_rel` vs the as-of sector ETF, `pct_change`).
       Sector ETFs created after ~2015 (XLRE, XLC) have no pre-inception prices → those windows
       fall back to SPY-relative. Documented.
    6. **PIT discipline & Purged-K-Fold compatibility** — `scanned_at` = historical window date,
       `recorded_at` = `scanned_at + days_after` (never the run date); weekly cadence. Cite
       CLAUDE.md Load-bearing rule #1 (time-series / Purged-K-Fold CV, never random k-fold — ISL
       Ch. 5) and #6 (feature-leakage audit / as-of timestamps). Explain these timestamps are what
       Phase 23's forward-chaining folds + embargo consume.
    7. **Single feature path & live-only gate** — `computeTechnicalSnapshot` is the sole extractor
       for both backfill and live (train/serve skew defense). Backfill feeds the lift-gate CV pool
       (raw membership) but a cell needs >=10 LIVE outcomes to graduate ACTIVE (D-10); Phase 18
       decay zeroes old rows in the live posterior (D-01).

    Then create `docs/cards/MODEL-CARD-historical-backfill.md` mirroring the existing model-card
    header shape (read MODEL-CARD-citation-coverage.md). Sections: Overview, Intended Use
    (bootstrap lift-gate N, NOT to graduate cells alone), Data Provenance (Yahoo `close`,
    `source='backfill'`, weekly, >=5y), Limitations (survivorship bias, current-cap proxy A1,
    sector-ETF-inception SPY fallback, split-adjustment retroactivity), and the Live-Only Gate
    safeguard (LIVE_OUTCOME_THRESHOLD=10). Cross-link `docs/paper/methodology.md`.
  </action>
  <verify>
    <automated>test -f docs/paper/methodology.md && test -f docs/cards/MODEL-CARD-historical-backfill.md && grep -qi 'survivorship' docs/paper/methodology.md && grep -qi 'adjclose' docs/paper/methodology.md && grep -qi 'Purged' docs/paper/methodology.md && grep -qi 'backfill' docs/cards/MODEL-CARD-historical-backfill.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/paper/methodology.md` exists; `grep -ic 'survivorship' docs/paper/methodology.md` >= 1 (D-03), `grep -ic 'adjclose' docs/paper/methodology.md` >= 1 (D-02 contrast), `grep -ic 'Purged' docs/paper/methodology.md` >= 1 (CLAUDE.md rule #1), `grep -ic 'as-of' docs/paper/methodology.md` >= 1
    - `docs/cards/MODEL-CARD-historical-backfill.md` exists; contains a Limitations section mentioning survivorship + current-cap proxy, and the LIVE_OUTCOME_THRESHOLD safeguard
    - methodology.md cites CS229/ISL Ch. 5 by name for time-series CV
    - No placeholder text (`TODO`/`TBD`/`lorem`) in either file (`grep -ci 'TODO\|TBD\|lorem' docs/paper/methodology.md` === 0)
  </acceptance_criteria>
  <done>methodology.md + model card written with the survivorship, unadjusted-`close`, as-of-cap, sector-fallback, and live-only-gate caveats — COVERAGE-07 documentation residual satisfied; CLAUDE.md "document, don't ignore" mandate met.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: phase-27-status composite done-gate script (mirrors scripts/model-card-status.ts) + package.json wiring</name>
  <read_first>
    - scripts/model-card-status.ts (FULL file — the `runChecks(deps)` exported pattern, `Check` type, `require.main === module` entrypoint with `process.exit`, punch-list printing; mirror it exactly)
    - src/lib/backtest/universe.ts (Plan 01 — BACKFILL_UNIVERSE, UNIVERSE_VERSION, tickersByCuration to assert >=100 + cap-balance)
    - prisma/schema.prisma (Plan 01 — the `source String @default("live")` + `@@unique([ticker, scanned_at])` lines to grep)
    - package.json (lines 5-35 scripts block — where to add the new entry; mirror the `npx tsx scripts/...` shape of model-card-status / dsr-pbo-audit)
    - .planning/phases/27-historical-backfill/27-RESEARCH.md (§ Validation Architecture → "Phase gate: Full suite green + dry-run exit 0")
  </read_first>
  <files>scripts/phase-27-status.ts, package.json</files>
  <action>
    Create `scripts/phase-27-status.ts` mirroring `scripts/model-card-status.ts`: a `#!/usr/bin/env
    tsx` file exporting `export type Check = { name: string; ok: boolean; detail: string };` and an
    `export async function runChecks(deps): Promise<Check[]>` that takes injected fs/exec/import
    surfaces (so it is unit-testable without spawning), plus a `require.main === module` entrypoint
    wiring real deps and `process.exit(failed.length ? 1 : 0)` with a printed punch list.

    The gate MUST verify these Phase 27 conditions (each a `Check`):

    1. **schema-source-column** — read `prisma/schema.prisma`; ok iff it contains
       `source String @default("live")`.
    2. **schema-unique-constraint** — ok iff schema contains `@@unique([ticker, scanned_at])`.
    3. **universe-size** — import `BACKFILL_UNIVERSE` from `../src/lib/backtest/universe`; ok iff
       `length >= 100`. detail = the count + `UNIVERSE_VERSION`.
    4. **universe-cap-balance** — import `tickersByCuration`; ok iff each of `large_cap`,
       `mid_cap`, `small_cap` has `>= 30` (target ~40); detail = the three counts.
    5. **single-feature-path** — read `scripts/backfill-historical.ts`; ok iff it contains
       `computeTechnicalSnapshot` AND does NOT match `from ['"]technicalindicators['"]`
       (COVERAGE-08).
    6. **source-tagged-writes** — read `scripts/backfill-historical.ts`; ok iff it contains
       `source: 'backfill'` (COVERAGE-09).
    7. **pit-no-adjclose** — read `scripts/backfill-historical.ts`; ok iff it does NOT contain
       `adjclose` AND contains `computeOutcomeRecordedAt` (COVERAGE-07 / D-02).
    8. **live-gate-enforced** — read `src/lib/learning.ts` (contains `enforceLiveOnlyGate` and
       `LIVE_OUTCOME_THRESHOLD = 10`) AND `src/app/api/cron/learn/route.ts` (contains
       `enforceLiveOnlyGate` and `source: outcome.source ?? 'live'`) (COVERAGE-10).
    9. **cli-dry-run** — exec `npx tsx scripts/backfill-historical.ts --dry-run --max-tickers 3`
       with a generous timeout; ok iff exit code 0. (This is the live-ish reproducibility check;
       in the injected-deps test it is mocked. Guard: if `DATABASE_URL` unset, the CLI exits 0
       cleanly per Plan 02, so the check still passes in CI.)
    10. **vitest-green** — exec `npm test`; ok iff exit 0. (Mocked in unit test of the gate.)
    11. **tsc-clean** — exec `npx tsc --noEmit`; ok iff exit 0. (Mocked in unit test.)

    Then add to `package.json` scripts block (mirror the existing `model-card-status` line):
    ```json
    "phase-27-status": "npx tsx scripts/phase-27-status.ts",
    ```
    Do NOT run the heavy exec checks (9/10/11) inside `runChecks` when a `deps.exec` mock is
    provided — they go through the injected `exec` so a unit test can assert the check list shape
    without spawning processes (same discipline as model-card-status.ts).
  </action>
  <verify>
    <automated>test -f scripts/phase-27-status.ts && grep -q 'export async function runChecks' scripts/phase-27-status.ts && grep -q 'phase-27-status' package.json && grep -q 'enforceLiveOnlyGate' scripts/phase-27-status.ts && grep -q 'BACKFILL_UNIVERSE' scripts/phase-27-status.ts && npx tsc --noEmit 2>&1 | grep 'phase-27-status' || echo "TSC-CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-27-status.ts` exists; `grep -c 'export async function runChecks' scripts/phase-27-status.ts` === 1; `grep -c 'require.main === module' scripts/phase-27-status.ts` === 1
    - The 11 named checks are present (grep each name: `schema-source-column`, `schema-unique-constraint`, `universe-size`, `universe-cap-balance`, `single-feature-path`, `source-tagged-writes`, `pit-no-adjclose`, `live-gate-enforced`, `cli-dry-run`, `vitest-green`, `tsc-clean`)
    - `grep -c 'phase-27-status' package.json` >= 1 (npm script wired)
    - `npx tsc --noEmit` reports no errors in `scripts/phase-27-status.ts`
    - `node -e "require('child_process').execSync('grep -q \"BACKFILL_UNIVERSE\" scripts/phase-27-status.ts')"` succeeds (gate imports the universe)
  </acceptance_criteria>
  <done>phase-27-status done-gate script created with 11 injected-deps checks mirroring model-card-status.ts; package.json wires `npm run phase-27-status`; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final phase verification — run the done-gate end to end</name>
  <read_first>
    - scripts/phase-27-status.ts (Task 2 — the gate being exercised)
    - .planning/phases/27-historical-backfill/27-VALIDATION.md (§ Sampling Rate "Before /gsd-verify-work: Full suite green + npx tsc --noEmit clean")
    - .planning/ROADMAP.md (lines 182-188 — Phase 27 success criteria, all six)
  </read_first>
  <files>scripts/phase-27-status.ts</files>
  <action>
    Run the full Phase 27 verification chain and fix any gate failures surfaced (the gate prints a
    punch list of unmet conditions):

    1. `npm test` — full unit suite must be green (includes the 4 backfill unit tests:
       universe, pit-discipline, single-feature-path, live-gate). Fix any red.
    2. `npx tsc --noEmit` — must be clean across the repo (no errors from any Phase 27 file).
    3. `npm run phase-27-status` — the composite done-gate must exit 0. If a check fails, the punch
       list names the unmet condition; fix the underlying artifact (do NOT weaken a check to pass).
       Note: the `cli-dry-run` check runs `--dry-run --max-tickers 3`; with no `DATABASE_URL` it
       exits 0 via the Plan 02 clean-exit guard, so the gate is CI-safe.

    This task is verification-only — it makes no code changes beyond fixing genuine failures the
    gate surfaces. The MANUAL, operator-run full backfill (`npx tsx scripts/backfill-historical.ts`
    live, then trigger `/api/cron/learn` with `$CRON_SECRET`) is recorded as a manual-only step in
    27-VALIDATION.md and is NOT part of this autonomous gate (it is long-running against live Yahoo).
  </action>
  <verify>
    <!-- FINAL GATE — expected runtime >60s (chains full unit suite + tsc + composite done-gate).
         Intentionally exceeds the per-task Nyquist <60s sampling latency; this is the phase
         sign-off task, not an inner-loop sample. -->
    <automated>npm test && npx tsc --noEmit && npm run phase-27-status</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (all unit tests green, including the four backfill tests)
    - `npx tsc --noEmit` exits 0 (no type errors anywhere)
    - `npm run phase-27-status` exits 0 (every done-gate condition holds; no check weakened to pass)
  </acceptance_criteria>
  <done>Full unit suite green, tsc clean, and `npm run phase-27-status` exits 0 — all six ROADMAP Phase 27 success criteria mechanically verified; phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| done-gate script → repo source + schema | Reads source files + schema; spawns `npm test`/`tsc`/CLI dry-run (read-only verification) |
| methodology/model-card docs → public IS paper | Documentation; no runtime surface; must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-17 | Tampering | A check weakened to make the gate falsely pass | mitigate | Task 3 acceptance forbids weakening checks; the gate asserts concrete strings (source column, unique, threshold=10, no-adjclose) that map to real artifacts |
| T-27-18 | Information Disclosure | Secrets in docs or done-gate output | mitigate | docs are price-methodology prose only; the gate prints check names + counts, never env values; `cli-dry-run` invokes the CLI which itself never echoes DATABASE_URL (Plan 02 T-27-06) |
| T-27-19 | Denial of Service / hang | `cli-dry-run`/`npm test` exec hanging the gate | mitigate | exec checks run with a bounded timeout and `--max-tickers 3`; dry-run writes nothing and exits 0 cleanly when DATABASE_URL is unset |
| T-27-20 | Spoofing/Auth/Session (ASVS V2/V3/V4) | network surface | accept | N/A — local docs + local CLI gate; no network-exposed endpoint added |
</threat_model>

<verification>
- `docs/paper/methodology.md` + `docs/cards/MODEL-CARD-historical-backfill.md` exist with survivorship/adjclose/Purged/as-of caveats (grep-verified)
- `scripts/phase-27-status.ts` exports `runChecks`, has the 11 named checks + entrypoint; `npm run phase-27-status` wired
- `npm test` green, `npx tsc --noEmit` clean, `npm run phase-27-status` exits 0
- No placeholder text in the docs; no check weakened to pass
</verification>

<success_criteria>
- COVERAGE-07 residual: survivorship-bias + unadjusted-`close` + as-of-cap caveats documented (D-02/D-03) — not silently ignored
- Single composite reproducible done-gate command (`npm run phase-27-status`) exits 0 only when all of COVERAGE-06..10 hold (CLAUDE.md single-command mandate)
- All six ROADMAP Phase 27 success criteria mechanically verifiable; phase ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/27-historical-backfill/27-04-SUMMARY.md`
</output>

---
context: community-intelligence-depth
status: ready_to_implement
last_updated: 2026-04-19T00:00:00.000Z
---

## Next Task: Deepen Community Intelligence Section

TJ's direction: Stop summarizing. The section should read like real investigative journalism — direct quotes, recurring themes, and things the community is talking about that are NOT covered elsewhere in the report.

---

### Plan

**1. Scrape more raw comments (Firecrawl)**
- Current: scrape top-level posts/headlines only
- Change: scrape comment threads too — get 50-100 raw comments per community, not just post titles
- Firecrawl's scrape or crawl endpoint; target Reddit comment pages and SeekingAlpha article comment sections

**2. Haiku extraction pass — extract recurring themes + direct quotes**
- Current: one quote per community, one-sentence summary
- Change:
  - Extract 5-10 verbatim comment quotes per community
  - Identify themes mentioned by multiple users (not just once)
  - Flag anything discussed in community that does NOT appear in the financial data or news already in the report
  - Return: `{ themes: [{ theme, frequency, quotes: string[] }], unique_to_community: string[] }`

**3. Gemini writing pass — write long-form prose, not a summary**
- Current: one sentence per community in `community_analysis`
- Change: Gemini writes a proper paragraph (150-250 words) per community that:
  - Names specific recurring topics by how often they appear ("multiple users flagged...", "a recurring concern was...")
  - Uses direct quote fragments inline ("one commenter noted '...'")
  - Explicitly calls out things only the community is discussing (not in news/fundamentals)
  - Takes a stance: is the sentiment meaningful signal or noise?
- SYSTEM_PROMPT must forbid vague summaries — require specificity, quote fragments, frequency language

**4. UI — give the section more room**
- Move Community Intelligence out of the narrow sidebar card
- Full-width section below the main report body
- Each community gets its own titled block with the long paragraph + 2-3 pulled quotes displayed as blockquotes
- Keep the sentiment badge + source icon but pair with actual substance

---

### Files to touch (find exact paths before implementing)
- Firecrawl scrape logic (community scraping route/lib)
- Haiku extraction prompt
- Gemini community analysis prompt (`SYSTEM_PROMPT` or equivalent)
- `AnalysisResult` type — expand `community_analysis` fields
- Community Intelligence UI card component
- ResearchReport layout (move card to full-width)

---

### Do NOT start implementing until you have read the current code for all 5 areas above.
